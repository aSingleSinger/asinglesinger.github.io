<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>John Doe</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>http://example.com/</id>
  <link href="http://example.com/" rel="alternate"/>
  <link href="http://example.com/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, John Doe</rights>
  <title>Hexo</title>
  <updated>2026-03-30T11:18:23.326Z</updated>
  <entry>
    <author>
      <name>John Doe</name>
    </author>
    <content>
      <![CDATA[<h1 id="MySQL"><a href="#MySQL" class="headerlink" title="MySQL"></a>MySQL</h1><p>数据库（DataBase），存储数据的仓库，数据是有组织的进行存储。<br>数据库管理系统（DataBase Management System），操纵和管理数据库的大型软件。<br>SQL，操作关系型数据库的编程语言，定义了一套操作关系型数据库的标准。<br>数据库管理系统就是比如Oracle，MySQL。通过SQL就可以操作数据库管理系统，从而操作、管理数据库。</p><blockquote><p>启动、停止服务器<br>在 windows 中</p></blockquote><ol><li>Win+R 输入 services.msc，找到 MySQL80 服务手动开启或关闭（服务名是安装的时候自己指定的）</li><li>指令启动</li></ol><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">net start mysql80</span><br><span class="line">net stop mysql80</span><br></pre></td></tr></table></figure><p>在 macos 中启动、停止服务器</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mysql.server <span class="built_in">start</span></span><br><span class="line">mysql.server stop</span><br></pre></td></tr></table></figure><blockquote><p>连接MySQL<br>在 windows 中</p></blockquote><ol><li>通过 Command Line Client，直接输入密码进行连接</li><li>通过命令行工具（需要配置MySQL环境变量）</li></ol><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysql [-h 127.0.0.1] [-P 3306] -u root -p</span><br></pre></td></tr></table></figure><p>在 macos 中<br>和 windows 里面的方法2一样</p><blockquote><p>SQL通用语法</p></blockquote><ol><li><p>SQL语句可以单行或者多行书写，用分号结尾。</p></li><li><p>MySQL不区分大小写，关键字建议用大写</p></li><li><ol start="3"><li>注释：</li></ol><ul><li>单行注释，<code>--</code>，MySQL还可以用 <code>#</code></li><li>多行注释 <code>/* */</code></li></ul></li></ol><h2 id="SQL"><a href="#SQL" class="headerlink" title="SQL"></a>SQL</h2><ul><li>DDL（Data Definition Language）数据库定义语言，用来定义或修改数据库的结构<br><strong>CREATE</strong>: 创建数据库、表、索引。<br><strong>ALTER</strong>: 修改表结构（加字段、改类型）。<br><strong>DROP</strong>: 删除表或整个数据库。<br><strong>TRUNCATE</strong>: 清空表里的所有数据，但保留表结构。</li><li>DML（Data Manipulation Language) 数据操作语言， 用于对表中的数据进行增删改。</li><li>DQL（Data Query Language）数据查询语言</li></ul><h3 id="数据库操作"><a href="#数据库操作" class="headerlink" title="数据库操作"></a>数据库操作</h3><p>查询所有数据库</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> DATABASES;</span><br></pre></td></tr></table></figure><p>查询当前数据库</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> DATABASE();</span><br></pre></td></tr></table></figure><p>创建数据库</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> DATABASE<span class="operator">/</span>SCHEMA [IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span>] 数据库名 [<span class="keyword">DEFAULT</span> CHARSET 字符集] [<span class="keyword">COLLATE</span> 排序规则];</span><br></pre></td></tr></table></figure><p>&#x3D;&#x3D;在创建数据库的时候字符集推荐使用 utf8mb4&#x3D;&#x3D;，因为 <code>utf8</code> 是存储是占用三个字节，但是有些特殊字符是占用四个字节的，所以使用 <code>uft8mb4</code>。排序规则默认是 <code>utf8m4_0900_ai_ci</code>。<br>用 <code>DATABASE</code> 或者 <code>SCHEMA</code> 效果是一样的，都是指数据库。</p><p>删除</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">DROP</span> DATABASE [IF <span class="keyword">EXISTS</span>] 数据库名;</span><br></pre></td></tr></table></figure><p>使用</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">USE 数据库名;</span><br></pre></td></tr></table></figure><h3 id="数据类型"><a href="#数据类型" class="headerlink" title="数据类型"></a>数据类型</h3><table><thead><tr><th>分类</th><th>类型</th><th>大小</th><th>有符号（SIGNED）范围</th><th>无符号（UNSIGNED）范围</th><th>描述</th></tr></thead><tbody><tr><td></td><td>TINYINT</td><td>1 安te</td><td>（-128, 127）</td><td>(0, 255)</td><td>小整数值</td></tr><tr><td></td><td>SMALLINT</td><td>2 bytes</td><td>（-32768, 32767）</td><td>(0, 65535)</td><td>大整数值</td></tr><tr><td></td><td>MEDIUMINT</td><td>3 bytes</td><td>（-8388608, 8388607）</td><td>（0, 16777215）</td><td>大整数值</td></tr><tr><td></td><td>INT或INTEGER</td><td>4 bytes</td><td>（-2147483648,2147483647）</td><td>（0, 4294967295）</td><td>大整数值</td></tr><tr><td>数值类型</td><td>BIGINT</td><td>8 bytes</td><td>（-2^63, 2^63-1）</td><td>(0, 2^64-1)</td><td>极大整数值</td></tr><tr><td></td><td>FLOAT</td><td>4 bytes</td><td>(-3.402823466 E+38, 3.402823466351 E+38)</td><td>0和（1.175494351 E-38, 3.402823466 E+38）</td><td>单精度浮点数值</td></tr><tr><td></td><td>DOUBLE</td><td>8 bytes</td><td>(-1.7976931348623157 E+308, 1.7976931348623157 E+308)</td><td>0和(2.2250738585072014 E-308, 1.7976931348623157 E+308)</td><td>双精度浮点数值</td></tr><tr><td></td><td>DECIMAL</td><td></td><td>依赖于M（精度）和D（标度）的值</td><td>依赖于M（精度）和D（标度）的值</td><td>小数值（精确定点数）</td></tr></tbody></table><blockquote><p><code>123.45</code> 即精度 5，标度 2。</p></blockquote><table><thead><tr><th>分类</th><th>类型</th><th>大小</th><th>描述</th></tr></thead><tbody><tr><td></td><td>CHAR</td><td>0-255 bytes</td><td>定长字符串</td></tr><tr><td></td><td>VARCHAR</td><td>0-65535 bytes</td><td>变长字符串</td></tr><tr><td></td><td>TINYBLOB</td><td>0-255 bytes</td><td>不超过255个字符的二进制数据</td></tr><tr><td></td><td>TINYTEXT</td><td>0-255 bytes</td><td>短文本字符串</td></tr><tr><td></td><td>BLOB</td><td>0-65535 bytes</td><td>二进制形式的长文本数据</td></tr><tr><td>字符串类型</td><td>TEXT</td><td>0-65535 bytes</td><td>长文本数据</td></tr><tr><td></td><td>MEDIUMBLOB</td><td>0-16777215 bytes</td><td>二进制形式的中等长度文本数据</td></tr><tr><td></td><td>MEDIUMTEXT</td><td>0-16777215 bytes</td><td>中等长度文本数据</td></tr><tr><td></td><td>LONGBLOB</td><td>0-4294967295 bytes</td><td>二进制形式的极大文本数据</td></tr><tr><td></td><td>LONGTEXT</td><td>0-4294967295 bytes</td><td>极大文本数据</td></tr><tr><td></td><td>BLOB</td><td>可以用来存储图片、视频、音频、文档、压缩包、软件安装包，但是效率不高，一般用专门的文件服务器存储。</td><td></td></tr></tbody></table><blockquote><p><code>varchar(10)</code> 和 <code>char(10)</code>，值代表允许的最大字符串长度，超出就会报错，但是 <code>char</code> 类型是固定长度，无论实际内容多少都是长度 <code>10</code>，但是 <code>varchar</code> 会根据内容调整，存取效率上 <code>char</code> 更高。所以用 <code>char</code> 安排固定长度的数据。</p></blockquote><table><thead><tr><th>分类</th><th>类型</th><th>大小</th><th>范围</th><th>格式</th><th>描述</th></tr></thead><tbody><tr><td></td><td>DATE</td><td>3</td><td>1000-01-01 至 9999-12-31</td><td>YYYY-MM-DD</td><td>日期值</td></tr><tr><td></td><td>TIME</td><td>3</td><td>-838:59:59 至 838:59:59</td><td>HH: MM: SS</td><td>时间值或持续时间</td></tr><tr><td>日期类型</td><td>YEAR</td><td>1</td><td>1901 至 2155</td><td>YYYY</td><td>年份值</td></tr><tr><td></td><td>DATETIME</td><td>8</td><td>1000-01-01 00:00:00<br>至 9999-12-31 23:59:59</td><td>YYYY-MM-DD HH: MM: SS</td><td>混合日期和时间值</td></tr><tr><td></td><td>TIMESTAMP</td><td>4</td><td>1970-01-01 00:00:01 至 2038-01-19 03:14:07</td><td>YYYY-MM-DD HH: MM: SS</td><td>混合日期和时间值，时间戳</td></tr></tbody></table><h3 id="表操作"><a href="#表操作" class="headerlink" title="表操作"></a>表操作</h3><h4 id="查询"><a href="#查询" class="headerlink" title="查询"></a>查询</h4><p>查询当前数据库所有表</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> TABLES;</span><br></pre></td></tr></table></figure><p>查询表结构</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">DESC</span> 表名;</span><br></pre></td></tr></table></figure><p>查询指定表的建表语句</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> <span class="keyword">CREATE TABLE</span> 表名;</span><br></pre></td></tr></table></figure><h4 id="创建表"><a href="#创建表" class="headerlink" title="创建表"></a>创建表</h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> 表名(</span><br><span class="line">字段<span class="number">1</span> 字段<span class="number">1</span>类型[COMMENT 字段<span class="number">1</span>注释],</span><br><span class="line">字段<span class="number">2</span> 字段<span class="number">2</span>类型[COMMENT 字段<span class="number">2</span>注释],</span><br><span class="line">字段<span class="number">3</span> 字段<span class="number">3</span>类型[COMMENT 字段<span class="number">3</span>注释],</span><br><span class="line">.....</span><br><span class="line">字段n 字段n类型[COMMENT 字段n注释] # 最后一个字段不要加逗号</span><br><span class="line">)[COMMENT 表注释];</span><br></pre></td></tr></table></figure><p>小例子<br>设计一张员工信息表，要求如下：</p><ol><li>﻿﻿﻿编号（纯数字）</li><li>﻿﻿﻿员工工号（字符串类型，长度不超过10位）</li><li>﻿﻿﻿员工姓名（字符串类型，长度不超过10位）</li><li>﻿﻿﻿性别（男&#x2F;女，存储一个汉字）</li><li>﻿﻿﻿年龄（正常人年龄，不可能存储负数）</li><li>﻿﻿﻿身份证号（二代身份证号均为18位，身份证中有X这样的字符）</li><li>﻿﻿﻿入职时间（取值年月日即可）</li></ol><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> emp(</span><br><span class="line">id <span class="type">INT</span> comment <span class="string">&#x27;编号&#x27;</span>,</span><br><span class="line">workno <span class="type">VARCHAR</span>(<span class="number">10</span>) comment <span class="string">&#x27;工号&#x27;</span>,</span><br><span class="line">name <span class="type">VARCHAR</span>(<span class="number">10</span>) comment <span class="string">&#x27;姓名&#x27;</span>,</span><br><span class="line">gender <span class="type">CHAR</span>(<span class="number">1</span>) comment <span class="string">&#x27;性别&#x27;</span>,</span><br><span class="line">age TINYINT UNSIGNED comment <span class="string">&#x27;年龄&#x27;</span>,</span><br><span class="line">idcard <span class="type">CHAR</span>(<span class="number">18</span>) comment <span class="string">&#x27;身份证号&#x27;</span>,</span><br><span class="line">entrydata <span class="type">DATE</span> comment <span class="string">&#x27;入职时间&#x27;</span></span><br><span class="line">) comment <span class="string">&#x27;员工表&#x27;</span>;</span><br></pre></td></tr></table></figure><h4 id="修改表"><a href="#修改表" class="headerlink" title="修改表"></a>修改表</h4><p>添加字段</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> 表名 <span class="keyword">ADD</span> 字段名 类型(长度) [COMMENT 注释] [约束];</span><br><span class="line"></span><br><span class="line"># 举例</span><br><span class="line"><span class="keyword">ALTER TABLE</span> emp <span class="keyword">ADD</span> nickname <span class="type">varchar</span>(<span class="number">20</span>) COMMENT <span class="string">&#x27;昵称&#x27;</span>;</span><br></pre></td></tr></table></figure><p>修改字段</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"># 修改数据类型</span><br><span class="line"><span class="keyword">ALTER TABLE</span> 表名 MODIFY 字段名 新数据类型(长度);</span><br><span class="line"></span><br><span class="line"># 修改字段名和字段类型</span><br><span class="line"><span class="keyword">ALTER TABLE</span> 表名 CHANGE 旧字段名 新字段名 类型(长度) [COMMENT 注释] [约束];</span><br><span class="line"><span class="keyword">ALTER TABLE</span> emp CHANGE nickname username <span class="type">varchar</span>(<span class="number">30</span>);</span><br></pre></td></tr></table></figure><p>删除字段</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> 表名 <span class="keyword">DROP</span> 字段名;</span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER TABLE</span> emp <span class="keyword">DROP</span> username;</span><br></pre></td></tr></table></figure><p>修改表名</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> 表名 RENAME <span class="keyword">TO</span> 新表名;</span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER TABLE</span> emp RENAME <span class="keyword">TO</span> employee;</span><br></pre></td></tr></table></figure><blockquote><p>整体结构 <code>ALTER TABLE ADD/MODIFY/CHANGE/DROP/RENAME TO …</code></p></blockquote><h4 id="删除表"><a href="#删除表" class="headerlink" title="删除表"></a>删除表</h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"># 删除表</span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> [IF <span class="keyword">EXISTS</span>] 表名;</span><br><span class="line"></span><br><span class="line"># 删除指定表, 并重新创建该表 <span class="comment">--&gt; 清空数据,然后重新创建表</span></span><br><span class="line"><span class="keyword">TRUNCATE</span> <span class="keyword">TABLE</span> 表名;</span><br></pre></td></tr></table></figure><h3 id="表数据操作"><a href="#表数据操作" class="headerlink" title="表数据操作"></a>表数据操作</h3><h4 id="添加数据"><a href="#添加数据" class="headerlink" title="添加数据"></a>添加数据</h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"># 给指定字段添加数据</span><br><span class="line"><span class="keyword">INSERT INTO</span> 表名(字段名<span class="number">1</span>, 字段名<span class="number">2</span>,...) <span class="keyword">VALUES</span>(值<span class="number">1</span>, 值<span class="number">2</span>,...);</span><br><span class="line"></span><br><span class="line"># 给全部字段添加数据(按照数据库字段顺序)</span><br><span class="line"><span class="keyword">INSERT INTO</span> 表名 <span class="keyword">VALUES</span>(值<span class="number">1</span>, 值<span class="number">2</span>,...);</span><br><span class="line"></span><br><span class="line"># 批量添加数据</span><br><span class="line"><span class="keyword">INSERT INTO</span> 表名(字段名<span class="number">1</span>, 字段名<span class="number">2</span>,...) <span class="keyword">VALUES</span>(值<span class="number">1</span>, 值<span class="number">2</span>,...),(值<span class="number">1</span>, 值<span class="number">2</span>,...),(值<span class="number">1</span>, 值<span class="number">2</span>,...);</span><br><span class="line"><span class="keyword">INSERT INTO</span> 表名 <span class="keyword">VALUES</span>(值<span class="number">1</span>, 值<span class="number">2</span>,...),(值<span class="number">1</span>, 值<span class="number">2</span>,...),(值<span class="number">1</span>, 值<span class="number">2</span>,...);</span><br></pre></td></tr></table></figure><ul><li>插入数据的时候，字段顺序和值的顺序要一致</li><li>&#x3D;&#x3D;字符串和日期型数据要用引号包裹&#x3D;&#x3D;</li></ul><h4 id="修改数据"><a href="#修改数据" class="headerlink" title="修改数据"></a>修改数据</h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">UPDATE</span> 表名 <span class="keyword">SET</span> 字段名<span class="number">1</span><span class="operator">=</span>值<span class="number">1</span>,字段名<span class="number">2</span><span class="operator">=</span>值<span class="number">2</span>, ...[<span class="keyword">WHERE</span> 条件];</span><br><span class="line"></span><br><span class="line"><span class="keyword">UPDATE</span> employee <span class="keyword">SET</span> name <span class="operator">=</span> <span class="string">&#x27;二狗&#x27;</span> <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure><ul><li>修改语句的条件如果没有则会修改整张表的所有数据。</li></ul><h4 id="删除数据"><a href="#删除数据" class="headerlink" title="删除数据"></a>删除数据</h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> 表名 [<span class="keyword">WHERE</span> 条件];</span><br></pre></td></tr></table></figure><ul><li>如果没有条件会删除整个表的数据。效果等价于 <code>TRUNCATE TABLE 表名;</code><br>区别如下：<ol><li><code>TRUNCATE</code> 执行速度更快</li><li>自增ID，<code>TRUNCATE</code> 会进行重置，但是 <code>DELETE</code> 会保持当前计数</li><li><code>TRUNCATE</code> 不能回滚，<code>DELETE</code> 可以</li><li><code>TRUNCATE</code> 不会触发 <code>DELETE</code> 触发器，<code>DELETE</code> 可以。</li></ol></li><li>DELETE 语句不能删除某一个字段的值（可以用UPDATE）。</li></ul><h4 id="查询数据"><a href="#查询数据" class="headerlink" title="查询数据"></a>查询数据</h4><p>&#x3D;&#x3D;书写顺序&#x3D;&#x3D;（结合后面执行顺序，二者是不同的）</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">字段列表 <span class="comment">--&gt; 字段名 [AS 别名]</span></span><br><span class="line"><span class="keyword">FROM</span></span><br><span class="line">表名列表 <span class="comment">--&gt; 表名 [AS 别名]</span></span><br><span class="line"><span class="keyword">WHERE</span></span><br><span class="line">条件列表</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span></span><br><span class="line">分组字段列表</span><br><span class="line"><span class="keyword">HAVING</span></span><br><span class="line">分组后条件列表</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span></span><br><span class="line">排序字段列表</span><br><span class="line">LIMIT</span><br><span class="line">分页参数</span><br></pre></td></tr></table></figure><h5 id="基本查询"><a href="#基本查询" class="headerlink" title="基本查询"></a>基本查询</h5><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"># 查询多个字段</span><br><span class="line"><span class="keyword">SELECT</span> 字段<span class="number">1</span>,字段<span class="number">2</span>,字段<span class="number">3</span> ... <span class="keyword">FROM</span> 表名;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> 表名;</span><br><span class="line"></span><br><span class="line"># 设置别名</span><br><span class="line"><span class="keyword">SELECT</span> 字段<span class="number">1</span> [<span class="keyword">AS</span> 别名<span class="number">1</span>], 字段<span class="number">2</span> [<span class="keyword">AS</span> 别名<span class="number">2</span>] ... <span class="keyword">FROM</span> 表名;</span><br><span class="line"></span><br><span class="line"># 去除重复记录</span><br><span class="line"><span class="keyword">SELECT</span> <span class="keyword">DISTINCT</span> 字段列表 <span class="keyword">FROM</span> 表名;</span><br></pre></td></tr></table></figure><ul><li>实际开发中，尽量不要用 <code>*</code>，首先是性能差距大（推荐罗列字段），其次是增加了耦合，比如表结构的更改，你的同事对表添加了一个字段，但是你定义的Java类还没有变动，ORM映射出现问题，开始报错。</li></ul><h5 id="条件查询"><a href="#条件查询" class="headerlink" title="条件查询"></a>条件查询</h5><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> 字段列表 <span class="keyword">FROM</span> 表名 <span class="keyword">WHERE</span> 条件列表;</span><br></pre></td></tr></table></figure><p>条件</p><table><thead><tr><th>比较运算符</th><th>功能</th></tr></thead><tbody><tr><td>&gt;</td><td>大于</td></tr><tr><td>&gt;&#x3D;</td><td>大于等于</td></tr><tr><td>&lt;</td><td>小于</td></tr><tr><td>&lt;&#x3D;</td><td>小于等于</td></tr><tr><td>&#x3D;</td><td>等于</td></tr><tr><td>&lt;&gt;或!&#x3D;</td><td>不等于</td></tr><tr><td>BETWEEN … AND …</td><td>在某个范围之内（含最小、最大值）<code>[a,b]</code></td></tr><tr><td>IN(…)</td><td>在in之后的列表中的值，多选一</td></tr><tr><td>LIKE 占位符</td><td>模糊匹配（_匹配单个字符，％匹配任意个字符）</td></tr><tr><td>IS NULL</td><td>是NULL</td></tr></tbody></table><ul><li><code>BETWEEN AND</code> 也可以用于日期和字母</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> age <span class="keyword">BETWEEN</span> <span class="number">15</span> <span class="keyword">AND</span> <span class="number">20</span>;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> age <span class="keyword">BETWEEN</span> <span class="number">20</span> <span class="keyword">AND</span> <span class="number">15</span>;  # 查不到,需要前面是最小值,后面是最大值</span><br></pre></td></tr></table></figure><ul><li>如果字符串本身含有通配符 <code>_ %</code> ，有以下几种方式</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> products <span class="keyword">WHERE</span> name <span class="keyword">LIKE</span> <span class="string">&#x27;%\%%&#x27;</span>; # 在之前加反斜杠转义符</span><br><span class="line"></span><br><span class="line"># 自定义转义符，就是自己可以指定任何一个字符作为临时转义符</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> name <span class="keyword">LIKE</span> <span class="string">&#x27;%/_%&#x27;</span> <span class="keyword">ESCAPE</span> <span class="string">&#x27;/&#x27;</span>; # 这里用 <span class="operator">/</span> 作为转义符</span><br><span class="line"></span><br><span class="line"># 特殊情况，你需要查询反斜杠本身 \</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> paths <span class="keyword">WHERE</span> folder_path <span class="keyword">LIKE</span> <span class="string">&#x27;%\\\\%&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="number">1.</span> 先是字符串解析, MySQL 把它当作字符串读取，\\ 代表本身就会转义为 \，所以最终是 <span class="string">&#x27;%\\%&#x27;</span></span><br><span class="line"><span class="number">2.</span> 然后经过 <span class="keyword">LIKE</span> 模式解析器, \\ 让 \ 之后的字符变为普通字符，所以最终是 <span class="string">&#x27;%\%&#x27;</span>，就可以查询到字符 \ 了。</span><br><span class="line"></span><br><span class="line">? 那之前的查询语句是在哪个位置转义的呢？</span><br><span class="line">而之前的 <span class="string">&#x27;%\%%&#x27;</span>，过字符串解析的时候 <span class="string">&#x27;\%&#x27;</span> 没什么含义的，还记得(\n, \r, \\ 这些转义字符吗)。所以它是在<span class="keyword">LIKE</span>模式解析器里面转义的</span><br></pre></td></tr></table></figure><table><thead><tr><th>逻辑运算符</th><th>功能</th></tr></thead><tbody><tr><td>AND 或 &amp;&amp;</td><td>并且（多个条件同时成立）</td></tr><tr><td>OR 或 ||</td><td>或者（多个条件任意一个成立）</td></tr><tr><td>NOT 或！</td><td>非，不是</td></tr></tbody></table><h5 id="聚合函数"><a href="#聚合函数" class="headerlink" title="聚合函数"></a>聚合函数</h5><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> 聚合函数(字段列表) <span class="keyword">FROM</span> 表名;</span><br></pre></td></tr></table></figure><table><thead><tr><th>函数</th><th>功能</th></tr></thead><tbody><tr><td>count</td><td>统计数量</td></tr><tr><td>max</td><td>最大值</td></tr><tr><td>min</td><td>最小值</td></tr><tr><td>avg</td><td>平均值</td></tr><tr><td>sum</td><td>求和</td></tr></tbody></table><ul><li>NULL 值不参与所有聚合函数运算</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(idCard) <span class="keyword">FROM</span> emp; # 比如数据库共 <span class="number">16</span> 条数据，但是有个人的身份证号是<span class="keyword">NULL</span>，那查出来就是 <span class="number">15</span></span><br></pre></td></tr></table></figure><h5 id="分组查询"><a href="#分组查询" class="headerlink" title="分组查询"></a>分组查询</h5><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> 字段列表 <span class="keyword">FROM</span> 表名 [<span class="keyword">WHERE</span> 条件] <span class="keyword">GROUP</span> <span class="keyword">BY</span> 分组字段名 [<span class="keyword">HAVING</span> 分组后过滤条件]</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"># 根据性别分组, 统计男性员工和女性员工的数量</span><br><span class="line"><span class="keyword">SELECT</span> gender, <span class="built_in">count</span>(<span class="operator">*</span>) <span class="keyword">from</span> emp <span class="keyword">GROUP</span> <span class="keyword">BY</span> gender; # 先查询然后分组，再对每个分组的结果去执行 <span class="keyword">SELECT</span> 后面内容的统计</span><br><span class="line"></span><br><span class="line"><span class="comment">-----------------</span></span><br><span class="line"><span class="operator">|</span>gender<span class="operator">|</span><span class="built_in">count</span>(<span class="operator">*</span>)<span class="operator">|</span></span><br><span class="line"><span class="comment">-----------------</span></span><br><span class="line"><span class="operator">|</span>女    <span class="operator">|</span> <span class="number">7</span>      <span class="operator">|</span></span><br><span class="line"><span class="operator">|</span>男    ｜ <span class="number">8</span>     ｜</span><br><span class="line"><span class="comment">-----------------</span></span><br><span class="line"></span><br><span class="line"># 查询年龄小于 <span class="number">45</span> 的员工，并根据工作地址分组，获取员工数据大于等于 <span class="number">3</span> 的工作地址</span><br><span class="line"><span class="keyword">SELECT</span> workaddress, <span class="built_in">count</span>(<span class="operator">*</span>) address_count <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> age <span class="operator">&lt;</span> <span class="number">45</span> <span class="keyword">GROUP</span> <span class="keyword">BY</span> workaddress <span class="keyword">HAVING</span> address_count <span class="operator">&gt;=</span> <span class="number">3</span>;</span><br></pre></td></tr></table></figure><blockquote><p>where 和 having 区别</p></blockquote><ol><li>where 是分组之前进行过滤，不满足where条件，不参与分组，而 having 是分组之后对结果进行过滤</li><li>where 不能对聚合函数进行判断，但是 having 可以</li></ol><blockquote><p>注意</p></blockquote><ol><li><p>&#x3D;&#x3D;执行顺序&#x3D;&#x3D;</p><ol><li>from 指定查询的表</li><li>where 每个数据过滤</li><li>group by 对数据分类打包</li><li>select 搞一下聚合函数，处理想要展示的列</li><li>having 对分组之后对结果进行筛选</li><li>order by 排序</li></ol></li><li><p>having&#x2F;order by 可以使用别名，因为别名是在 select 中定义的，执行顺序在他们两个之前，where 不行，group by 呢在标准的 SQL 里面是不行的，但是 MySQL 做了扩展，支持它是使用了。所以 group by&#x2F;having&#x2F;order by 都可以使用别名。</p></li><li><p>分组之后，查询的字段一般为聚合函数和分组字段，查询其他字段无任何意义</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"># 比如</span><br><span class="line"><span class="keyword">SELECT</span> name, gender, <span class="built_in">count</span>(<span class="operator">*</span>) <span class="keyword">FROM</span> emp <span class="keyword">GROUP</span> <span class="keyword">BY</span> gender;</span><br><span class="line"># 会返回结果</span><br><span class="line">二狗, 男, <span class="number">8</span></span><br><span class="line"># 但是男性不止二狗，这个查询返回的是第一个男性的名字，所以其他字段查询没有意义</span><br></pre></td></tr></table></figure></li></ol><h5 id="排序查询"><a href="#排序查询" class="headerlink" title="排序查询"></a>排序查询</h5><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> 字段列表 <span class="keyword">FROM</span> 表名 <span class="keyword">ORDER</span> <span class="keyword">BY</span> 字段<span class="number">1</span> 排序方式<span class="number">1</span>，字段<span class="number">2</span> 排序方式<span class="number">2</span> ...;</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">ORDER</span> <span class="keyword">BY</span> age, entrydata <span class="keyword">DESC</span>; # 不指定排序方式就默认是 <span class="keyword">ASC</span></span><br></pre></td></tr></table></figure><p>排序方式</p><ul><li>ASC：升序（默认值）</li><li>DESC：降序<br>如果是多字段排序，当第一个字段值相同时，才会根据第二个字段进行排序；</li></ul><h5 id="分页查询"><a href="#分页查询" class="headerlink" title="分页查询"></a>分页查询</h5><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> 字段列表 <span class="keyword">FROM</span> 表名 LIMIT 起始索引，查询记录数;</span><br></pre></td></tr></table></figure><ul><li>起始索引从 0 开始，起始索引 &#x3D; (查询页码 - 1) * 每页显示记录数；</li><li>分页查询是数据库的方言，不同数据库有不同的实现，MySQL中是LIMIT；</li><li>如果查询的是第一页数据，起始索引可以省略，直接简写为 LIMIT 10。</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"># 查询性别为男，且年龄在<span class="number">20</span><span class="number">-40</span>岁（含）以内的前<span class="number">5</span>个员工信息，对查询的结果按年龄升序排序，年龄相同按入职时间升序排序。</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> gender <span class="operator">=</span> <span class="string">&#x27;男&#x27;</span> <span class="keyword">AND</span> (age <span class="keyword">BETWEEN</span> <span class="number">20</span> <span class="keyword">AND</span> <span class="number">40</span>) <span class="keyword">ORDER</span> age, entrydata LIMIT <span class="number">5</span>;</span><br></pre></td></tr></table></figure><p>&#x3D;&#x3D;注意 LIMIT 要放在最后&#x3D;&#x3D;。</p><h3 id="数据控制"><a href="#数据控制" class="headerlink" title="数据控制"></a>数据控制</h3><p>用来管理数据库用户、控制数据库的访问权限。</p><ul><li>哪些用户可以管理数据库</li><li>用户可以访问服务器的哪些数据库</li></ul><h4 id="管理用户"><a href="#管理用户" class="headerlink" title="管理用户"></a>管理用户</h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"># 查询用户</span><br><span class="line">USE mysql;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> <span class="keyword">user</span>;</span><br><span class="line"></span><br><span class="line"># 创建用户</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> <span class="string">&#x27;用户名&#x27;</span>@<span class="string">&#x27;主机名&#x27;</span> IDENTIFIED <span class="keyword">BY</span> <span class="string">&#x27;密码&#x27;</span>;</span><br><span class="line"></span><br><span class="line"># 修改用户密码</span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">USER</span> <span class="string">&#x27;用户名&#x27;</span>@<span class="string">&#x27;主机名&#x27;</span> IDENTIFIED <span class="keyword">WITH</span> mysql_native_password <span class="keyword">BY</span> <span class="string">&#x27;新密码&#x27;</span>;</span><br><span class="line"></span><br><span class="line"># 删除用户</span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">USER</span> <span class="string">&#x27;用户名&#x27;</span>@<span class="string">&#x27;主机名&#x27;</span>;</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"># 创建用户只能在当前主机访问</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> <span class="string">&#x27;dog&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span> IDENTIFIED <span class="keyword">BY</span> <span class="string">&#x27;123456&#x27;</span>; # 只是创建，还没有分配数据库的访问、修改之类的权限</span><br><span class="line"></span><br><span class="line"># 创建用户可以在任意主机访问数据库</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> <span class="string">&#x27;dog&#x27;</span>@<span class="string">&#x27;%&#x27;</span> IDENTIFIED <span class="keyword">BY</span> <span class="string">&#x27;123456&#x27;</span>;</span><br></pre></td></tr></table></figure><ul><li><p>主机名可以用 <code>%</code> 通配，代表所有主机</p></li><li><p>这部分SQL开发人员用的比较少，一般是DBA（数据库管理员）使用。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184044452.png"><br>我们目前用的是 <code>root + localhost</code>，也就是 <code>root</code> 用户只能在本机访问数据库。这些用户数据也是存储到数据库里面的。</p></li></ul><h4 id="权限控制"><a href="#权限控制" class="headerlink" title="权限控制"></a>权限控制</h4><p>常用的权限有以下几种：</p><table><thead><tr><th>权限</th><th>说明</th></tr></thead><tbody><tr><td>ALL, ALL PRIVILEGES</td><td>所有权限</td></tr><tr><td>SELECT</td><td>查询数据</td></tr><tr><td>INSERT</td><td>插入数据</td></tr><tr><td>UPDATE</td><td>修改数据</td></tr><tr><td>DELETE</td><td>删除数据</td></tr><tr><td>ALTER</td><td>修改表</td></tr><tr><td>DROP</td><td>删除数据库&#x2F;表&#x2F;视图</td></tr><tr><td>CREATE</td><td>创建数据库&#x2F;表</td></tr></tbody></table><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"># 查询权限</span><br><span class="line"><span class="keyword">SHOW</span> GRANTS <span class="keyword">FOR</span> <span class="string">&#x27;用户名&#x27;</span>@<span class="string">&#x27;主机名&#x27;</span>;</span><br><span class="line"></span><br><span class="line"># 授予权限</span><br><span class="line"><span class="keyword">GRANT</span> 权限列表 <span class="keyword">ON</span> 数据库名.表名 <span class="keyword">TO</span> <span class="string">&#x27;用户名&#x27;</span>@<span class="string">&#x27;主机名&#x27;</span>;</span><br><span class="line"></span><br><span class="line"># 撤销权限</span><br><span class="line"><span class="keyword">REVOKE</span> 权限列表 <span class="keyword">ON</span> 数据库名.表名 <span class="keyword">FROM</span> <span class="string">&#x27;用户名&#x27;</span>@<span class="string">&#x27;主机名&#x27;</span>;</span><br></pre></td></tr></table></figure><ul><li>多个权限之间用逗号分隔</li><li><code>*.*</code> 代表所有数据库所有表，<code>test.*</code> 代表 <code>test</code> 数据库所有表</li><li>创建用户之后，用户只有连接到数据库服务器的权限，还没有对一些数据库的使用进行授权</li></ul><h2 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h2><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184100845.png"></p><h3 id="字符串函数"><a href="#字符串函数" class="headerlink" title="字符串函数"></a>字符串函数</h3><table><thead><tr><th>函数</th><th>功能</th></tr></thead><tbody><tr><td>CONCAT(S1,S2,…Sn)</td><td>字符串拼接，将S1，S2，…Sn拼接成一个字符串</td></tr><tr><td>LOWER(str)</td><td>将字符串str全部转为小写</td></tr><tr><td>UPPER(str)</td><td>将字符串str全部转为大写</td></tr><tr><td>LPAD(str,n,pad)</td><td>左填充，用字符串pad对str的左边进行填充，达到n个字符串长度</td></tr><tr><td>RPAD(str,n,pad)</td><td>右填充，用字符串pad对str的右边进行填充，达到n个字符串长度</td></tr><tr><td>TRIM(str)</td><td>去掉字符串头部和尾部的空格</td></tr><tr><td>SUBSTRING(str,start,len)</td><td>返回从字符串str从start位置起的len个长度的字符串</td></tr><tr><td>CHAR_LENGTH(str)</td><td>返回字符串中的字符个数。无论是一个英文、一个数字，还是一个汉字、一个表情符号（Emoji），都只算 1 个单位。</td></tr><tr><td>LENGTH(str)</td><td>字符串占用的字节数。结果取决于你数据库使用的字符集，在 UTF8MB4编码下，英文字母&#x2F;数字：占 1 字节，大部分汉字占3个字节，表情符号占4个字节。</td></tr><tr><td>执行函数语句：</td><td></td></tr></tbody></table><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> 函数(参数);</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> CONCAT(<span class="string">&#x27;Hello&#x27;</span>, <span class="string">&#x27; MySQL&#x27;</span>); # Hello, MySQL</span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">LOWER</span>(<span class="string">&#x27;Hello&#x27;</span>); # HELLO</span><br><span class="line"><span class="keyword">SELECT</span> LPAD(<span class="string">&#x27;01&#x27;</span>, <span class="number">5</span>, <span class="string">&#x27;-&#x27;</span>); # <span class="comment">---01</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">SUBSTRING</span>(<span class="string">&#x27;Hello MySQL&#x27;</span>, <span class="number">1</span>, <span class="number">5</span>); # Hello</span><br></pre></td></tr></table></figure><ul><li>&#x3D;&#x3D;substring 是从 1 开始计数，limit 是从  0 开始计数&#x3D;&#x3D;，要好好注意一下，好像MySQL大部分都是从 <code>1</code> 开始，但是比如 limit，<code>WEEK()</code>函数或者MySQL里面操作JSON数组都是从 <code>0</code> 开始计数。</li><li>substring 不给 <code>len</code> 参数，则默认取到最后</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"># 企业员工的工号为<span class="number">5</span>位数，不足五位的前面补 <span class="number">0</span>。</span><br><span class="line"><span class="keyword">UPDATE</span> emp <span class="keyword">SET</span> workno <span class="operator">=</span> LPAD(workno, <span class="number">5</span>, <span class="string">&#x27;0&#x27;</span>);</span><br></pre></td></tr></table></figure><h3 id="数值函数"><a href="#数值函数" class="headerlink" title="数值函数"></a>数值函数</h3><table><thead><tr><th>函数</th><th>功能</th></tr></thead><tbody><tr><td><code>CEIL(x)</code></td><td>向上取整</td></tr><tr><td><code>FLOOR(x)</code></td><td>向下取整</td></tr><tr><td><code>MOD(x,y)</code></td><td>返回<code>x/y</code>的模（就是余数）</td></tr><tr><td><code>RAND()</code></td><td>返回<code>0~1</code>内的随机数</td></tr><tr><td><code>ROUND(x,y)</code></td><td>求参数<code>x</code>的四舍五入的值，保留<code>y</code>位小数</td></tr></tbody></table><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> ROUND(<span class="number">2.345</span>, <span class="number">2</span>); # <span class="number">2.35</span></span><br><span class="line"></span><br><span class="line"># 生成一个六位数的随机验证码</span><br><span class="line"><span class="keyword">SELECT</span> LPAD(ROUND(RAND()<span class="operator">*</span><span class="number">1000000</span>, <span class="number">0</span>), <span class="number">6</span>, <span class="string">&#x27;0&#x27;</span>);</span><br><span class="line"># <span class="number">1.</span> RAND()<span class="operator">*</span><span class="number">1000000</span>，生成含小数的一个整数位最大为<span class="number">6</span>位的随机数</span><br><span class="line"># <span class="number">2.</span> 四舍五入，去除小数位</span><br><span class="line"># <span class="number">3.</span> 有可能整数部分不足 <span class="number">6</span> 位，进行补零</span><br></pre></td></tr></table></figure><h3 id="日期函数"><a href="#日期函数" class="headerlink" title="日期函数"></a>日期函数</h3><table><thead><tr><th>函数</th><th>功能</th></tr></thead><tbody><tr><td><code>CURDATE()</code></td><td>返回当前日期</td></tr><tr><td><code>CURTIME()</code></td><td>返回当前时间</td></tr><tr><td><code>NOW()</code></td><td>返回当前日期和时间</td></tr><tr><td><code>YEAR(date)</code></td><td>获取指定date的年份</td></tr><tr><td><code>MONTH(date)</code></td><td>获取指定date的月份</td></tr><tr><td><code>DAY(date)</code></td><td>获取指定date的日期</td></tr><tr><td><code>DATE_ADD(date, INTERVAL expr type)</code></td><td>返回一个日期&#x2F;时间值加上一个时间间隔expr后的时间值</td></tr><tr><td><code>DATEDIFF(date1, date2)</code></td><td>返回起始时间date1和结束时间date2之间的天数</td></tr></tbody></table><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> NOW(); # <span class="number">2026</span><span class="number">-02</span><span class="number">-27</span> <span class="number">13</span>:<span class="number">56</span>:<span class="number">31</span></span><br><span class="line"><span class="keyword">SELECT</span> DATE_ADD(NOW(), <span class="type">INTERVAL</span> <span class="number">70</span> <span class="keyword">DAY</span>); # <span class="number">2026</span><span class="number">-05</span><span class="number">-08</span> <span class="number">13</span>:<span class="number">56</span>:<span class="number">31</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> CURDATE(); # <span class="number">2026</span><span class="number">-02</span><span class="number">-27</span></span><br><span class="line"><span class="keyword">SELECT</span> DATE_ADD(CURDATE(), <span class="type">INTERVAL</span> <span class="number">70</span> <span class="keyword">DAY</span>); # <span class="number">2026</span><span class="number">-05</span><span class="number">-08</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> CURTIME(); # <span class="number">13</span>:<span class="number">57</span>:<span class="number">50</span></span><br><span class="line"><span class="keyword">SELECT</span> DATE_ADD(CURTIME(), <span class="type">INTERVAL</span> <span class="number">70</span> <span class="keyword">DAY</span>); # <span class="number">2026</span><span class="number">-05</span><span class="number">-08</span> <span class="number">13</span>:<span class="number">57</span>:<span class="number">50</span> <span class="comment">--&gt; 这里时间自动升级为DATETIME类型了</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> DATEDIFF(<span class="string">&#x27;2026-01-01&#x27;</span>, <span class="string">&#x27;2026-02-01&#x27;</span>); # <span class="number">-31</span></span><br><span class="line"><span class="keyword">SELECT</span> DATEDIFF(<span class="string">&#x27;2026-02-01&#x27;</span>, <span class="string">&#x27;2026-01-01&#x27;</span>); # <span class="number">31</span></span><br></pre></td></tr></table></figure><ul><li><code>DATE_ADD</code> 后面先接一个 <code>INTERVAL</code>，然后是一个数值，然后是类型，比如 <code>YEAR, MONTH, DAY, HOUR</code> 等。</li><li><code>DATEDIFF</code> 是第一个日期减去第二个日期后他们天数的差值。</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"># 查询员工入职天数，并根据入职天数倒序排序</span><br><span class="line"><span class="keyword">SELECT</span> name, DATEDIFF(CURDATE(), entrydate) <span class="keyword">AS</span> <span class="string">&#x27;entrydays&#x27;</span> <span class="keyword">FROM</span> emp <span class="keyword">ORDER</span> <span class="keyword">BY</span> entrydays <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure><h3 id="流程函数"><a href="#流程函数" class="headerlink" title="流程函数"></a>流程函数</h3><table><thead><tr><th>函数</th><th>功能</th></tr></thead><tbody><tr><td><code>IF(value, t, f)</code></td><td>如果value为true，则返回t，香则返回f</td></tr><tr><td><code>IFNULL(valuel, value2)</code></td><td>如果value1不为空，返回value1，否则返回value2</td></tr><tr><td><code>CASE WHEN [val1 ] THEN [res1] … ELSE [ default ] END</code></td><td>如果val1为true，返回res1，…否则返回default默认值</td></tr><tr><td><code>CASE [expr] WHEN [val1 ] THEN [res1] … ELSE [ default ] END</code></td><td>如果expr的值等于val1，返回res1，…否则返回default默认值</td></tr></tbody></table><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> IF(<span class="literal">TRUE</span>, <span class="string">&#x27;二狗&#x27;</span>, <span class="string">&#x27;傻狗&#x27;</span>);  # 二狗</span><br><span class="line"><span class="keyword">SELECT</span> IFNULL(<span class="string">&#x27;&#x27;</span>, <span class="string">&#x27;二狗&#x27;</span>); #  空字符串，因为 <span class="string">&#x27;&#x27;</span>是空串，不代表空</span><br><span class="line"><span class="keyword">SELECT</span> IFNULL(<span class="keyword">NULL</span>, <span class="string">&#x27;二狗&#x27;</span>);  # 二狗</span><br><span class="line"></span><br><span class="line"># 统计班级学员成绩</span><br><span class="line"># <span class="operator">&gt;=</span><span class="number">85</span> 优秀</span><br><span class="line"># <span class="operator">&gt;=</span><span class="number">60</span> 及格</span><br><span class="line"># 否则 不及格</span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">name,</span><br><span class="line">(<span class="keyword">CASE</span> <span class="keyword">WHEN</span> math<span class="operator">&gt;=</span><span class="number">85</span> <span class="keyword">THEN</span> <span class="string">&#x27;优秀&#x27;</span> <span class="keyword">WHEN</span> math<span class="operator">&gt;=</span><span class="number">60</span> <span class="keyword">THEN</span> <span class="string">&#x27;及格&#x27;</span> <span class="keyword">ELSE</span> <span class="string">&#x27;不及格&#x27;</span> <span class="keyword">END</span>) <span class="string">&#x27;数学&#x27;</span>,</span><br><span class="line"><span class="keyword">FROM</span> score;</span><br></pre></td></tr></table></figure><ul><li><code>CASE</code> 是两种形式，第一个用于数值范围（<code>a&gt;=b</code>），第二个是用于取值判断（<code>a == b</code>）。</li></ul><h2 id="约束"><a href="#约束" class="headerlink" title="约束"></a>约束</h2><p>约束是作用于表中字段上的规则，用于限制存储在表中的数据。目的是保证数据库中数据的正确、有效性和完整性。</p><table><thead><tr><th>约束</th><th>描述</th><th>关键字</th></tr></thead><tbody><tr><td>非空约束</td><td>限制该字段的数据不能为null</td><td><code>NOT NULL</code></td></tr><tr><td>唯一约束</td><td>保证该字段的所有数据都是唯一、不重复的</td><td><code>UNIQUE</code></td></tr><tr><td>主键约束</td><td>主键是一行数据的唯一标识，要求非空且唯一</td><td><code>PRIMARY KEY</code></td></tr><tr><td>默认约束</td><td>保存数据时，如果未指定该字段的值，则采用默认值</td><td><code>DEFAULT</code></td></tr><tr><td>检查约束（8.0.16版本之后）</td><td>保证字段值满足某一个条件</td><td><code>CHECK</code></td></tr><tr><td>外键约束</td><td>用来让两张表的数据之间建立连接，保证数据的一致性和完整性</td><td><code>FOREIGN KEY</code></td></tr></tbody></table><p>约束是作用于表中字段上的，可以在创建表&#x2F;修改表的时候添加约束。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> <span class="keyword">user</span>(</span><br><span class="line">id <span class="type">int</span> <span class="keyword">PRIMARY KEY</span> AUTO_INCREMENT COMMENT <span class="string">&#x27;主键&#x27;</span>, # 主键、自增长</span><br><span class="line">name <span class="type">varchar</span>(<span class="number">10</span>) <span class="keyword">NOT NULL</span> <span class="keyword">UNIQUE</span> COMMENT <span class="string">&#x27;姓名&#x27;</span>, # 不为空、唯一</span><br><span class="line">age <span class="type">int</span> <span class="keyword">CHECK</span> (age <span class="operator">&gt;</span> <span class="number">0</span> <span class="operator">&amp;&amp;</span> age <span class="operator">&lt;=</span> <span class="number">120</span>) COMMENT <span class="string">&#x27;年龄&#x27;</span>, # 检查</span><br><span class="line">status <span class="type">char</span>(<span class="number">1</span>) <span class="keyword">DEFAULT</span> <span class="string">&#x27;1&#x27;</span> COMMENT <span class="string">&#x27;状态&#x27;</span>, # 默认值</span><br><span class="line">gender <span class="type">char</span>(<span class="number">1</span>) COMMENT <span class="string">&#x27;性别&#x27;</span></span><br><span class="line">) COMMENT <span class="string">&#x27;用户表&#x27;</span>;</span><br></pre></td></tr></table></figure><h3 id="外键"><a href="#外键" class="headerlink" title="外键"></a>外键</h3><p>用来让两张表的数据之间建立连接，从而保证数据的一致性和完整性。<br>比如员工的所属部门ID和部门表。当部门表删除了一个数据，但是员工表未变动，就会出现员工还在，但是部门没有了（查询也会出现错误）。所以需要外键来保证数据一致性和完整性</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"># 添加外键</span><br><span class="line"><span class="keyword">CREATE TABLE</span> 表名(</span><br><span class="line">字段名 数据类型,</span><br><span class="line">...</span><br><span class="line">[<span class="keyword">CONSTRAINT</span>] [外键名称] <span class="keyword">FOREIGN KEY</span>(外键字段名) <span class="keyword">REFERENCES</span> 主表(主表列名)</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"># 修改表结构来添加外键</span><br><span class="line"><span class="keyword">ALTER TABLE</span> 表名 <span class="keyword">ADD CONSTRAINT</span> 外键名称 <span class="keyword">FOREIGN KEY</span>(外键字段名) <span class="keyword">REFERENCES</span> 主表(主表列名)</span><br><span class="line"></span><br><span class="line"># 删除外键</span><br><span class="line"><span class="keyword">ALTER TABLE</span> 表名 <span class="keyword">DROP</span> <span class="keyword">FOREIGN KEY</span> 外键名称;</span><br></pre></td></tr></table></figure><p>举例</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> emp <span class="keyword">ADD CONSTRAINT</span> fk_emp_dept_id <span class="keyword">FOREIGN KEY</span> (dept_id) <span class="keyword">REFERENCES</span> dept(id); # FOREIGN_KEY(本表字段名) <span class="keyword">REFERENCES</span> 要引用的表名(字段名)</span><br></pre></td></tr></table></figure><blockquote><p>删除&#x2F;更新行为</p></blockquote><table><thead><tr><th>行为</th><th>说明</th></tr></thead><tbody><tr><td><code>NO ACTION</code></td><td>当在父表中删除&#x2F;更新对应记录时，首先检查该记录是否有对应外键，如果有则不允许删除&#x2F;更新。（与 RESTRICT 一致）</td></tr><tr><td><code>RESTRICT</code></td><td>当在父表中删除&#x2F;更新对应记录时，首先检查该记录是否有对应外键，如果有则不允许删除&#x2F;更新。（与NO ACTION一致）</td></tr><tr><td><code>CASCADE</code></td><td>当在父表中删除&#x2F;更新对应记录时，首先检查该记录是否有对应外键，如果有，则也删除&#x2F;更新外键在子表中的记录。</td></tr><tr><td><code>SET NULL</code></td><td>当在父表中删除对应记录时，首先检查该记录是否有对应外键，如果有则设置子表中该外键值为null（这就要求该外键允许取null）。</td></tr><tr><td><code>SET DEFAULT</code></td><td>父表有变更时，子表将外键列设置成一个默认的值（Innodb不支持）</td></tr></tbody></table><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> emp <span class="keyword">ADD CONSTRAINT</span> fk_emp_dept_id <span class="keyword">FOREIGN KEY</span> (dept_id) <span class="keyword">REFERENCES</span> dept(id) <span class="keyword">ON</span> <span class="keyword">UPDATE</span> CASCADE <span class="keyword">ON</span> <span class="keyword">DELETE</span> CASCADE;</span><br></pre></td></tr></table></figure><p>配置时在后面加上 <code>ON UPDATE、ON DELETE</code> 对应的行为即可。如果不指定，默认行为是 <code>RESTRICT</code></p><h2 id="多表查询"><a href="#多表查询" class="headerlink" title="多表查询"></a>多表查询</h2><p>表结构之间的关系分为三种</p><ul><li>一对多<br>比如一个部门有多个员工，一个员工对应一个部门<br>实现：在多的一方建立外键，指向一的一方的主键。</li><li>多对多<br>比如学生与课程的关系<br>实现： 建立第三张中间表，中间表至少包含两个外键，分别关联两方主键</li><li>一对一<br>比如用户和用户详情的关系<br>多用于单表拆分，将一张表的基础字段放在一张表中，其他详情字段放在另一张表中，以提高操作效率<br>实现：在任意一方加入外键，关联另外一方的外键，并且设置外键为唯一的（UNIQUE）</li></ul><p>多表查询分类</p><ul><li>﻿﻿连接查询<ul><li>内连接：相当于查询A、B交集部分数据</li><li>外连接：<ul><li>左外连接：查询左表所有数据，以及两张表交集部分数据</li><li>右外连接：查询右表所有数据，以及两张表交集部分数据</li></ul></li><li>自连接：当前表与自身的连接查询，自连接必须使用表别名</li></ul></li><li>﻿﻿子查询</li></ul><h3 id="连接查询"><a href="#连接查询" class="headerlink" title="连接查询"></a>连接查询</h3><h4 id="内连接"><a href="#内连接" class="headerlink" title="内连接"></a>内连接</h4><p>内连接查询的是两张表交集的部分</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184229482.png"></p><p>隐式内连接</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> 字段列表 <span class="keyword">FROM</span> 表<span class="number">1</span>,表<span class="number">2</span> <span class="keyword">WHERE</span> 条件...;</span><br></pre></td></tr></table></figure><p>显式内连接</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> 字段列表 <span class="keyword">FROM</span> 表<span class="number">1</span> [<span class="keyword">INNER</span>] <span class="keyword">JOIN</span> 表<span class="number">2</span> <span class="keyword">ON</span> 连接条件...;</span><br></pre></td></tr></table></figure><ul><li>隐式内连接和显式内连接语法上不同，效果一样</li><li><code>INNER</code> 可省略，默认就是这个</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查询所有员工的姓名和部门信息</span></span><br><span class="line"></span><br><span class="line"># 隐式内连接</span><br><span class="line"><span class="keyword">SELECT</span> e.name, d.name </span><br><span class="line"><span class="keyword">FROM</span> emp e, dept d</span><br><span class="line"><span class="keyword">WHERE</span> e.dept_id <span class="operator">=</span> d.id;</span><br><span class="line"></span><br><span class="line"># 显式内连接</span><br><span class="line"><span class="keyword">SELECT</span> e.name, d.name </span><br><span class="line"><span class="keyword">FROM</span> emp e <span class="keyword">INNER</span> <span class="keyword">JOIN</span> dept d</span><br><span class="line"><span class="keyword">ON</span> e.dept_id <span class="operator">=</span> d.id;</span><br></pre></td></tr></table></figure><ul><li>&#x3D;&#x3D;如果为表起了别名，就不能用原来的表名了&#x3D;&#x3D;</li></ul><h4 id="外连接"><a href="#外连接" class="headerlink" title="外连接"></a>外连接</h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"># 左外连接</span><br><span class="line"><span class="keyword">SELECT</span> 字段列表 <span class="keyword">FROM</span> 表<span class="number">1</span> <span class="keyword">LEFT</span> [<span class="keyword">OUTER</span>] <span class="keyword">JOIN</span> 表<span class="number">2</span> <span class="keyword">ON</span> 条件...;</span><br><span class="line"></span><br><span class="line"># 右外连接</span><br><span class="line"><span class="keyword">SELECT</span> 字段列表 <span class="keyword">FROM</span> 表<span class="number">1</span> <span class="keyword">RIGHT</span> [<span class="keyword">OUTER</span>] <span class="keyword">JOIN</span> 表<span class="number">2</span> <span class="keyword">ON</span> 条件...;</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查询emp表的所有数据和对应的部门信息(左外连接)</span></span><br><span class="line"><span class="keyword">SELECT</span> e.<span class="operator">*</span>, d.name </span><br><span class="line"><span class="keyword">FROM</span> emp e <span class="keyword">LEFT</span> <span class="keyword">OUTER</span> <span class="keyword">JOIN</span> dept d</span><br><span class="line"><span class="keyword">ON</span> e.dept_id <span class="operator">=</span> d.id;</span><br></pre></td></tr></table></figure><ul><li><p>注意如果 A 的一行可以在 B 中匹配多行，那么内外连接都会产生多行</p></li><li><p>右外连接可以转化为左外连接（一样的）</p></li><li><p>内连接的 <code>ON</code> 相当于 <code>WHERE</code>，只有满足条件的数据才能被读取到</p></li><li><p>外连接到 <code>ON</code> 相当于配对，如果找到了 <code>e.dept_id = d.id</code> 的行，就把 <code>dept</code> 表的字段拼接上去，如果没找到，把这个字段设置为 <code>NULL</code> 然后拼接上去。<br>所以最终内连接的元素数相当于绿色部分，左连接相当于（蓝色部分+绿色部分），也就是 A 的全部，其中蓝色可以理解为没找到，设置为 <code>NULL</code> 之后拼接上去的，而绿色就是找到的。所以外连接的结果行数 &gt;&#x3D; 内连接。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184243333.png"><!-- {"width":587} --></p></li></ul><h4 id="自连接"><a href="#自连接" class="headerlink" title="自连接"></a>自连接</h4><p>比如员工有领导，领导也是员工（设置一个 <code>manager_id</code> 字段指向员工 <code>id</code>）</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> 字段列表 <span class="keyword">FROM</span> 表A 别名A <span class="keyword">JOIN</span> 表A 别名B <span class="keyword">ON</span> 条件 ...;</span><br></pre></td></tr></table></figure><p>自连接查询也可以是内连接查询，也可以是外连接查询</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查询员工及其所属领导名字</span></span><br><span class="line"><span class="keyword">SELECT</span> a.name, b.name <span class="keyword">FROM</span> emp a, emp b <span class="keyword">ON</span> a.managerid <span class="operator">=</span> b.id;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询所有员工的名字及其所属领导名字，没有领导也要查出来</span></span><br><span class="line"><span class="keyword">SELECT</span> a.name, b.name <span class="keyword">FROM</span> emp a <span class="keyword">LEFT</span> <span class="keyword">JOIN</span> emp b <span class="keyword">ON</span> a.managerid <span class="operator">=</span> b.id;</span><br></pre></td></tr></table></figure><h3 id="联合查询"><a href="#联合查询" class="headerlink" title="联合查询"></a>联合查询</h3><p>对于联合查询，就是把多次查询的结果合并起来，形成一个新的查询结果集<br>要求联合查询的多张表的列数必须保持一致，字段类型也需要保持一致。比如要返回 <code>*</code> 都返回 <code>*</code>，不能两个查询一个返回 <code>name</code> 字段，一个返回 <code>*</code>，会报错。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> 字段列表 <span class="keyword">FROM</span> 表A ...</span><br><span class="line"><span class="keyword">UNION</span> [<span class="keyword">ALL</span>]</span><br><span class="line"><span class="keyword">SELECT</span> 字段列表 <span class="keyword">FROM</span> 表B ...;</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 将工资低于5000和年龄大于50岁的员工查询出来</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> salary <span class="operator">&lt;</span> <span class="number">5000</span></span><br><span class="line"><span class="keyword">UNION</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> age <span class="operator">&gt;</span> <span class="number">50</span>; # <span class="keyword">UNION</span> 会将两次语句查询的结果进行去重</span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> salary <span class="operator">&lt;</span> <span class="number">5000</span></span><br><span class="line"><span class="keyword">UNION</span> <span class="keyword">ALL</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> age <span class="operator">&gt;</span> <span class="number">50</span>; # <span class="keyword">UNION</span> <span class="keyword">ALL</span>直接把两个语句查询的结果进行拼接，不去重</span><br></pre></td></tr></table></figure><h3 id="子查询"><a href="#子查询" class="headerlink" title="子查询"></a>子查询</h3><p>SQL语句中嵌套 SELECT 语句，称为嵌套查询，又称子查询</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> t1 <span class="keyword">WHERE</span> column1 <span class="operator">=</span> (<span class="keyword">SELECT</span> column1 <span class="keyword">FROM</span> t2);</span><br></pre></td></tr></table></figure><p>子查询外部的语句可以是INSERT&#x2F;UPDATE&#x2F;DELETE&#x2F;SELECT的任何一个</p><p>根据子查询结果不同，分为：</p><ul><li>标量子查询（子查询结果为单个值）</li><li>列子查询（子查询结果为一列）</li><li>行子查询（子查询结果为一行）</li><li>表子查询（子查询结果为多行多列）</li></ul><p>根据子查询位置，分为：WHERE之后、FROM之后、SELECT 之后。</p><h4 id="标量子查询"><a href="#标量子查询" class="headerlink" title="标量子查询"></a>标量子查询</h4><p>子查询返回的结果是单个值（数字、字符串、日期等），最简单的形式，这种子查询成为标量子查询。<br>常用的操作符：<code>= &lt;&gt; &gt; &gt;= &lt; &lt;=</code></p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查询销售部的所有员工信息</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> dept_id <span class="operator">=</span> (<span class="keyword">SELECT</span> id <span class="keyword">FROM</span> dept <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;销售部&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询在 &quot;二狗&quot; 入职之后的员工</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> entrydate <span class="operator">&gt;</span> (<span class="keyword">SELECT</span> entrydate <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;二狗&#x27;</span>);</span><br></pre></td></tr></table></figure><h4 id="列子查询"><a href="#列子查询" class="headerlink" title="列子查询"></a>列子查询</h4><p>子查询返回的结果是一列（可以是多行），这种子查询称为列子查询<br>常用的操作符：IN、NOT IN、ANY、SOME、ALL</p><table><thead><tr><th>操作符</th><th></th></tr></thead><tbody><tr><td>IN</td><td>在指定的集合范围之内，多选一</td></tr><tr><td>NOT IN</td><td>不在指定的集合范围之内</td></tr><tr><td>ANY</td><td>子查询返回列表中，有任意一个满足即可</td></tr><tr><td>SOME</td><td>与ANY等同，使用SOME的地方都可以使用ANY</td></tr><tr><td>ALL</td><td>子查询返回列表的所有值都必须满足</td></tr></tbody></table><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查询销售部和市场部的所有员工信息</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> dept_id <span class="keyword">IN</span> (<span class="keyword">SELECT</span> id <span class="keyword">FROM</span> dept <span class="keyword">WHERE</span> name <span class="keyword">IN</span> (<span class="string">&#x27;销售部&#x27;</span>, <span class="string">&#x27;市场部&#x27;</span>));</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询比财务部所有人工资都高的员工信息</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> salary <span class="operator">&gt;</span> <span class="keyword">all</span> (<span class="keyword">SELECT</span> salary <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> dept_id <span class="operator">=</span> (<span class="keyword">SELECT</span> id <span class="keyword">FROM</span> dept <span class="keyword">WHERE</span> name<span class="operator">=</span><span class="string">&#x27;财务部&#x27;</span>))</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询比研发部任意一人工资高的员工信息</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> salary <span class="operator">&gt;</span> <span class="keyword">ANY</span> (<span class="keyword">SELECT</span> salary <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> dept_id <span class="operator">=</span> (<span class="keyword">SELECT</span> id <span class="keyword">FROM</span> dept <span class="keyword">WHERE</span> name<span class="operator">=</span><span class="string">&#x27;研发部&#x27;</span>))</span><br></pre></td></tr></table></figure><h4 id="行子查询"><a href="#行子查询" class="headerlink" title="行子查询"></a>行子查询</h4><p>子查询返回的结果是一行（可以是多列），这种子查询称为行子查询。<br>常用的操作符：&#x3D;、&lt;&gt;、IN、NOT IN</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查询与二狗的薪资及直属领导相同的员工信息</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> (salary, managerid) <span class="operator">=</span> (<span class="keyword">SELECT</span> salary, managerid <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> name<span class="operator">=</span><span class="string">&#x27;二狗&#x27;</span>);</span><br></pre></td></tr></table></figure><ul><li>&#x3D;&#x3D;可以使用 <code>(a,b)</code> 的形式来进行条件的判断&#x3D;&#x3D;</li></ul><h4 id="表子查询"><a href="#表子查询" class="headerlink" title="表子查询"></a>表子查询</h4><p>子查询返回的结果是多行多列，这种子查询称为表子查询。<br>常用的操作符：IN</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查询与二狗，傻狗的职位和薪资相同的员工信息</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> (salary, job) <span class="keyword">IN</span> (<span class="keyword">SELECT</span> salary, job <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> name<span class="operator">=</span><span class="string">&#x27;二狗&#x27;</span> <span class="keyword">or</span> name<span class="operator">=</span><span class="string">&#x27;傻狗&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询入职日期是 2026-01-01 之后的员工信息，及其部门信息</span></span><br><span class="line"><span class="keyword">SELECT</span> e.<span class="operator">*</span>, d.<span class="operator">*</span> <span class="keyword">FROM</span> (<span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> entrydate <span class="operator">&gt;</span> <span class="string">&#x27;2026-01-01&#x27;</span>) e <span class="keyword">LEFT</span> <span class="keyword">JOIN</span> dept d <span class="keyword">ON</span> e.dept_id <span class="operator">=</span> d.id;</span><br></pre></td></tr></table></figure><h3 id="练习"><a href="#练习" class="headerlink" title="练习"></a>练习</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查询年龄小于 30 岁的员工的姓名、年龄、职位、部门信息</span></span><br><span class="line"><span class="keyword">SELECT</span> e.name, e.age, e.job, d.name <span class="keyword">FROM</span> emp e <span class="keyword">LEFT</span> <span class="keyword">JOIN</span> dept d <span class="keyword">ON</span> e.dept_id <span class="operator">=</span> d.id <span class="keyword">WHERE</span> e.age <span class="operator">&lt;</span> <span class="number">30</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 有一个薪资等级表，字段是 grade, losal, hisal，分别代表等级、最低薪资、最高信息，现在需要查询所有员工的薪资等级</span></span><br><span class="line"><span class="keyword">SELECT</span> e.<span class="operator">*</span>, s.grade <span class="keyword">FROM</span> emp e, salgrade s <span class="keyword">WHERE</span> e.salary <span class="operator">&gt;=</span> s.losal <span class="keyword">AND</span> e.salary <span class="operator">&lt;=</span> s.hisal;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询低于本部门平均工资的员工信息</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> emp e2 <span class="keyword">WHERE</span> salary <span class="operator">&lt;</span> (<span class="keyword">SELECT</span> <span class="built_in">avg</span>(e1.salary) <span class="keyword">FROM</span> emp e1 <span class="keyword">WHERE</span> e1.dept_id <span class="operator">=</span> e2.dept_id);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询所有的部门信息，并统计部门的人数</span></span><br><span class="line"><span class="keyword">SELECT</span> id, name, (<span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> dept_id <span class="operator">=</span> d.id) <span class="keyword">FROM</span> dept d;</span><br></pre></td></tr></table></figure><p>&#x3D;&#x3D;SELECT 中也能出现子查询&#x3D;&#x3D;</p><h2 id="事务"><a href="#事务" class="headerlink" title="事务"></a>事务</h2><p>事务是一组操作的集合，它是一个不可分割的工作单位，事务会把所有的操作作为一个整体一起向系统提交或撤销操作请求，即这些操作要么同时成功，要么同时失败。</p><p>&#x3D;&#x3D;默认MySQL的事务是自动提交的，也就是当执行一条DML语句（插入、更新、删除），MySQL会立即隐式的提交事务&#x3D;&#x3D;</p><p>比如一个银行用户表，有 id，name，money 三个字段</p><h3 id="事务操作"><a href="#事务操作" class="headerlink" title="事务操作"></a>事务操作</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"># 查看<span class="operator">/</span>设置事务提交方式</span><br><span class="line"><span class="keyword">SELECT</span> @<span class="variable">@autocommit</span>;</span><br><span class="line"><span class="keyword">SET</span> @<span class="variable">@autocommit</span><span class="operator">=</span><span class="number">0</span>;  # 设置手动提交</span><br><span class="line"></span><br><span class="line"># 提交事务</span><br><span class="line"><span class="keyword">COMMIT</span>;</span><br><span class="line"></span><br><span class="line"># 回滚事务</span><br><span class="line"><span class="keyword">ROLLBACK</span>;</span><br></pre></td></tr></table></figure><p>上面这个如果在使用事务的时候，需要先把手动提交打开，然后执行代码，如果发现成功就 <code>COMMIT</code>，如果失败就回滚。但是这样有个不好的地方，因为MySQL默认是自动提交，如果你使用手动提交，那么也就是说你执行 <code>INSERT/UPDATE/DELETE</code> 的时候都需要手动提交，也就是 <code>COMMIT</code>。下面这种是事务开启的时候是手动提交，结束之后（也就是 <code>COMMIT</code> 或者 <code>ROLLBACK</code> 之后）会自动改为自动提交，比较方便。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"># 开启事务</span><br><span class="line"><span class="keyword">START</span> TRANSACTION 或 <span class="keyword">BEGIN</span>;</span><br><span class="line"></span><br><span class="line"># 提交事务</span><br><span class="line"><span class="keyword">COMMIT</span>;</span><br><span class="line"></span><br><span class="line"># 回滚事务</span><br><span class="line"><span class="keyword">ROLLBACK</span>;</span><br></pre></td></tr></table></figure><p>举例</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">START</span> TRANSACTION # 执行后，变为了手动提交，想要结束事务，必须通过命令 <span class="keyword">COMMIT</span> 或者 <span class="keyword">ROLLBACK</span></span><br><span class="line"></span><br><span class="line"># 查询余额</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> account <span class="keyword">WHERE</span> name<span class="operator">=</span><span class="string">&#x27;二狗&#x27;</span>;</span><br><span class="line"></span><br><span class="line"># 更新二狗余额</span><br><span class="line"><span class="keyword">UPDATE</span> account <span class="keyword">SET</span> money <span class="operator">=</span> money <span class="operator">-</span> <span class="number">1000</span> <span class="keyword">WHERE</span> name<span class="operator">=</span><span class="string">&#x27;二狗&#x27;</span>;</span><br><span class="line"><span class="comment">-- 可能存在异常</span></span><br><span class="line"># 更新虎哥余额</span><br><span class="line"><span class="keyword">UPDATE</span> account <span class="keyword">SET</span> money <span class="operator">=</span> money <span class="operator">+</span> <span class="number">1000</span> <span class="keyword">WHERE</span> name<span class="operator">=</span><span class="string">&#x27;虎哥&#x27;</span>;</span><br><span class="line"></span><br><span class="line"># 如果前四个指令正确执行了，就 <span class="keyword">COMMIT</span></span><br><span class="line"><span class="keyword">COMMIT</span>;</span><br><span class="line"></span><br><span class="line"># 如果指令中间出现异常，就 <span class="keyword">ROLLBACK</span></span><br><span class="line"><span class="keyword">ROLLBACK</span>;</span><br></pre></td></tr></table></figure><p>这是数据库层面的哈，检测异常是在 Java 代码中，所以Java实现的时候是调用数据库的 <code>START TRANSACTION</code>、<code>COMMIT</code>、<code>ROLLBACK</code> 这些指令来实现事务的。</p><h3 id="事务四大特性"><a href="#事务四大特性" class="headerlink" title="事务四大特性"></a>事务四大特性</h3><p>ACID</p><ul><li>﻿﻿原子性（Atomicity）：事务是不可分割的最小操作单元，要么全部成功，要么全部失败。</li><li>﻿﻿一致性（Consistency）：事务完成时，必须使所有的数据都保持一致状态。</li><li>﻿﻿隔离性（Isolation）：数据库系统提供的隔离机制，保证事务在不受外部并发操作影响的独立环境下运行。</li><li>﻿﻿持久性（Durability）：事务一旦提交或回滚，它对数据库中的数据的改变就是永久的。</li></ul><h3 id="并发事务引发问题"><a href="#并发事务引发问题" class="headerlink" title="并发事务引发问题"></a>并发事务引发问题</h3><p>多个并发任务在执行过程中出现的问题，如下三种</p><table><thead><tr><th>问题</th><th>描述</th></tr></thead><tbody><tr><td>脏读</td><td>一个事务读到另外一个事务还没有提交的数据。</td></tr><tr><td>不可重复读</td><td>一个事务先后读取同一条记录，但两次读取的数据不同，称之为不可重复读。</td></tr><tr><td>幻读</td><td>一个事务按照条件查询数据时，没有对应的数据行，但是在插入数据时，又发现这行数据已经存在，好像出现了“幻影”。</td></tr></tbody></table><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184343134.png"></p><ol><li>事务A，执行第一个操作 select</li><li>事务A，执行第二个操作 update</li><li>事务B，执行第一个操作 select（A事务没提交，但是B读了）</li></ol><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184348381.png"></p><ol><li>事务A，执行第一个操作 select</li><li>事务B进行 commit，update 了数据</li><li>事务A执行了第二个操作</li><li>事务A执行了第三个操作 select （和第一次读的值不一样了）</li></ol><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184358213.png"></p><ol><li>事务A查询id&#x3D;1的数据</li><li>事务B插入id&#x3D;1的数据并提交</li><li>事务A插入id&#x3D;1的数据并提交（报错，主键重复）</li><li>事务A再次查询id&#x3D;1的数据，但是还是查不到<br>原因是因为查询的时候是查询的快照的数据，更新插入的时候是实际的数据。</li></ol><h3 id="事务隔离级别"><a href="#事务隔离级别" class="headerlink" title="事务隔离级别"></a>事务隔离级别</h3><p>事务隔离级别越高，数据越安全，但是性能越低。</p><table><thead><tr><th>隔离级别</th><th>赃读</th><th>不可重复读</th><th>幻读</th></tr></thead><tbody><tr><td>Read uncommitted</td><td>✅</td><td>✅</td><td>✅</td></tr><tr><td>Read committed</td><td>❌</td><td>✅</td><td>✅</td></tr><tr><td>Repeatable Read（默认）</td><td>❌</td><td>❌</td><td>✅</td></tr><tr><td>Serializable</td><td>❌</td><td>❌</td><td>❌</td></tr></tbody></table><p>MySQL 默认级别是 Repeatable Read，Oracle 是 Read committed（❌代表该隔离级别能解决的问题）</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查看事务隔离级别</span></span><br><span class="line"><span class="keyword">SELECT</span> @<span class="variable">@TRANSACTION_ISOLATION</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 设置事务隔离级别</span></span><br><span class="line"><span class="keyword">SET</span> [SESSION <span class="operator">|</span> <span class="keyword">GLOBAL</span>] TRANSACTION ISOLATION LEVEL &#123;READ UNCOMMITTED <span class="operator">|</span> READ COMMITTED <span class="operator">|</span> REPEATABLE READ <span class="operator">|</span> SERIALIZABLE&#125;</span><br></pre></td></tr></table></figure><p>SEESION 和 GLOBAL 是设置事务隔离级别是修改当前会话还是全局。<br>（课程里面去做了实际演示，可以去看看，第55节，只看这一集就行，开着弹幕）</p><h2 id="存储引擎"><a href="#存储引擎" class="headerlink" title="存储引擎"></a>存储引擎</h2><h3 id="MySQL-体系结构"><a href="#MySQL-体系结构" class="headerlink" title="MySQL 体系结构"></a>MySQL 体系结构</h3><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184426781.png"><br>连接层就控制各种语言连接数据库，你的权限，设置的最大连接数等等<br>服务层就是SQL语句执行、视图、触发器等功能<br>引擎层就包含了索引，不同的引擎索引设置不同</p><h3 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h3><p>存储引擎就是存储数据、建立索引、更新&#x2F;查询数据等技术的实现方式。存储引擎是基于表的，而不是基于库的，所以存储引擎也可被称为表类型。<br>&#x3D;&#x3D;所以同一个数据库下不同的表可以用不同的存储引擎&#x3D;&#x3D;<br>举例，我们创建表的时候一般都没指定引擎，因为默认就是 InnoDB</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查看创建表语句</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">CREATE TABLE</span> <span class="keyword">user</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE TABLE</span> `<span class="keyword">user</span>` (</span><br><span class="line">  `id` <span class="type">int</span> <span class="keyword">NOT NULL</span> AUTO_INCREMENT COMMENT <span class="string">&#x27;主键&#x27;</span>,</span><br><span class="line">  `name` <span class="type">varchar</span>(<span class="number">10</span>) <span class="keyword">NOT NULL</span> COMMENT <span class="string">&#x27;姓名&#x27;</span>,</span><br><span class="line">  `age` <span class="type">int</span> <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;年龄&#x27;</span>,</span><br><span class="line">  `status` <span class="type">char</span>(<span class="number">1</span>) <span class="keyword">DEFAULT</span> <span class="string">&#x27;1&#x27;</span> COMMENT <span class="string">&#x27;状态&#x27;</span>,</span><br><span class="line">  `gender` <span class="type">char</span>(<span class="number">1</span>) <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;性别&#x27;</span>,</span><br><span class="line">  <span class="keyword">PRIMARY KEY</span> (`id`),</span><br><span class="line">  <span class="keyword">UNIQUE</span> KEY `name` (`name`),</span><br><span class="line">  <span class="keyword">CONSTRAINT</span> `user_chk_1` <span class="keyword">CHECK</span> (((`age` <span class="operator">&gt;</span> <span class="number">0</span>) <span class="keyword">and</span> (`age` <span class="operator">&lt;=</span> <span class="number">120</span>)))</span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8mb4 <span class="keyword">COLLATE</span><span class="operator">=</span>utf8mb4_0900_ai_ci COMMENT<span class="operator">=</span><span class="string">&#x27;用户表&#x27;</span></span><br><span class="line"># ENGINE<span class="operator">=</span>InnoDB</span><br></pre></td></tr></table></figure><p>所以在创建表的时候，你可以在这里指定存储引擎</p><p>有哪些引擎？</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查看支持的引擎</span></span><br><span class="line"><span class="keyword">SHOW</span> ENGINES;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184438966.png"><br>MySQL是5.5.5之后取代MyISAM作为MySQL的默认存储引擎的<br>MEMORY 是存储在内存的。</p><h3 id="InnoDB-介绍"><a href="#InnoDB-介绍" class="headerlink" title="InnoDB 介绍"></a>InnoDB 介绍</h3><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184445285.png"></p><blockquote><p>介绍</p></blockquote><p>InnoDB是一种兼顾高可靠性和高性能的通用存储引擎，在MySQL5.5.5之后，InnoDB是默认的 MySQL 存储引擎。</p><blockquote><p>特点</p></blockquote><p>DML操作遵循ACID模型，支持&#x3D;&#x3D;事务&#x3D;&#x3D;；<br>&#x3D;&#x3D;行级锁&#x3D;&#x3D;，提高并发访问性能；<br>支持&#x3D;&#x3D;外键&#x3D;&#x3D;FOREIGN KEY约束，保证数据的完整性和正确性；</p><blockquote><p>文件</p></blockquote><p>xxx.ibd:xxx代表的是表名，innoDB引擎的每张表都会对应这样一个表空间文件，存储该表的表结构 （frm、Sdi）、数据和索引。<br>参数: innodb_file_per_table（该参数代表是每张表用一个文件还是共用一个文件，默认是每张表一个）<br>我们可以查看这个值的设置是如何</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> VARIABLES <span class="keyword">LIKE</span> <span class="string">&#x27;innodb_file_per_table&#x27;</span>;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184531068.png"></p><p>比如 macOS 系统通过 homebrew 安装到就会在 <code>/opt/homebrew/var/mysql/</code>，打开里面的一个数据库，文件如下。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184537357.png"></p><h4 id="逻辑存储结构"><a href="#逻辑存储结构" class="headerlink" title="逻辑存储结构"></a>逻辑存储结构</h4><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184545290.png"></p><ul><li><code>Page</code> 里面的 <code>Row</code> 就是存储的表数据的一行，它包含最后一次操作事务的id，指针，剩下的就是数据</li><li><code>Extent</code> 和 <code>Page</code> 大小都是固定的，所以一个区有64页</li></ul><h4 id="MyISAM"><a href="#MyISAM" class="headerlink" title="MyISAM"></a>MyISAM</h4><p>不支持事务，不支持外键，支持表锁，不支持行锁，访问速度快<br>它存储文件是三个（一张表）</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">tb_book_448.sdi  # 表结构(数字只是一个自增的序列号)</span><br><span class="line">tb_book.MYI      # 数据</span><br><span class="line">tb_book.MYD      # 索引</span><br></pre></td></tr></table></figure><h4 id="Memory"><a href="#Memory" class="headerlink" title="Memory"></a>Memory</h4><p>该引擎的表数据是存储在内存中的，由于受到硬件问题、或断电问题的影响，只能将这些表作为临时表或缓存使用。特点是内存存放，hash索引（默认）<br>文件是只有一个<br>xxx.sdi：存储表结构信息</p><h4 id="存储引擎特点"><a href="#存储引擎特点" class="headerlink" title="存储引擎特点"></a>存储引擎特点</h4><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184558669.png"><br>记住 InnoDB 和 MyISAM 的区别，支持事务，行级锁以及外键。人家官方在介绍的时候，就重点提了这几个</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184605978.png"></p><h3 id="存储引擎选择"><a href="#存储引擎选择" class="headerlink" title="存储引擎选择"></a>存储引擎选择</h3><p>在选择存储引擎时，应该根据应用系统的特点选择合适的存储引擎。对于复杂的应用系统，还可以根据实际情况选择多种存储引擎进行组合。</p><ul><li>﻿﻿InnoDB：是Mysq|的默认存储引擎，支持&#x3D;&#x3D;事务&#x3D;&#x3D;、外键。如果应用对事务的完整性有比较高的要求，在并发条件下要求数据的一致性，数据操作除了插入和查询之外，还包含很多的更新、删除操作，那么InnoDB存储引擎是比较合适的选择。</li><li>﻿﻿MyISAM ：如果应用是以读操作和插入操作为主，只有很少的更新和删除操作，并且对事务的完整性、并发性要求不是很高，那么选择这个存储引擎是非常合适的。</li><li>﻿﻿MEMORY：将所有数据保存在内存中，访问速度快，通常用于临时表及缓存。MEMORY的缺陷就是对表的大小有限制，太大的表无法缓存在内存中，而且无法保障数据的安全性。</li></ul><ul><li>MyISAM 比如日志性的数据、电商里面足迹、评论这种信息可以用这个。</li><li>一般 MyISAM 会被 MongoDB 替代，而 MEMORY 这种内存式的，会被 Redis 替代。所以这两个了解即可。</li></ul><h2 id="索引"><a href="#索引" class="headerlink" title="索引"></a>索引</h2><h3 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h3><h4 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h4><p>索引（index）是帮助MySQL高效获取数据的&#x3D;&#x3D;数据结构&#x3D;&#x3D;（有序）。在数据之外，数据库系统还维护着满足特定查找算法的数据结构，这些数据结构以某种方式引用（指向）数据，这样就可以在这些数据结构上实现高级查找算法，这种数据结构就是索引。<br>&#x3D;&#x3D;索引是一种数据结构&#x3D;&#x3D;<br>如果没有索引，就会去查询全表，效率极低</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184654218.png"></p><h4 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h4><table><thead><tr><th>优势</th><th>劣势</th></tr></thead><tbody><tr><td>提高数据检索的效率，降低数据库的IO成本</td><td>索引列也是要占用空间的。</td></tr><tr><td>通过索引列对数据进行排序，降低数据排序的成本，降低CPU的消耗。</td><td>索引大大提高了查询效率，同时却也降低更新表的速度，如对表进行INSERT、UPDATE、DELETE时，效率降低。</td></tr></tbody></table><h3 id="索引结构"><a href="#索引结构" class="headerlink" title="索引结构"></a>索引结构</h3><p>MySQL的索引是在存储引擎层实现的，不同的存储引擎有不同的结构，主要包含以下几种：</p><table><thead><tr><th>索引结构</th><th>描述</th></tr></thead><tbody><tr><td>B+Tree索引</td><td>最常见的索引类型，大部分引擎都支持B+树索引</td></tr><tr><td>Hash索引</td><td>底层数据结构是用哈希表实现的，只有精确匹配索引列的查询才有效，不支持范围查询</td></tr><tr><td>R-tree（空间索引）</td><td>空间索引是MyISAM引擎的一个特殊索引类型，主要用于地理空间数据类型，通常使用较少</td></tr><tr><td>Full-text（全文索引）</td><td>是一种通过建立倒排索引，快速匹配文档的方式。类似于Lucene,Solr,ES</td></tr></tbody></table><p>不同引擎支持索引的情况如下：</p><table><thead><tr><th><strong>索引</strong></th><th><strong>InnoDB</strong></th><th><strong>MylSAM</strong></th><th><strong>Memory</strong></th></tr></thead><tbody><tr><td>B+tree索引</td><td>支持</td><td>支持</td><td>支持</td></tr><tr><td>Hash 索引</td><td>不支持</td><td>不支持</td><td>支持</td></tr><tr><td>R-tree 索引</td><td>不支持</td><td>支持</td><td>不支持</td></tr><tr><td>Full-text</td><td>5.6版本之后支持</td><td>支持</td><td>不支持</td></tr></tbody></table><h3 id="B-tree"><a href="#B-tree" class="headerlink" title="B-tree"></a>B-tree</h3><p>先熟悉一下二叉树，二叉树的解决方式就是后面的红黑树，但是大数据量大情况下，层级深检索速度还是慢。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184808410.png"></p><ul><li>普通大二叉树极端情况下会退化成链表，查询效率从 $O(\log n)$ 变为 $O(n)$。红黑树的优势是查询、插入、删除的时间复杂度始终稳定在 $O(\log n)$。</li></ul><h4 id="B-Tree（多路平衡查找树）"><a href="#B-Tree（多路平衡查找树）" class="headerlink" title="B-Tree（多路平衡查找树）"></a>B-Tree（多路平衡查找树）</h4><p>以一颗最大度数为5（5阶）的b-tree为例（每个节点最多存储4个key，5个指针）：<br>树的度数指的是一个节点的子节点个树。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184833800.png"><br>可以看一下进阶11，有B-Tree构建过程的视频。</p><h3 id="B-Tree"><a href="#B-Tree" class="headerlink" title="B+Tree"></a>B+Tree</h3><p>以一颗最大度数为4（4阶）的b+tree为例：</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184846792.png"><br>和 B-Tree 的区别</p><ul><li>所有的数据都会出现在叶子节点，上面的节点主要起到索引作用。</li><li>叶子节点之间形成单向链表<br>也有视频可以看看，不长。</li></ul><p>MySQL索引数据结构对经典的B+Tree进行了优化，在原来的基础上，增加了指向相邻叶子节点的链表指针，形成了带有顺序指针的B+Tree，提高了区间访问的性能。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184854991.png"></p><h3 id="Hash"><a href="#Hash" class="headerlink" title="Hash"></a>Hash</h3><p>哈希索引就是采用一定的hash算法，将键值换算成新的hash值，映射到对应的槽位上，然后存储在hash表中。<br>如果两个（或多个）键值，映射到一个相同的槽位上，他们就产生了hash冲突（也称为hash碰撞），可以通过链表来解决。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330184901492.png"></p><blockquote><p>Hash索引特点</p></blockquote><ol><li>﻿﻿﻿Hash索引只能用于对等比较（&#x3D;，in），不支持范围查询（between，&gt;，&lt;，⋯）</li><li>﻿﻿﻿无法利用索引完成排序操作</li><li>﻿﻿﻿查询效率高，通常只需要一次检索就可以了(没有哈希碰撞的时候)，效率通常要高于B+tree索引</li></ol><blockquote><p>存储引擎支持</p></blockquote><p>在MySQL中，支持hash索引的是Memory引擎，而InnoDB中具有自适应hash功能，hash索引是存储引擎根据B+Tree索引在指定条件下自动构建的。</p><h3 id="索引分类"><a href="#索引分类" class="headerlink" title="索引分类"></a>索引分类</h3><table><thead><tr><th>分类</th><th>含义</th><th>特点</th><th>关键字</th></tr></thead><tbody><tr><td>主键索引</td><td>针对于表中主键创建的索引</td><td>默认自动创建，只能有一个</td><td>PRIMARY</td></tr><tr><td>唯一索引</td><td>避免同一个表中某数据列中的值重复</td><td>可以有多个</td><td>UNIQUE</td></tr><tr><td>常规索引</td><td>快速定位特定数据</td><td>可以有多个</td><td></td></tr><tr><td>全文索引</td><td>全文索引查找的是文本中的关键词，而不是比较索引中的值</td><td>可以有多个</td><td>FULLTEXT</td></tr></tbody></table><p>在InnoDB存储引擎中，根据索引的存储形式，可以分为两种</p><table><thead><tr><th>分类</th><th>含义</th><th>特点</th></tr></thead><tbody><tr><td>聚集索引（Clustered Index）</td><td>将数据存储与索引放到了一块，索引结构的叶子节点保存了行数据</td><td>必须有，而且只有一个</td></tr><tr><td>二级索引（Secondary Index）</td><td>将数据与索引分开存储，索引结构的叶子节点关联的是对应的主键</td><td>可以存在多个</td></tr></tbody></table><p>聚集索引如果没有，查的时候是不是就没有索引，，，那B+树都没法用了。</p><p>聚集索引选取规则：</p><ul><li>如果存在主键，主键索引就是聚集索引</li><li>如果不存在主键，将使用第一个唯一索引作为聚集索引</li><li>如果表没有主键，也没有唯一索引，则InnoDB会自动生成一个rowid作为隐藏的聚集索引</li></ul><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185002264.png"><br>比如上图，聚集索引下的叶子节点存储的才是完整的数据，二级索引叶子节点下存储的是索引值，这样避免数据冗余。注意哈，这里二级索引的键值就是名字（数字能比大小，名字不也能吗)</p><p>考虑查询语句</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> <span class="keyword">user</span> <span class="keyword">WHERE</span> name<span class="operator">=</span><span class="string">&#x27;Arm&#x27;</span>;</span><br></pre></td></tr></table></figure><ol><li>去二级索引上查询，查到 Arm 所在 Id 为 10</li><li>再去聚集索引上查询 Id 为 10 的值。（这里Id是主键，是聚集索引）<br>这样的话就是差了两次嗷，称为&#x3D;&#x3D;回表查询&#x3D;&#x3D;</li></ol><h3 id="索引语法"><a href="#索引语法" class="headerlink" title="索引语法"></a>索引语法</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 创建索引</span></span><br><span class="line"><span class="keyword">CREATE</span> [<span class="keyword">UNIQUE</span><span class="operator">|</span>FULLTEXT] INDEX index_name <span class="keyword">ON</span> table_name (index_col_name, ...);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看索引</span></span><br><span class="line"><span class="keyword">SHOW</span> INDEX <span class="keyword">FROM</span> table_name;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 删除索引</span></span><br><span class="line"><span class="keyword">DROP</span> INDEX index_name <span class="keyword">ON</span> table_name;</span><br></pre></td></tr></table></figure><ul><li>如果不指定 <code>UNIQUE|FULLTEXT</code>，那么就是一个普通索引。</li><li>索引命名一般为 <code>idx_表名_字段名</code></li><li>创建索引的时候可以指定多个字段，这种叫做联合索引<br>下面是练习创建索引</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- name 字段为姓名字段，该字段的值可能会重复，为该字段创建索引</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_user_name <span class="keyword">ON</span> tb_user(name);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- phone 手机号字段的值，非空且唯一，创建唯一索引</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">UNIQUE</span> idx_user_phone <span class="keyword">ON</span> tb_user(phone);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 为 profession、age、status 创建联合索引</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_user_pro_age_sta <span class="keyword">ON</span> tb_user(profession, age, status);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 为 email 建立合适的索引来提升查询效率</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_user_email <span class="keyword">ON</span> tb_user(email);</span><br></pre></td></tr></table></figure><ul><li>联合索引顺序有讲究哈，一般频繁使用的放在左边。</li></ul><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185020549.png"><br>这里展示创建完索引之后看索引的信息</p><ol><li>索引类型 <code>Index_type</code> 都是 <code>BTREE</code>，实际上是 <code>B+TREE</code>，因为B+树也属于B树。</li><li>观察 <code>idx_user_pro_age_sta</code>，它有个 <code>Seq_in_index</code>，和创建时指定的字段顺序有关。</li></ol><h3 id="SQL性能分析"><a href="#SQL性能分析" class="headerlink" title="SQL性能分析"></a>SQL性能分析</h3><h4 id="查看执行频次"><a href="#查看执行频次" class="headerlink" title="查看执行频次"></a>查看执行频次</h4><p>MySQL 客户端连接成功后，通过 <code>SHOW [SESSION | GLOBAL] STATUS</code> 命令可以提供服务器状态信息。通过如下指令，可以查看当前数据库的 <code>INSERT、UPDATE、DELETE、SELECT</code> 的访问频次：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> <span class="keyword">GLOBAL</span> STATUS <span class="keyword">LIKE</span> <span class="string">&#x27;Com_______&#x27;</span>; # 这是模糊匹配，一个下划线代表一个字符</span><br></pre></td></tr></table></figure><p>如下是执行结果，可以看到各种语句执行的次数，从而判断这个数据库它是查询次数多还是插入、更新次数多等，从而帮助选择性能优化目标。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185032367.png"></p><ul><li><code>GLOBAL</code> 是MySQL服务启动开始，到服务关闭所有的指令</li><li><code>SESSION</code> 是当前连接发生的累计状态，当连接断开时，会话就销毁了</li></ul><blockquote><p>前者是直接在数据库层面设置好，这样所有人连接的时候都是开启的，然后后者是相当于某个程序（Java程序）连接发生的累计下来的次数，如果重新连接，就归零了。<br>后者还需要说明，比如咱们用原生的JDBC，确实如此，比如还记得什么 <code>getConnection()</code>，用完立刻又 <code>close()</code>，这就是一次 <code>SESSION</code>。<br>但是如果用了比如Druid连接池，连接池会复用的，因为一个 <code>Connection</code> 这种频繁连接关闭也挺耗时，所以一个 <code>Connection</code> 不用的时候先放回池里，等连接池因为超时（ <code>max-idle-time</code> ）或者异常才真正关闭，<code>SESSION</code> 才真正关闭，记录清零。</p></blockquote><h4 id="慢查询日志"><a href="#慢查询日志" class="headerlink" title="慢查询日志"></a>慢查询日志</h4><p>慢查询日志记录了所有执行时间超过指定参数（<code>long_query_time</code>，单位：秒，默认 <code>10</code> 秒）的所有SQL语句的日志。 MySQL的慢查询日志默认没有开启，</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> VARIABLES <span class="keyword">LIKE</span> <span class="string">&#x27;slow_query_log&#x27;</span>;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185052316.png"><br>需要在MySQL的配置文件中 （<code>/etc/my.cnf</code>） 配置如下信息：（homebrew 安装的在 <code>/opt/homebrew/etc/my.cnf</code>)</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"># 开启MySQL慢日志查询开关</span><br><span class="line">slow_query_log=1</span><br><span class="line"># 设置慢日志的时间为2秒，SQL语句执行时间超过2秒，就会视为慢查询，记录慢查询日志，否则不记录</span><br><span class="line">long_query_time=2</span><br></pre></td></tr></table></figure><p>之后重启 mysqld 服务，就打开了慢查询开关，慢日志文件在 <code>/var/lib/mysql/localhost-slow.log</code>。（homebrew 安装的在 <code>/opt/homebrew/var/mysql/你的电脑名-slow.log</code>）示例如下，这是一次计数千万条数据的语句。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185059816.png"></p><h4 id="show-profiles"><a href="#show-profiles" class="headerlink" title="show profiles"></a>show profiles</h4><p><code>show profiles</code> 能够在做SQL优化时帮助我们了解时间都耗费到哪里去了。通过 <code>have_profiling</code> 参数，能够看到当前 MySQL 是否支持 <code>profile</code> 操作</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> @<span class="variable">@have_profiling</span>;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185108533.png"><br>默认 <code>profiling</code> 时关闭的，可以通过 <code>set</code> 语句在 <code>session/global</code> 级别开启 <code>profiling</code></p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> @<span class="variable">@profiling</span>;  # <span class="number">0</span></span><br><span class="line"></span><br><span class="line"># 设置为<span class="number">1</span></span><br><span class="line"><span class="keyword">SET</span> profiling<span class="operator">=</span><span class="number">1</span>;</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> @<span class="variable">@profiling</span>;  # <span class="number">1</span></span><br></pre></td></tr></table></figure><ul><li>这种 <code>@@</code> 类似于前面事务开关那个指令哈</li></ul><p>执行一系列的业务SQL的操作，然后通过如下指令查看指令的执行耗时：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"># 查看每一条<span class="keyword">SQL</span>的耗时基本情况</span><br><span class="line"><span class="keyword">SHOW</span> PROFILES;</span><br><span class="line"></span><br><span class="line"># 查看指定query_id的<span class="keyword">SQL</span>语句各个阶段的耗时情况</span><br><span class="line"><span class="keyword">SHOW</span> PROFILE <span class="keyword">FOR</span> query query_id;</span><br><span class="line"></span><br><span class="line"># 查看指定query_id的<span class="keyword">SQL</span>语句CPU的使用情况</span><br><span class="line"><span class="keyword">SHOW</span> PROFILE cpu <span class="keyword">FOR</span> query query_id;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185120205.png"><br>这里，根据 <code>name</code> 查时间要比根据 <code>id</code> 查时间长，因为前面说过的回表查询，<code>name</code> 会多查一次表。比如查看第五个，后面两列是第三个语句比第二个语句多个 CPU。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185125970.png"><br>一般开发人员不关注，太细节了。</p><h4 id="explain"><a href="#explain" class="headerlink" title="explain"></a>explain</h4><p><code>EXPLAIN</code> 或者 <code>DESC</code> 命令获取 MySQL 如何执行 <code>SELECT</code> 语句的信息，包括在 <code>SELECT</code> 语句执行过程中表如何连接和连接的顺序。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"># 直接在 <span class="keyword">select</span> 语句之前加上关键字 explain<span class="operator">/</span><span class="keyword">desc</span></span><br><span class="line">EXPLAIN <span class="keyword">SELECT</span> 字段列表 <span class="keyword">FROM</span> 表名 <span class="keyword">WHERE</span> 条件;</span><br></pre></td></tr></table></figure><p>比如有表如下</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185133608.png"><br>执行</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查询所有学生选课情况</span></span><br><span class="line"><span class="keyword">SELECT</span> s.<span class="operator">*</span>, c.<span class="operator">*</span> <span class="keyword">FROM</span> student s, course c, student_course sc <span class="keyword">WHERE</span> s.id<span class="operator">=</span>sc.studentid <span class="keyword">AND</span> c.id<span class="operator">=</span>sc.courseid;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185139173.png"></p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查看执行计划</span></span><br><span class="line">EXPLAIN <span class="keyword">SELECT</span> s.<span class="operator">*</span>, c.<span class="operator">*</span> <span class="keyword">FROM</span> student s, course c, student_course sc <span class="keyword">WHERE</span> s.id<span class="operator">=</span>sc.studentid <span class="keyword">AND</span> c.id<span class="operator">=</span>sc.courseid;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185146519.png"><br>这里面 <code>id</code> 相同，执行顺序从上到下，先执行的 student 表，再执行的 student_course 表。</p><p>现在再看一个 查询选修了 MySQL 课程的学生;(子查询)<br><img src="/MySQL/image%2034.png"><br>执行顺序是</p><ol><li><code>id=3</code> 查询 <code>course</code> 表</li><li><code>id=2</code> 查询 <code>student_course</code> 表</li><li><code>id=1</code> 按照从上到下的顺序，其实就是先是执行 <code>in (select studentid from…)</code> 这个子查询，然后是最后的 <code>select * from student s …</code></li></ol><blockquote><p>字段含义</p></blockquote><ul><li><p><code>id</code>：SELECT 查询等序号，表示查询中执行 SELECT 子句或者是操作表的顺序（id相同，执行顺序从上到下；id不同，值越大，越先执行）。</p></li><li><p><code>select_type</code>：表示 SELECT 的类型，常见的取值有 SIMPLE （简单表，即不使用表连接或者子查询）、PRIMARY（主查询，即外层的查询）、UNION（UNION 中的第二个或者后面的查询语句）、SUBQUERY（SELECT&#x2F;WHERE之后包含了子查询）等</p></li><li><p><code>type</code>：表示连接类型，性能由好到差的连接类型为 NULL、system、const、eq_ref、ref、range、index、all，优化时尽量把 <code>type</code> 往前面优化。<br><code>NULL</code> 是不查询任何表才是 NULL，咱们业务系统一般不考虑这个了，，，</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185157575.png"><br><code>system</code> 是指在查询系统表。<br><code>const</code> 一般根据主键或者唯一索引来查询是这样的</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185208127.png"><br><code>ref</code> 建立的索引是非唯一性索引的时候，查询是这个</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185213988.png"></p></li><li><p><code>possible_key</code> 显示可能应用在这张表上的索引，一个或多个。</p></li><li><p><code>key</code> 实际使用的索引，如果为 NULL，则没有使用索引</p></li><li><p><code>Key_len</code> 表示索引中使用的字节数，该值为索引字段最大可能长度，并非实际使用长度，在不损失精确性的前提下，长度越短越好</p></li><li><p><code>rows</code> MySQL认为必须要执行查询的行数，在 InnoDB 引擎的表中是一个估计值，可能并不总是准确的。</p></li><li><p><code>filtered</code> 表示返回结果的行数占需读取行数的百分比，这个值越大越好</p></li></ul><p>这个地方讲的不详细，用到的时候再去详细学吧。</p><h3 id="索引使用"><a href="#索引使用" class="headerlink" title="索引使用"></a>索引使用</h3><p>假如说你有个一千万的数据，里面有身份证号作为字段信息，但是你没有为他建立索引</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> <span class="keyword">user</span> <span class="keyword">WHERE</span> idCard <span class="operator">=</span> <span class="string">&#x27;XXXXXXXXX&#x27;</span>;</span><br></pre></td></tr></table></figure><p>这个语句执行的时间会很长（比如十几秒），因为需要遍历数据库<br>但是如果你为它创建了一个索引，无论是普通索引还是唯一索引，它查询的效率会很高！会立马出现答案（0.0几秒）</p><blockquote><p>但是当你为这个字段构建索引的时候过程会比较长，因为数据多，创建索引实际就是在创建一个B+树。</p></blockquote><h4 id="最左前缀法则"><a href="#最左前缀法则" class="headerlink" title="最左前缀法则"></a>最左前缀法则</h4><p>如果索引了多列（联合索引），要遵循最左前缀法则。最左前缀法则是指查询从索引的最左列开始，并且不跳过索引中的列。如果跳跃某一列，索引将部分失效（后面的字段索引失效）。</p><p>比如前面我们创建了一个联合索引</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 为 profession、age、status 创建联合索引</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_user_pro_age_sta <span class="keyword">ON</span> tb_user(profession, age, status);</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185223482.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185234145.png"><br>在根据 <code>profession、age、status</code> 或者 <code>profession、age</code> 或者 <code>profession</code> 查询的时候，都会使用到联合索引，注意 <code>ken_len</code><br>索引长度依次为 <code>54、49、47</code>，说明 <code>profession、age、status</code> 到索引长度依次为 <code>47,2,5</code>。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185243800.png"><br>查询时，条件不加  <code>profession</code>，根据最左前缀法则，最左边的列没使用（也就是 profession 没用），所以没有使用索引。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185252086.png"><br>再看这种，跳过了中间的 <code>age</code>，但是也加了 <code>status</code> 条件，这时候查询也是用了索引，但是 <code>key_len</code> 是 47，说明只用了 <code>profession</code> 的，<code>status</code> 没用。</p><p>这也就是前面说的，再看一遍</p><blockquote><p>如果索引了多列（联合索引），要遵循最左前缀法则。最左前缀法则是指查询从索引的最左列开始，并且不跳过索引中的列。如果跳跃某一列，索引将部分失效（后面的字段索引失效）。</p></blockquote><p>然后我们再看这个</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185259224.png"><br>查询条件是 <code>age、status、profession</code>。但是索引依旧完整能用 <code>key_len=54</code>，所以是跟 SQL 语句字段的书写顺序没用关系的。</p><p>最后看，如果联合索引中，出现范围查询(&gt;,&lt;)，范围查询右侧的列索引失效<br><img src="/MySQL/image%2043.png"><br>第一个 <code>age &gt; 30</code> ，<code>key_len=49</code>，说明 <code>status</code> 的没用，查询的时候是用的 <code>profession、age</code>。</p><h4 id="索引失效情况"><a href="#索引失效情况" class="headerlink" title="索引失效情况"></a>索引失效情况</h4><p>&#x3D;&#x3D;尽量不要对索引列进行运算，否则索引会失效&#x3D;&#x3D;<br>比如 <code>phone</code> 列建立了索引，如果单独查询会正常使用索引</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185309467.png"><br>但是比如用一些字符串函数操作</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185315852.png"><br>那么索引就会失效。</p><p>&#x3D;&#x3D;字符串类型字段使用时，不加引号，索引将失效&#x3D;&#x3D;<br>而且这种方式不规范，虽然也能查询到</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185326322.png"><br>正常情况应该如上</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185331996.png"><br>不加引号的时候，<code>type</code> 变为 <code>ALL</code> 了。</p><p>&#x3D;&#x3D;如果模糊匹配里面仅仅是尾部模糊匹配，索引不会失效。如果是头部模糊匹配，索引失效。&#x3D;&#x3D;</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185339943.png"><br>比如这里，只有第一个语句索引是生效的。</p><p>&#x3D;&#x3D;用 or 分割开的条件，如果 or 前的条件中的列有索引，而后面的列中没用索引，那么涉及的索引都不会被用到&#x3D;&#x3D;</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185351753.png"><br>比如这里 <code>age</code> 没有索引。</p><p>&#x3D;&#x3D;如果MySQL评估使用索引比全表更慢，则不使用索引&#x3D;&#x3D;</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185358574.png"><br>当查询条件是 <code>profession is null</code> 的时候，会使用索引，当查询条件使用 <code>is not null</code> 的时候，走的全表查询。但这可不是固定这样的喔<br><img src="/MySQL/image%2051.png"><br>比如我们把这个字段都设置为 <code>null</code>，然后再去查询，结果相反了。所以说MySQL会根据数据分布去评估用哪种更好。</p><p>然后呢，我们再举例一下别的，比如如下数据</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185407417.png"><br>按照 <code>phone</code> 条件去查询的时候</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185427381.png"><br>当条件是 <code>phone &gt;= &#39;17799990010&#39;</code> 的时候走的还是全表，而不是索引，下面 <code>phone &gt;= &#39;17799990013&#39;</code> 又开始走索引了。</p><h4 id="SQL-提示"><a href="#SQL-提示" class="headerlink" title="SQL 提示"></a>SQL 提示</h4><p>SQL提示，是优化数据库的一个重要手段，简单来说，就是在 SQL 语句中加入一些人为的提示来达到优化操作的目的。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- use index</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> tb_user USE INDEX(idx_user_pro) <span class="keyword">WHERE</span> profession <span class="operator">=</span> <span class="string">&#x27;软件工程&#x27;</span>;</span><br><span class="line"><span class="comment">-- ignore index</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> tb_user IGNORE INDEX(idx_user_pro) <span class="keyword">WHERE</span> profession <span class="operator">=</span> <span class="string">&#x27;软件工程&#x27;</span>;</span><br><span class="line"><span class="comment">-- force index</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> tb_user FORCE INDEX(idx_user_pro) <span class="keyword">WHERE</span> profession <span class="operator">=</span> <span class="string">&#x27;软件工程&#x27;</span>;</span><br></pre></td></tr></table></figure><p>原来 <code>profession</code> 有个联合索引</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185438730.png"></p><ul><li>第一个语句查询条件是 <code>profession = &#39;XX&#39;</code>，那么会用到聚合索引（满足最左前缀原则）</li><li>第二个语句我们为 <code>profession</code> 字段单独创建了一个索引</li><li>第三个语句再次执行查询，可以看到 <code>possible_keys</code> 可能用到的索引有两个，<code>key</code> 为最终使用的索引 <code>idx_user_pro_age_sta</code>，也就是聚合索引，这是 SQL 优化器选择的。</li></ul><p>当我们想自己选择的时候，哎，就是用上面那些语句了</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185447729.png"></p><ul><li>第一个 <code>use index(idx_user_pro)</code>，最终选择了 <code>idx_user_pro</code></li><li>第二个和第三个都是说忽略索引</li></ul><p>&#x3D;&#x3D;use index 只是推荐SQL优化器选择这个索引，但是最终用不用不一定，如果想强制使用这个索引，要用 force index&#x3D;&#x3D;</p><p>再举例如下，本来 <code>phone</code> 有索引，查询如下，会使用索引</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185457506.png"><br>但是如果 <code>ignore index</code>，那么查询的时候就不使用索引了。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185502836.png"></p><blockquote><p>有没有像前面 如果MySQL优化器评估使用索引比全表慢，就不使用索引了。现在相当于我们在手动操作。</p></blockquote><h4 id="覆盖索引-回表查询"><a href="#覆盖索引-回表查询" class="headerlink" title="覆盖索引&amp;回表查询"></a>覆盖索引&amp;回表查询</h4><p>尽量使用覆盖索引（查询使用了索引，并且需要返回的列，在该索引中已经全部能够找到），减少 <code>select *</code>。<br>其实就是回表查询，前面搞了那么多乱七八糟的索引，我们再重新说一下配置<br>表结构和表索引如下</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185514755.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185519659.png"></p><ul><li>一个 <code>id</code> 主键，一个 <code>phone</code> 唯一索引，一个 <code>name</code> 普通索引，一个 <code>profession、age、status</code> 聚合索引</li></ul><p>我们展示三个查询它的不同</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查询所有信息</span></span><br><span class="line">EXPLAIN <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> tb_user <span class="keyword">WHERE</span> profession <span class="operator">=</span> <span class="string">&#x27;计算机技术&#x27;</span> <span class="keyword">AND</span> age <span class="operator">=</span> <span class="number">23</span> <span class="keyword">AND</span> status <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询 id, profession, age, status</span></span><br><span class="line">EXPLAIN <span class="keyword">SELECT</span> id,profession,age,status <span class="keyword">FROM</span> tb_user <span class="keyword">WHERE</span> profession <span class="operator">=</span> <span class="string">&#x27;计算机技术&#x27;</span> <span class="keyword">AND</span> age <span class="operator">=</span> <span class="number">23</span> <span class="keyword">AND</span> status <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询 id, profession, age, status, name</span></span><br><span class="line">EXPLAIN <span class="keyword">SELECT</span> id,profession,age,status,name <span class="keyword">FROM</span> tb_user <span class="keyword">WHERE</span> profession <span class="operator">=</span> <span class="string">&#x27;计算机技术&#x27;</span> <span class="keyword">AND</span> age <span class="operator">=</span> <span class="number">23</span> <span class="keyword">AND</span> status <span class="operator">=</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185533963.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185540502.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185546990.png"></p><p>我们找共同点和区别，首先三个语句都用了聚合索引。不同的是 <code>Extra</code> 信息不同<br><code>select *</code> 和 <code>select id,profession,age,status,name</code> 的都是 <code>Using index condition</code>，另外一个是 <code>Using where; Using index</code>。<br>什么区别呢？</p><ul><li><code>Using index condition</code> 查询使用了索引，但是需要回表查询数据</li><li><code>Using where; Using index</code> 查找使用了索引，但是需要的数据都在索引列中能找到，所以不需要回表查询数据</li></ul><p>什么时候会涉及回表查询？看下面这个例子（课里面就这么给的例子，显得很乱了）</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185614299.png"><br>id 为主键，name 有索引。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185626916.png"></p><p>当执行语句</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> <span class="keyword">user</span> <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">2</span>;</span><br></pre></td></tr></table></figure><p>直接用聚集索引查找，索引 <code>2</code> 比索引 <code>5</code> 小，所以查最左边，<code>id</code> 找到后，返回绿色的框框 <code>row</code><br>当执行语句</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id,name <span class="keyword">FROM</span> <span class="keyword">user</span> <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;Arm&#x27;</span>;</span><br></pre></td></tr></table></figure><p>这个时候用的是辅助索引，<code>Arm</code> 比 <code>Lee</code> 小，找最左边，找到之后，因为 <code>name</code> 就作为索引值，所以 <code>name</code> 是直到的，同时辅助索引叶子节点存放的是 <code>id</code>，所以 <code>id</code> 信息也知道，所以直接返回 <code>id, name</code> 就可以了，不用再去聚集索引拿别的信息了。<br>当执行语句</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, name, gender <span class="keyword">FROM</span> <span class="keyword">user</span> <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;Arm&#x27;</span>;</span><br></pre></td></tr></table></figure><p>这个时候查完只有 <code>id, name</code> 信息，<code>gender</code> 是不知道的，所以需要回表查询，去聚集索引那里，根据辅助索引得到的 <code>id = 2</code> 再去查，得到 <code>row</code> 之后取到里面的 <code>gender</code> 信息之后一并返回。</p><p>所以除了主键、索引列这些信息之外，如果还需要别的信息，就会涉及到回表查询。</p><p>思考：一张表，有四个字段（id, username, password, status），由于数据量大，需要对以下 SQL 语句进行优化，该如何进行才是最优方案？</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, username, password <span class="keyword">FROM</span> tb_user <span class="keyword">WHERE</span> username <span class="operator">=</span> <span class="string">&#x27;二狗&#x27;</span>;</span><br></pre></td></tr></table></figure><p>应该对 <code>username, password</code> 建立聚合索引，因为如果只对 <code>username</code> 建立索引，那么还需要回表查询去找 <code>password</code> 的信息。</p><h4 id="前缀索引"><a href="#前缀索引" class="headerlink" title="前缀索引"></a>前缀索引</h4><p>当字段类型为字符串（varchar，text等）时，有时候需要索引很长的字符串，这会让索引变得很大，查询时，浪费大量的磁盘IO，影响查询效率。此时可以只将字符串的一部分前缀，建立索引，这样可以大大节约索引空间，从而提高索引效率。</p><p>语法如下，<code>(n)</code> 代表用这个字段的前 <code>n</code> 个作为键。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> INDEX idx_xxxx <span class="keyword">ON</span> table_name(cloumn(n));</span><br></pre></td></tr></table></figure><p>MySQL 在构建这个B+树的时候，就只会提取该列每个值的前 <code>n</code> 个字符，这样每个叶子节点里，存的就是 <code>前 n 个字符 + 主键 ID</code>，这样索引占用的磁盘空间直线下降，每个索引页（默认16K）能容纳更多的索引，树的层级就可能变矮，查询需要的I&#x2F;O次数就会变少。</p><p>我们举例看看查询执行做了什么<br>假设我们已经对 <code>email</code> 字段建立了长度为 <code>4</code> 对前缀索引 <code>idx_email(email(4))</code>。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> users <span class="keyword">WHERE</span> email <span class="operator">=</span> <span class="string">&#x27;zhangsan@gmail.com&#x27;</span>;</span><br></pre></td></tr></table></figure><p>MySQL 的执行引擎会做下面几步</p><ol><li>截取：提取要查询的字符串的前 <code>4</code> 个字符，也就是 <code>zhan</code>。</li><li>走索引：在 <code>idx_email</code> 的B+树中快速定位到所有前缀为 <code>zhan</code> 的记录。</li><li>获取主键：拿到这批记录对应的 ID（可能会拿到 <code>zhangsan</code>、<code>zhangwei</code>、<code>zhanli</code> 等多个不同邮箱等主键）。</li><li>强制回表（关键）：拿着这些主键ID，回到聚簇索引中查出完整的整行数据</li><li>最终对比： 在内存中将查出的完整 email 字段与 <code>zhangsan@gmail.com</code> 进行精确比对，丢弃不符合的数据，最后将真正的结果返回给你。</li></ol><p>确定前缀索引最佳长度 n 的核心技术指标叫做区分度（Selectivity，也称选择性）<br>区分度的计算公式非常简单：<br>$$<br>\text{Selectivity} &#x3D; \frac{\text{COUNT(DISTINCT LEFT(column, n))}}{\text{COUNT(*)}} \notag<br>$$<br>区分度的取值范围在 0 到 1 之间。值越接近 1，说明这个前缀的重复率越低，索引的过滤效果越好，回表次数越少。<br>计算过程通常如下，比如 <code>email</code> 哈<br>第一步，先计算完整字段的基准区分度</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(<span class="keyword">DISTINCT</span> email) <span class="operator">/</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">AS</span> selectivity_base <span class="keyword">FROM</span> users;</span><br></pre></td></tr></table></figure><p>就是看看即使用完整的建立索引，区分度最高能到多少</p><p>第二步，测量不同前缀长度 <code>n</code> 的区分度</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> </span><br><span class="line"><span class="built_in">COUNT</span>(<span class="keyword">DISTINCT</span> <span class="keyword">LEFT</span>(email, <span class="number">4</span>)) <span class="operator">/</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">AS</span> sel_4, </span><br><span class="line"><span class="built_in">COUNT</span>(<span class="keyword">DISTINCT</span> <span class="keyword">LEFT</span>(email, <span class="number">5</span>)) <span class="operator">/</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">AS</span> sel_5, </span><br><span class="line"><span class="built_in">COUNT</span>(<span class="keyword">DISTINCT</span> <span class="keyword">LEFT</span>(email, <span class="number">6</span>)) <span class="operator">/</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">AS</span> sel_6, </span><br><span class="line"><span class="built_in">COUNT</span>(<span class="keyword">DISTINCT</span> <span class="keyword">LEFT</span>(email, <span class="number">7</span>)) <span class="operator">/</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">AS</span> sel_7, </span><br><span class="line"><span class="built_in">COUNT</span>(<span class="keyword">DISTINCT</span> <span class="keyword">LEFT</span>(email, <span class="number">8</span>)) <span class="operator">/</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">AS</span> sel_8 </span><br><span class="line"><span class="keyword">FROM</span> users;</span><br></pre></td></tr></table></figure><p>第三步：根据边际收益拍板<br>执行完第二步后，得到类似这样的一组数据：</p><ul><li>sel_4 &#x3D; 0.65</li><li>sel_5 &#x3D; 0.82</li><li>sel_6 &#x3D; 0.91 <em>(大幅提升)</em></li><li>sel_7 &#x3D; 0.93 <em>(提升放缓)</em></li><li>sel_8 &#x3D; 0.94 <em>(几乎不再提升)</em><br>通常经验阈值区分度达到 0.9 左右就可以。</li></ul><p>前缀索引也存在致命限制</p><ul><li><strong>无法使用覆盖索引（Covering Index）</strong>： 即使你写的 SQL 是 <code>SELECT email FROM users WHERE email = &#39;zhangsan@gmail.com&#39;</code>（只查 email 这一列），MySQL 也<strong>必须回表</strong>去主键索引里捞出完整的 email。因为它不敢确定索引里的 ‘zhan’ 到底是不是你要的完整数据。</li><li><strong>无法用于</strong> ORDER BY <strong>排序</strong>： 因为索引里只存了前缀，前缀有序不代表完整的字符串有序。</li><li><strong>无法用于</strong> GROUP BY <strong>分组</strong>： 同理，前缀相同的记录，完整字符串可能不同，无法直接利用索引完成聚合。</li></ul><p>实际业务中很少用前缀索引</p><h4 id="单列索引-联合索引"><a href="#单列索引-联合索引" class="headerlink" title="单列索引&amp;联合索引"></a>单列索引&amp;联合索引</h4><p>创建联合索引的时候，是如下这种，先用第一个字段 <code>phone</code> 来排序，然后同一个 <code>phone</code> 才用 <code>name</code> 进行排序。所以这也是最左前缀原则。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185657040.png"><br>在业务场景中，如果存在多个查询条件，考虑针对查询字段建立索引时，建议建立联合索引，而非单列索引。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">explain <span class="keyword">select</span> id, phone, name <span class="keyword">from</span> tb _user <span class="keyword">where</span> phone<span class="operator">=</span> <span class="string">&#x27;17799990010&#x27;</span> <span class="keyword">and</span> name <span class="operator">=</span> <span class="string">&#x27;二狗&#x27;</span> ;</span><br></pre></td></tr></table></figure><p>这种又查 <code>phone</code> 又查 <code>name</code> 多的，就建立联合索引，这样也能在结果返回 <code>phone, name</code> 的时候避免回表查询。</p><p>所以，在建立索引的时候， 尽量使用联合索引（但是字段太多也不好，二三四个还行，太多不好了）。<br>常用场景</p><ol><li>&#x3D;&#x3D;多个 <code>AND</code> 条件&#x3D;&#x3D;，比如订单表中，经常根据“用户 ID”和“订单状态”来查询， 如果只有单列索引，MySQL 只能先通过 user_id 过滤出一堆记录，再回表查出整行数据来判断 status。而联合索引 (user_id, status) 可以在索引树上一次性定位到同时满足两个条件的主键 ID。</li><li>&#x3D;&#x3D;覆盖索引&#x3D;&#x3D;，减少回表查询</li><li>&#x3D;&#x3D;排序与分组&#x3D;&#x3D;，利用索引天然的有序性来替代内存排序，比如查询某个分类下的商品，并按价格从低到高排列。<code>SELECT * FROM products WHERE category_id = 1 ORDER BY price ASC;</code>。在 B+ 树中，当 category_id 相同时，price 是绝对有序存储的。MySQL 直接按顺序读取即可，不需要在内存中再执行排序算法。</li></ol><h4 id="索引设计原则"><a href="#索引设计原则" class="headerlink" title="索引设计原则"></a>索引设计原则</h4><ol><li>﻿﻿针对于数据量较大，且查询比较频繁的表建立索引。（超过一百万数据的数据库并且查询比较多，如果插入、更新比较多也没必要建立索引）</li><li>﻿﻿针对于常作为查询条件（where）、排序（order by）、分组（group by）操作的字段建立索引。</li><li>﻿﻿尽量选择区分度高的列作为索引，尽量建立唯一索引，区分度越高，使用索引的效率越高。（比如手机号、身份证号、姓名）</li><li>﻿﻿如果是字符串类型的字段，字段的长度较长，可以针对于字段的特点，建立前缀索引。</li><li>﻿﻿尽量使用联合索引，减少单列索引，查询时，联合索引很多时候可以覆盖索引，节省存储空间，避免回表，提高查询效率。(注意最左前缀原则)</li><li>﻿﻿要控制索引的数量，索引并不是多多益善，索引越多，维护索引结构的代价也就越大，会影响增删改的效率。</li><li>﻿﻿如果索引列不能存储NULL值，请在创建表时使用NOT NULL约束它。当优化器知道每列是否包含NULL值时，它可以更好地确定哪个索引最有效地用于查询。</li></ol><h2 id="SQL优化"><a href="#SQL优化" class="headerlink" title="SQL优化"></a>SQL优化</h2><h3 id="插入数据"><a href="#插入数据" class="headerlink" title="插入数据"></a>插入数据</h3><h4 id="insert-优化"><a href="#insert-优化" class="headerlink" title="insert 优化"></a>insert 优化</h4><p>当想批量插入数据的时候，不要 insert 一条一条插入了，因为每一次 insert 都需要与数据库建立连接、进行传输、事务提交等。</p><ul><li><p>批量插入</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">insert into</span> tb_test <span class="keyword">values</span>(<span class="number">1</span>, <span class="string">&#x27;ergou&#x27;</span>),(<span class="number">2</span>, <span class="string">&#x27;shagou&#x27;</span>);</span><br></pre></td></tr></table></figure><p>批量插入大小在 <code>500~1000</code> 比较合适，再多也不好</p></li><li><p>手动提交事务<br>因为MySQL是有默认自动提交事务的，所以每次 insert 都会自动提交，涉及频繁的开始事务，提交事务</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">START</span> TRANSACTION;</span><br><span class="line">insert.....</span><br><span class="line"><span class="keyword">COMMIT</span>;</span><br></pre></td></tr></table></figure></li><li><p>主键顺序插入<br>主键按照主键值从小大到插入，比如 <code>1, 2, 5, 10, 20</code> 这个顺序插入比 <code>20 10 1 5 2</code> 要快。</p></li></ul><h4 id="大批量数据插入"><a href="#大批量数据插入" class="headerlink" title="大批量数据插入"></a>大批量数据插入</h4><p>如果一次性需要插入大批量数据（比如100万条），使用insert语句插入性能较低，此时可以使用MySQL数据库提供的load指令进行插入。操作如下：</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185713413.png"><br>怎么插入呢？</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 客户端连接服务器的时候，加上参数 --local-infile</span></span><br><span class="line">mysql <span class="comment">--local-infile -u root -p</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 设置全局参数 local_infile 为 1, 开始从本地加载文件导入数据的开关</span></span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">GLOBAL</span> local_infile <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 执行 load 指令将准备好的数据, 加载到表结构中</span></span><br><span class="line">LOAD DATA <span class="keyword">LOCAL</span> INFILE <span class="string">&#x27;/root/sql1.log&#x27;</span> <span class="keyword">INTO</span> <span class="keyword">TABLE</span> <span class="string">&#x27;tb_user&#x27;</span> FIELDS TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;,&#x27;</span> LINES TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;\n&#x27;</span>;</span><br></pre></td></tr></table></figure><p>字段之间用 <code>,</code> 分割，数据用 <code>\n</code> 分割。<br>数据存放形式为如下形式（不用写字段名之类的，光数据就行）</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1</span>, a, <span class="number">19</span></span><br><span class="line"><span class="number">2</span>, b, <span class="number">20</span></span><br><span class="line"><span class="number">3</span>, c, <span class="number">18</span></span><br></pre></td></tr></table></figure><p>注意哦！！！主键顺序插入性能高于乱序插入</p><h3 id="主键优化"><a href="#主键优化" class="headerlink" title="主键优化"></a>主键优化</h3><h4 id="数据组织方式"><a href="#数据组织方式" class="headerlink" title="数据组织方式"></a>数据组织方式</h4><p>在InnoDB存储引擎中，表数据都是根据主键顺序组织存放的，这种存储方式的表称为索引组织表（index organized table &#x3D;&#x3D;IOT&#x3D;&#x3D;）。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185726599.png"><br>看，这里的数据，叶子节点 <code>id</code> 都是从小到大的喔。</p><h4 id="页分裂"><a href="#页分裂" class="headerlink" title="页分裂"></a>页分裂</h4><p>页可以为空，也可以填充一半，也可以填充100%。每个页包含了2-N行数据（如果说允许页存1条数据，那不就变成了链表了，所以MySQL规定至少存两条数据，如果一行数据很大，会行溢出，行溢出会有专门的处理方式，也就是把这个很大的字段单独放在一个位置，然后存储的时候存储一个指针指向这个位置），根据主键排列。</p><p>主键顺序插入</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185732830.png"><br>依次插入1～8，如果还有数据，那么再申请新的页进行插入</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185741724.png"><br>然后中间用指针连接。</p><p>主键乱序插入</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185745415.png"><br>在已经插入的数据1～107，插入 id&#x3D;50的，理应插在 47 后面</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185750830.png"><br>插入的时候会开辟一个新的页，然后在第一页的一半砍掉</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185755883.png"><br>后半部分移动到新的页，并且把50放进去，之后就是更新链表指针</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185800834.png"></p><p>所以主键乱序的时候就可能出现页分裂。</p><h4 id="页合并"><a href="#页合并" class="headerlink" title="页合并"></a>页合并</h4><p>当删除一行记录时，实际上记录并没有被物理删除，只是记录被标记（flaged）为删除并且它的空间变得允许被其他记录声明使用。<br>当页中删除的记录达到 <code>MERGE_THRESHOLD</code>（默认为页的50%），InnoDB会开始寻找最靠近的页（前或后）看看是否可以将两个页合并以优化空间使用。</p><p>比如下面，13～16被删除了</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185805271.png"><br>可以看到9～12为待合并的，但是前面 page1 是满的，然后看后面 page3 只用了一半，于是合并</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185810301.png"></p><p><code>MERGE_THRESHOLD</code>：合并页的阈值，可以自己设置，在创建表或者创建索引时指定。</p><h4 id="主键设计原则"><a href="#主键设计原则" class="headerlink" title="主键设计原则"></a>主键设计原则</h4><ul><li>满足业务需求的情况下，尽量降低主键的长度。（因为二级索引它的叶子节点存放的是主键，所以主键越短，每页能存放的数据就越多）</li><li>插入数据时，尽量选择顺序插入，选择使用 <code>AUTO_INCREMENT</code> 自增主键。（细想一下自增主键，一次一次增大（从 <code>1~10</code>），即使你中间删除了一个某一个 <code>id</code>（<code>5</code>），但是 <code>id</code> 并不影响自增，再插入时，还是 <code>11</code>。）</li><li>尽量不要使用 UUID 做主键或者是其他自然主键，如身份证号。（UUID是无序的）</li><li>业务操作时，避免对主键的修改</li></ul><h3 id="order-by-优化"><a href="#order-by-优化" class="headerlink" title="order by 优化"></a>order by 优化</h3><ol><li><p>Using filesort：通过表的索引或全表扫描，读取满足条件的数据行，然后在排序缓冲区sort buffer中完成排序操作，所有不是通过索引直接返回排序结果的排序都叫 FileSort排序。</p></li><li><p>Using index：通过有序索引顺序扫描直接返回有序数据，这种情况即为 using index，不需要额外排序，操作效率高。<br>下面开始演示，我们的 <code>tb_user</code> 表索引如下，只有一个</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185816354.png"><br>当我们执行命令</p></li></ol><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">EXPLAIN <span class="keyword">SELECT</span> id, age, phone <span class="keyword">FROM</span> tb_user <span class="keyword">ORDER</span> <span class="keyword">BY</span> age;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185823333.png"></p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">EXPLAIN <span class="keyword">SELECT</span> id, age, phone <span class="keyword">FROM</span> tb_user <span class="keyword">ORDER</span> <span class="keyword">BY</span> age, phone;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185829398.png"><br>结果是用不到索引排序的，因为没有符合最左前缀原则。<code>order by</code> 里面没有先出现 <code>profession</code>。</p><p>我们先创建一个索引</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 创建 age, phone 的联合索引</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_user_age_phone <span class="keyword">ON</span> tb_user(age, phone);</span><br></pre></td></tr></table></figure><p>然后查看排序</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">EXPLAIN <span class="keyword">SELECT</span> id, age, phone <span class="keyword">FROM</span> tb_user <span class="keyword">ORDER</span> <span class="keyword">BY</span> age;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185841818.png"></p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">EXPLAIN <span class="keyword">SELECT</span> id, age, phone <span class="keyword">FROM</span> tb_user <span class="keyword">ORDER</span> <span class="keyword">BY</span> age, phone;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185847613.png"><br>都使用了索引排序，效率比较高</p><p>但是如果我们想按照倒序排序呢？</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- age倒序、phone倒序</span></span><br><span class="line">EXPLAIN <span class="keyword">SELECT</span> id, age, phone <span class="keyword">FROM</span> tb_user <span class="keyword">ORDER</span> <span class="keyword">BY</span> age <span class="keyword">DESC</span>, phone <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185855073.png"><br>索引用上了，并且出现了一个 <code>Backward index scan</code>，反向扫描索引。因为索引排序是升序排的，先按照 <code>age</code> 排，<code>age</code> 相同再按照 <code>phone</code> 排。而叶子节点是双向循环链表，所以在这里就用到了吧（从后往前扫描)。</p><p>但是如果你想先按照 <code>phone</code> 再按照 <code>age</code> 排序是不行的，因为索引建立是先 <code>age</code> 再 <code>phone</code>。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">EXPLAIN <span class="keyword">SELECT</span> id, age, phone <span class="keyword">FROM</span> tb_user <span class="keyword">ORDER</span> <span class="keyword">BY</span> phone, age;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185900197.png"><br>这个时候，你看是用到了索引的 <code>idx_user_age_phone</code>，注意索引主要是和查询有关的，查询的时候我还是可以用这个索引的，因为 <code>ORDER BY</code> 没办法根据这个索引排序，所以又出现了 <code>Using filesort</code>。</p><p>另外，如果你想执行下面这个语句</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- age 升序，phone 倒序</span></span><br><span class="line">EXPLAIN <span class="keyword">SELECT</span> id, age, phone <span class="keyword">FROM</span> tb_user <span class="keyword">ORDER</span> <span class="keyword">BY</span> age <span class="keyword">ASC</span>, phone <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185905284.png"><br>也是会进行重排的，对 <code>age</code> 相同的区域再进行重排（你可能要疑问，为什么不对里面的 <code>phone</code> 倒序查？就不用排序了，原因是&#x3D;&#x3D;索引扫描必须是单向连续的&#x3D;&#x3D;，只能从左往右或者从右往左，不能先正又反，而且反复横跳，所以MySQL引擎宁愿选择排序)。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185911579.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185921283.png"><br>索引表里面 <code>Collation</code> 含义就是排序规则，<code>A</code> 就代表升序</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 创建索引的时候指定排序规则</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_user_age_phone_ad <span class="keyword">ON</span> tb_user(age <span class="keyword">ASC</span>, phone <span class="keyword">DESC</span>);</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 再次执行查看</span></span><br><span class="line">EXPLAIN <span class="keyword">SELECT</span> id, age, phone <span class="keyword">FROM</span> tb_user <span class="keyword">ORDER</span> <span class="keyword">BY</span> age <span class="keyword">ASC</span>, phone <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185927978.png"><br>也可以走索引了。</p><ul><li>如果 <code>age DESC, phone ASC</code>，那么就是反向扫描这个索引。</li></ul><p>当使用 <code>SELECT *</code> 会出现什么？</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185933728.png"><br>没有用 <code>key</code>，排序也是缓冲区排序。</p><p>这是因为查询的是 <code>*</code>，如果走索引会涉及回表查询，当 MySQL 发现回表次数太多（通常超过表中的数据的 20%～30%），随机I&#x2F;O（回表）比顺序I&#x2F;O（全表扫描）要慢，这个时候就会放弃索引，选择全表扫描+内存排序。</p><ul><li>如果不可避免的出现 filesort，大数据量排序时，可以适当增大排序缓冲区大小 <code>sort_buffer_size</code> （默认 256K）</li></ul><h3 id="group-by优化"><a href="#group-by优化" class="headerlink" title="group by优化"></a>group by优化</h3><ul><li>在分组操作时，可以通过索引来提高效率</li><li>分组操作时，索引的使用也是满足最左前缀法则的</li></ul><p>当前表，只有主键索引，没有任何索引<br>根据专业分组</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185940630.png"><br><code>type</code> 为 <code>ALL</code> 性能最低，<code>Extra</code> 显示用到临时表，性能也低。<br>对 <code>profession</code> 创建索引，记着之前说要尽可能创建联合索引</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185945741.png"><br>然后我们再进行分组</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185948584.png"><br><code>type</code> 从 ALL 变为 index，性能变高，<code>Extra</code> 也是从 Using temporary 变为 Using index。性能也变好了。</p><p>然后我们又根据 <code>age</code> 去分组</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330185953678.png"><br>这里好奇，对吧，<code>age</code> 不符合最左前缀原则，怎么还用了这个索引，<code>Extra</code> 里面还有 <code>Using index</code>，关键在于这个索引里面包含了你想要查询的信息（age），如果你用全表扫描（就是用主键），每个叶子节点存储的是所有的信息，你需要去查，这个索引虽然对统计没用，但是信息少，相对来说读取的磁盘页就少。其实用这个索引也是全部扫描，但是扫描的磁盘页少。<br>&#x3D;&#x3D;所以索引不仅能用来快速定位，还能用来覆盖索引（只拿信息  ）&#x3D;&#x3D;<br>用 Using temporary 是用来计数的，因为查询到的 <code>age</code> 是无序的，所以统计到一个值需要去计数<br>而前面 <code>profession</code> 就不用，因为它是第一个字段，先按照它来排序的，所以统计的时候不需要一个临时表了（相当于 <code>map</code> ），直接边读边算就可以了（一个变量)。</p><p>然后再看这个，查询的时候用了 profession</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190001520.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190006395.png"><br>此时只有 <code>Using index</code>，原因是，索引定位到 profession，在内部 <code>age</code> 是有序的，所以也是只要一个一个去计数就可以了，不需要临时表了。</p><h3 id="limit-优化"><a href="#limit-优化" class="headerlink" title="limit 优化"></a>limit 优化</h3><p>一个常见又非常头疼的问题就是 <code>limit 2000000,10</code>，此时需要MySQL排序前 <code>2000010</code> 记录，仅仅返回 <code>2000000-2000010</code> 的记录，其他记录丢弃，查询排序的代价非常大。</p><p>下面查询总数</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190017517.png"><br>耗时，十几秒，然后尝试分页查询</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> tb_sku limit <span class="number">0</span>, <span class="number">10</span>;</span><br><span class="line"><span class="comment">-- 10 rows in set (0.00 sec)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> tb_sku limit <span class="number">10</span>, <span class="number">10</span>;</span><br><span class="line"><span class="comment">-- 10 rows in set (0.00 sec)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> tb_sku limit <span class="number">1000000</span>, <span class="number">10</span>;</span><br><span class="line"><span class="comment">-- 10 rows in set (1.66 sec)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> tb_sku limit <span class="number">5000000</span>, <span class="number">10</span>;</span><br><span class="line"><span class="comment">-- 10 rows in set (10.79 sec)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> tb_sku limit <span class="number">9000000</span>, <span class="number">10</span>;</span><br><span class="line"><span class="comment">-- 10 rows in set (19.39 sec)</span></span><br></pre></td></tr></table></figure><p>越靠后的顺序查询时间越大。</p><p>优化思路：一般分页查询时，通过创建覆盖索引能够比较好地提高性能，可以通过覆盖索引加子查询形式进行优化。</p><p>我们尝试使用如下语句</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> tb_sku <span class="keyword">WHERE</span> id <span class="keyword">IN</span> (<span class="keyword">SELECT</span> id <span class="keyword">FROM</span> tb_sku <span class="keyword">ORDER</span> <span class="keyword">BY</span> id LIMIT <span class="number">9000000</span>, <span class="number">10</span>);</span><br></pre></td></tr></table></figure><p>会报错: ERROR 1235 (42000)：This version of MySQL doesn’t yet support ‘LIMIT &amp; IN&#x2F;ALL&#x2F;ANY&#x2F;SOME subquery’，因为 LIMIT 不能出现在 IN 子查询里面</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> s.<span class="operator">*</span> <span class="keyword">FROM</span> tb_sku s, (<span class="keyword">SELECT</span> id <span class="keyword">FROM</span> tb_sku <span class="keyword">ORDER</span> <span class="keyword">BY</span> id LIMIT <span class="number">9000000</span>, <span class="number">10</span>) a <span class="keyword">WHERE</span> s.id <span class="operator">=</span> a.id;</span><br></pre></td></tr></table></figure><p>先写一个只查ID的子查询（利用覆盖索引数数快），把翻页后的10个ID找出来，再用这10个ID回去查整行数据。<br>如果是原来那种 <code>SELECT * FROM tb_sku LIMIT 9000000, 10;</code> 会直接把每条数据都完整搬到内存，而这个只搬ID，所以更快了。</p><h3 id="count-优化"><a href="#count-优化" class="headerlink" title="count 优化"></a>count 优化</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">EXPLAIN <span class="keyword">SELECT</span> <span class="built_in">count</span>(<span class="operator">*</span>) <span class="keyword">FROM</span> tb_user;</span><br></pre></td></tr></table></figure><ul><li>﻿﻿MyISAM 引擎把一个表的总行数存在了磁盘上，因此执行 <code>count(*)</code> 的时候会直接返回这个数，效率很高；但是如果带 <code>where</code> 条件就不行了</li><li>﻿﻿InnoDB 引擎就麻烦了，它执行 <code>count(*)</code>  的时候，需要把数据一行一行地从引擎里面读出来，然后累积计数。</li></ul><p>⠀&#x3D;&#x3D;优化思路：自己计数&#x3D;&#x3D;。比如 Redis 里面自己维护一个计数的，插入数据就加一，删除数据就减一。</p><h4 id="count-的几种用法"><a href="#count-的几种用法" class="headerlink" title="count 的几种用法"></a>count 的几种用法</h4><p><code>count()</code> 是一个聚合函数，对于返回的结果集，一行行地判断，如果 <code>count</code> 函数的参数不是 <code>NULL</code>，累计值就加 <code>1</code>，否则不加，最后返回累计值。</p><ul><li><p><code>﻿﻿count(主键)</code> InnoDB 引擎会遍历整张表，把每一行的主键 <code>id</code> 值都取出来，返回给服务层。服务层拿到主键后，直接按行进行累加（主键不可能为 <code>null</code>）。</p></li><li><p><code>count(字段)</code> 没有 <code>not null</code> 约束：InnoDB 引擎会遍历整张表把每一行的字段值都取出来，返回给服务层，服务层判断是否为 <code>null</code>，不为 <code>null</code>，计数累加。 有 <code>not null</code> 约束：InnoDB 引擎会遍历整张表把每一行的字段值都取出来，返回给服务层，直接按行进行累加。</p></li><li><p><code>﻿﻿count(1)</code><br>InnoDB 引擎遍历整张表，但不取值。服务层对于返回的每一行，放一个数字 <code>1</code> 进去，直接按行进行累加。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"># 里面写数值几对计数没影响</span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">count</span>(<span class="number">1</span>) <span class="keyword">FROM</span> tb_user; # <span class="number">1</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">count</span>(<span class="number">0</span>) <span class="keyword">FROM</span> tb_user; # <span class="number">1</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">count</span>(<span class="number">-1</span>) <span class="keyword">FROM</span> tb_user; # <span class="number">1</span></span><br></pre></td></tr></table></figure></li><li><p><code>count(*)</code><br>InnoDB引擎并不会把全部字段取出来，而是专门做了优化，不取值，服务层直接按行进行累加。</p></li></ul><p>按照效率排序的话，<code>count(字段)&lt;count(id)&lt; count(1) ~ count(*)</code>，所以尽量使用 <code>count(*)</code>。</p><h3 id="update-优化"><a href="#update-优化" class="headerlink" title="update 优化"></a>update 优化</h3><p>InnoDB 的行锁是针对索引加的锁，不是针对记录加的锁，并且该索引不能失效，否则会从行锁升级为表锁。</p><p>所以更新的时候条件里尽量是带索引的列。<br>加入我们的 tb_user 里面有两条数据</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190036289.png"><br>我们从两个窗口执行命令<br>第一个窗口，先开启事务，并且执行 UPDATE 将二狗专业更新为 Java</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">START</span> TRANSACTION;</span><br><span class="line"></span><br><span class="line"><span class="keyword">UPDATE</span> tb_user <span class="keyword">SET</span> profession <span class="operator">=</span> <span class="string">&#x27;Java&#x27;</span> <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure><p>然后，第二个窗口也开启事务， 并且执行 UPDATE 将二狗专业更新为 Python</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">START</span> TRANSACTION;</span><br><span class="line"></span><br><span class="line"><span class="keyword">UPDATE</span> tb_user <span class="keyword">SET</span> profession <span class="operator">=</span> <span class="string">&#x27;Python&#x27;</span> <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190041163.png"><br>此时下方的 <code>Elapsed Time</code> 是一直增加的，因为第一个事务是把那行数据锁住了，因为没有进行 <code>COMMIT</code>，而当第一个窗口一直不 <code>COMMIT</code>，那这个窗口一直等会超时停止。<br>当第二个窗口还在等待的时候，然后我们第一个窗口执行 <code>COMMIT</code><br>那数据库信息就会更新为Java，第二个窗口也会执行完 UPDATE，等待第二个事务 <code>COMMIT</code> 完之后，信息就会更新为 Python。</p><p>上面这种情况就是行级锁，锁定了一行。</p><p>现在我们再做一组测试，此时 <code>name</code> 没有任何索引哈，我们窗口1将 <code>where</code> 条件改为 <code>name</code>。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">START</span> TRANSACTION;</span><br><span class="line"></span><br><span class="line"><span class="keyword">UPDATE</span> tb_user <span class="keyword">SET</span> profession <span class="operator">=</span> <span class="string">&#x27;Java&#x27;</span> <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;二狗&#x27;</span>;</span><br></pre></td></tr></table></figure><p>执行上面两个语句，然后窗口2执行语句（注意，修改的是 <code>id</code> 为 2 的数据了）</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">START</span> TRANSACTION;</span><br><span class="line"></span><br><span class="line"><span class="keyword">UPDATE</span> tb_user <span class="keyword">SET</span> profession <span class="operator">=</span> <span class="string">&#x27;Python&#x27;</span> <span class="keyword">WHERE</span> name<span class="operator">=</span><span class="string">&#x27;狗蛋&#x27;</span>;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190051293.png"><br>但是此时还在等待！！！！因为 <code>name</code> 作为条件的时候，并不知道哪些记录 <code>name</code> 为二狗，所以会升级为表锁，所有表都会上锁，这样第二个窗口无论更新表里面哪个记录，都需要窗口1提交事务才可以。这样效率会大大降低，所以要尽可能把表锁降级为行锁。</p><p>做法就是给这个查询条件建立索引就可以</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> INDEX idx_user_name <span class="keyword">ON</span> tb_user(name);</span><br></pre></td></tr></table></figure><p>然后再执行上面那个过程，就不会出现互锁的情况了，大家都只会占用一行数据。</p><h3 id="思考"><a href="#思考" class="headerlink" title="思考"></a>思考</h3><blockquote><p>为什么InnoDB 存储引擎选择使用B+tree索引结构？</p></blockquote><ul><li>相对于二叉树，层级更少，搜索效率高</li><li>对于B-tree，无论是叶子节点还是非叶子节点，都会保存数据，这样导致一页中存储的键值减少，指针跟着减少，要同样保存大量数据，只能增加树的高度，导致性能降低；（也就是前面我们看了，一页的大小就是1M，一个节点1页，如果说要存放指针，那么一页可以有好多指针，如果存放数据，那一页能放的指针就少了，树的高度会增加）因为你要注意，当数据有百万条时，一个节点不可能只有两三个指针的，所以说一个节点可能有非常多的指针，这个时候放数据就占据了指针的空间了。</li></ul><ul><li>﻿﻿相对Hash索引，B+tree支持范围匹配及排序操作（双向循环链表的好处）；</li></ul><blockquote><p>以下SQL语句，哪个执行效率高？</p></blockquote><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> <span class="keyword">user</span> <span class="keyword">WHERE</span> id<span class="operator">=</span><span class="number">10</span>;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> <span class="keyword">user</span> <span class="keyword">WHERE</span> name<span class="operator">=</span><span class="string">&#x27;二狗&#x27;</span>; # 二狗id是<span class="number">10</span></span><br></pre></td></tr></table></figure><p>参考索引分类，前者效率更高。</p><p>InnoDB 主键索引的B+tree高度为2、3的时候能存储多少数据？<br><img src="/MySQL/image%2023.png"><br>我们已知一个页的大小是16K，结合这个图，一个节点的大小就是一个页。InnoDB的指针占用 <code>6</code> 个字节的空间，这个是固定的哈（也就是图里面灰色的部分）。主键我们假设是用的 <code>bigint</code> 类型，那么占用字节数就是 <code>8</code>。如果一行的数据大小是 1K，那么一页就可以存 <code>16</code> 行这样的数据。</p><p>我们先算一行能放多少个指针<br><code>n * 8 + (n + 1) * 6 = 16 * 1024</code>，也就是 <code>n = 1170</code></p><ul><li><code>n * 8</code> 是放的主键值</li><li><code>(n + 1) * 6</code> 是放的往下指的指针</li><li><code>16 * 1024</code> 是字节数量，16K<br>那么当高度为 <code>2</code> 的时候，能存放 <code>1170 * 16 = 18720</code> 个数据，如果高度为 <code>3</code>，就再乘以 <code>1170</code>。大概有两千多万条记录！！！</li></ul><p>&#x3D;&#x3D;如果存储数据超长上亿，考虑分库分表&#x3D;&#x3D;。</p><h2 id="视图"><a href="#视图" class="headerlink" title="视图"></a>视图</h2><h3 id="介绍-1"><a href="#介绍-1" class="headerlink" title="介绍"></a>介绍</h3><p>视图（View）是一种虚拟存在的表。视图中的数据并不在数据库中实际存在，行和列数据来自定义视图的查询中使用的表，并且是在使用视图时动态生成的。<br>通俗的讲，视图只保存了查询的SQL逻辑，不保存查询结果。所以我们在创建视图的时候，主要的工作就落在创建这条SQL查询语句上。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 创建视图</span></span><br><span class="line"><span class="keyword">CREATE</span> [<span class="keyword">OR</span> REPLACE] <span class="keyword">VIEW</span> 视图名称[(列名列表)] <span class="keyword">AS</span> <span class="keyword">SELECT</span>语句 [<span class="keyword">WITH</span>[<span class="keyword">CASCADED</span> <span class="operator">|</span> <span class="keyword">LOCAL</span>] <span class="keyword">CHECK</span> OPTION]</span><br></pre></td></tr></table></figure><ul><li><code>OR REPLACE</code> 是可选项，意思是如果存在就替换</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查看创建视图语句</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">CREATE</span> <span class="keyword">VIEW</span> 视图名称</span><br><span class="line"><span class="comment">-- 查看视图数据</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> 视图名称</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 修改视图：方式一，借助下面这个 OR REPLACE</span></span><br><span class="line"><span class="keyword">CREATE</span> [<span class="keyword">OR</span> REPLACE] <span class="keyword">VIEW</span> 视图名称[(列名列表)] <span class="keyword">AS</span> <span class="keyword">SELECT</span>语句 [<span class="keyword">WITH</span>[<span class="keyword">CASCADED</span> <span class="operator">|</span> <span class="keyword">LOCAL</span>] <span class="keyword">CHECK</span> OPTION]</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 修改视图：方式二</span></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">VIEW</span> 视图名称[(列名列表)] <span class="keyword">AS</span> <span class="keyword">SELECT</span>语句 [<span class="keyword">WITH</span>[<span class="keyword">CASCADED</span><span class="operator">|</span><span class="keyword">LOCAL</span>] <span class="keyword">CHECK</span> OPTION]</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 删除视图</span></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">VIEW</span> [IF <span class="keyword">EXISTS</span>] 视图名称 [,视图名称]...</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190121083.png"><br>这是数据库表的数据，我们创建视图</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">VIEW</span> user_v_1 <span class="keyword">AS</span> <span class="keyword">SELECT</span> id, name, phone <span class="keyword">FROM</span> tb_user <span class="keyword">WHERE</span> id <span class="operator">&lt;=</span> <span class="number">3</span>;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190126659.png"><br>视图就创建成功了，然后查询什么的就不说了，当作正常的表，现在我们尝试插入数据</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">INSERT INTO</span> user_v_1 <span class="keyword">VALUES</span>(<span class="number">3</span>, <span class="string">&#x27;傻狗&#x27;</span>, <span class="string">&#x27;22222222222&#x27;</span>);</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190131927.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190137005.png"><br>视图和主表里面都是有数据的<br>我们再尝试执行下面这个</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">INSERT INTO</span> user_v_1 <span class="keyword">VALUES</span>(<span class="number">6</span>, <span class="string">&#x27;铁汁&#x27;</span>, <span class="string">&#x27;55555555555&#x27;</span>);</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190142079.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190148684.png"><br>视图里面没数据（因为没满足 <code>id &lt;= 3</code> 的条件)，主表里面是有数据的。</p><h3 id="检查选项"><a href="#检查选项" class="headerlink" title="检查选项"></a>检查选项</h3><p>当使用 <code>WITH CHECK OPTION</code> 子句创建视图时，MySQL会通过视图检查正在更改的每个行，例如插入，更新，删除，以使其符合视图的定义。MySQL允许基于另一个视图创建视图，它还会检查依赖视图中的规则以保持一致性。为了确定检查的范围，mysql提供了两个选项：<code>CASCADED</code> 和 <code>LOCAL</code>，默认值为 <code>CASCADED</code>。</p><p>当我们用如下命令创建视图的时候（尾部加上 <code>CASCADED CHECK OPTION</code>）</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">VIEW</span> user_v_1 <span class="keyword">AS</span> <span class="keyword">SELECT</span> id, name, phone <span class="keyword">FROM</span> tb_user <span class="keyword">WHERE</span> id <span class="operator">&lt;=</span> <span class="number">3</span> <span class="keyword">WITH</span> <span class="keyword">CASCADED</span> <span class="keyword">CHECK</span> OPTION;</span><br></pre></td></tr></table></figure><p>插入 <code>id=3</code> 的命令在两个地方都不会有问题，但是再插入 id 为 6 的时候就出现问题了</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190154757.png"><br>就没办法插入了。</p><p>所以如上我们总结，表为 user，视图 1 为在表上建立的视图，视图2为在视图1上建立的视图。<br>视图 1 设置 id &lt;&#x3D; 3 无 <code>WITH LOCAL CHECK OPTION</code> 时</p><ul><li>往视图插入 <code>id=3</code> 的数据，视图有数据，表有数据</li><li>往视图插入 <code>id=6</code> 的数据，视图有数据，表有数据<br>视图 1 设置 id &lt;&#x3D; 3 有 <code>WITH LOCAL CHECK OPTION</code> 时</li><li>往视图插入 <code>id=3</code> 的数据，视图有数据，表有数据</li><li>往视图插入 <code>id=6</code> 的数据，视图无数据，表无数据</li></ul><p>下面我们考虑更复杂的情况，三个视图</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">VIEW</span> user_v_1 <span class="keyword">AS</span> <span class="keyword">SELECT</span> id, name, phone <span class="keyword">FROM</span> tb_user <span class="keyword">WHERE</span> id <span class="operator">&lt;=</span> <span class="number">20</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">VIEW</span> user_v_2 <span class="keyword">AS</span> <span class="keyword">SELECT</span> id, name, phone <span class="keyword">FROM</span> user_v_1 <span class="keyword">WHERE</span> id <span class="operator">&gt;=</span> <span class="number">10</span> <span class="keyword">WITH</span> <span class="keyword">CASCADED</span> <span class="keyword">CHECK</span> OPTION;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">VIEW</span> user_v_3 <span class="keyword">AS</span> <span class="keyword">SELECT</span> id, name, phone <span class="keyword">FROM</span> user_v_2 <span class="keyword">WHERE</span> id <span class="operator">&lt;=</span> <span class="number">15</span>;</span><br></pre></td></tr></table></figure><p>考虑三种数据插入视图 <code>user_v_3</code></p><ul><li><code>id = 11</code>，第一层 <code>id &lt;= 15</code> 满足，第二层 <code>id &gt;= 10</code> 满足，第三层 <code>id &lt;= 20</code> 满足，所以底层的 <code>tb_user</code> 表可以插入，三个视图上都能显示</li><li><code>id = 17</code>，第一层不满足，但是没有 <code>CHECK OPTION</code>，所以继续往下判断，第二个视图满足，并且带有 <code>CHECK OPTION</code>，继续向下判断，第三个 <code>id &lt;= 20</code> 满足，所以能插入，底层的 <code>tb_user</code> 表可以插入，视图 1，2显示，3 不显示</li><li><code>id = 28</code>，第一层不满足，但是没有 <code>CHECK OPTION</code>，所以仍可以往下判断，第二个也满足，但是有 <code>CHECK OPTION</code>，继续往下，不满足，所以不能插入！！！</li></ul><p>乌鲁塞，太啰嗦了，我们简化讲解，假如说视图 v1, v2, v3，依次依赖，v1依赖user表，v2依赖v1,v3依赖v2。当某个视图创建时用的是 <code>WITH LOCAL CHECK OPTION</code>，那么在进行检查时只检查自己的是否满足，别人的我不管，如果是 <code>WITH CASCADED CHECK OPTION</code>，那么会检查自己的和自己所依赖的（递归一直到终点的那种），有任何一个不满足的都不行。</p><h3 id="更新及作用"><a href="#更新及作用" class="headerlink" title="更新及作用"></a>更新及作用</h3><h4 id="更新"><a href="#更新" class="headerlink" title="更新"></a>更新</h4><p>要使视图可更新，视图中的行与基础表中的行之间必须存在一对一的关系。如果视图包含以下任何一项，则该视图不可更新、插入</p><ol><li>聚合函数或窗口函数（<code>SUM()</code>、<code>MIN()</code>、<code>MAX()</code>、<code>COUNT()</code>等）</li><li>DISTINCT</li><li>GROUP BY</li><li>HAVING</li><li>UNION 或者 UNION ALL</li></ol><h4 id="作用"><a href="#作用" class="headerlink" title="作用"></a>作用</h4><ul><li><p>简单：视图不仅可以简化用户对数据的理解，也可以简化他们的操作。那些被经常使用的查询可以被定义为视图，从而使得用户不必为以后的操作每次指定全部的条件。</p></li><li><p>安全：数据库可以授权，但不能授权到数据库特定行和特定的列上。通过视图用户只能查询和修改他们所能见到的数据</p></li><li><p>数据独立：视图可帮助用户屏蔽真实表结构变化带来的影响。<br>比如真实表等表名发生变化，视图只需要更新一下对应列的别名就可以，从而也是一点点保护吧，比如如下</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 主表的 name 字段名改为了 studentName</span></span><br><span class="line"><span class="comment">-- 视图也要相应变化</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">VIEW</span> user_v_1 <span class="keyword">AS</span> <span class="keyword">SELECT</span> id, studentName <span class="keyword">AS</span> name <span class="keyword">FROM</span> tb_user;</span><br></pre></td></tr></table></figure></li></ul><h3 id="案例"><a href="#案例" class="headerlink" title="案例"></a>案例</h3><ol><li><p>为了保证数据库表的安全性，开发人员在操作 <code>tb_user</code> 表时，只能看到的用户的基本字段，屏蔽手机号和邮箱两个字段。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">VIEW</span> tb_user_view <span class="keyword">AS</span> <span class="keyword">SELECT</span> id, name, profession, age, gender, status, createtime <span class="keyword">FROM</span> tb_user;</span><br></pre></td></tr></table></figure></li><li><p>查询每个学生所选修的课程（三张表联查），这个功能在很多的业务中都有使用到，为了简化操作，定义一个视图。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">VIEW</span> tb_stu_coures_view <span class="keyword">AS</span> <span class="keyword">SELECT</span> s.name <span class="keyword">AS</span> student_name, s.no <span class="keyword">AS</span> student_no, c.name <span class="keyword">AS</span> course_name <span class="keyword">FROM</span> student s, student_coures sc, coures c <span class="keyword">WHERE</span> s.id <span class="operator">=</span> sc.studentid <span class="keyword">AND</span> sc.courseid <span class="operator">=</span> c.id;</span><br></pre></td></tr></table></figure></li></ol><h2 id="存储过程"><a href="#存储过程" class="headerlink" title="存储过程"></a>存储过程</h2><p>存储过程是事先经过编译并存储在数据库中的一段SQL语句的集合，调用存储过程可以简化应用开发人员的很多工作，减少数据在数据库和应用服务器之间的传输，对于提高数据处理的效率是有好处的。<br>存储过程思想上很简单，就是数据库 SQL语言层面的代码封装与重用。</p><p>比如一个过程可能是查询+更新表1+更新表2，如果从客户端发出执行三个SQL语句的命令，就不好，因为中间还要分三次传输之类的，所以更好的是在数据库建立一个三个SQL语句的函数，然后调用就可以了</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 创建</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> 存储过程名称([参数列表])</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="comment">--SQL语句</span></span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 调用</span></span><br><span class="line"><span class="keyword">CALL</span> 名称([参数]);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看存储过程</span></span><br><span class="line"># 从系统表里面查看， xxx 是数据库名(非常详细)</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> INFORMATION_SCHEMA.ROUTINES <span class="keyword">WHERE</span> ROUTINE_SCHEMA <span class="operator">=</span> <span class="string">&#x27;xxx&#x27;</span>;</span><br><span class="line"></span><br><span class="line"># 查看创建的<span class="keyword">SQL</span>语句</span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> 存储过程名称;</span><br></pre></td></tr></table></figure><p>举例</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> p1()</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">FROM</span> tb_user;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CALL</span> p1();</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看存储过程</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> INFORMATION_SCHEMA.ROUTINES <span class="keyword">WHERE</span> ROUTINE_SCHEMA <span class="operator">=</span> <span class="string">&#x27;demo&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> p1;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 删除</span></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">PROCEDURE</span> [IF <span class="keyword">EXISTS</span>] p1;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190205672.png"><br>可以看到创建这个存储过程时的语句。<br>如果有可视化的工具，双击就能直接看到</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190210807.png"></p><p>当在命令行里面输入创建过程的SQL语句的时候，如下</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190215410.png"><br>会报错，原因是因为终端把 <code>;</code> 当作了结束符，所以提前终止了。<br>我们需要通过 <code>delimiter</code> 设置SQL语句新的结束符<br>这个时候命令行输入如下</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 设置新的结束符</span></span><br><span class="line">delimiter $$</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 创建存储过程，用 $$ 结束</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> p1()</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">FROM</span> tb_user;</span><br><span class="line"><span class="keyword">END</span>$$</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 再执行过程</span></span><br><span class="line"><span class="keyword">CALL</span> p1()$$  <span class="comment">--&gt; 注意，如果你想用分号结束，就得 delimiter ; 设置回去</span></span><br></pre></td></tr></table></figure><h3 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h3><h4 id="系统变量"><a href="#系统变量" class="headerlink" title="系统变量"></a>系统变量</h4><p>系统变量是 MySQL 服务器提供，不是用户定义的，属于服务器层面。分为全局变量（<code>GLOBAL</code>）、会话变量（<code>SESSION</code>）。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190223016.png"></p><blockquote><p>一次 <code>New Query</code> 里面设置的变量也是会话变量</p></blockquote><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查看系统变量</span></span><br><span class="line"><span class="keyword">SHOW</span> [SESSION<span class="operator">|</span><span class="keyword">GLOBAL</span>] VARIABLES; <span class="comment">-- 查看所有系统变量</span></span><br><span class="line"><span class="keyword">SHOW</span> [SESSION<span class="operator">|</span><span class="keyword">GLOBAL</span>] VARIABLES <span class="keyword">LIKE</span> <span class="string">&#x27;...&#x27;</span>; <span class="comment">-- 可以通过 LIKE 模糊匹配方式查找变量</span></span><br><span class="line"><span class="keyword">SELECT</span> @@[SESSION<span class="operator">|</span><span class="keyword">GLOBAL</span>].系统变量名 <span class="comment">-- 查看指定变量的值</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 设置系统变量</span></span><br><span class="line"><span class="keyword">SET</span> [SESSION<span class="operator">|</span><span class="keyword">GLOBAL</span>] 系统变量名<span class="operator">=</span>值;</span><br><span class="line"><span class="keyword">SET</span> @@[SESSION<span class="operator">|</span><span class="keyword">GLOBAL</span>].系统变量名<span class="operator">=</span>值;</span><br></pre></td></tr></table></figure><p>默认查看的都是 SESSION 级别的</p><blockquote><p>即使你设置了全局变量的值，但是如果服务器重启，又会恢复到初始值，所以如果想不失效，需要在 <code>/etc/my.cnf</code> 中配置。</p></blockquote><h4 id="用户自定义变量"><a href="#用户自定义变量" class="headerlink" title="用户自定义变量"></a>用户自定义变量</h4><p>用户定义变量是用户根据需要自己定义的变量，用户变量不用提前声明，在用的时候直接用“@变量名”使用就可以。其作用域为当前连接（当前会话）。</p><blockquote><p>系统变量是两个 <code>@</code>，用户自定义变量是一个 <code>@</code></p></blockquote><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 赋值</span></span><br><span class="line"><span class="keyword">SET</span> <span class="variable">@var_name</span><span class="operator">=</span>expr [,<span class="variable">@var_name</span><span class="operator">=</span>expr]...;</span><br><span class="line"><span class="keyword">SET</span> <span class="variable">@var_name</span>:<span class="operator">=</span>expr [,<span class="variable">@var_name</span>:<span class="operator">=</span>expr]...; # 推荐使用 :<span class="operator">=</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> <span class="variable">@var_name</span>:<span class="operator">=</span>expr [,<span class="variable">@var_name</span>:<span class="operator">=</span>expr]...;</span><br><span class="line"><span class="keyword">SELECT</span> 字段名 <span class="keyword">INTO</span><span class="variable">@var_name</span> <span class="keyword">FROM</span> 表名;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 使用</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="variable">@var_name</span>;</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SET</span> <span class="variable">@myname</span>:<span class="operator">=</span><span class="string">&#x27;二狗&#x27;</span>;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="variable">@myname</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 通过查询结果进行赋值</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">INTO</span> <span class="variable">@mycount</span> <span class="keyword">FROM</span> tb_user;</span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> <span class="variable">@abc</span>; # <span class="keyword">NULL</span> 因为未进行赋值，但是不会报错</span><br></pre></td></tr></table></figure><ul><li>用户定义的变量无需对其进行声明或初始化，只不过获取到的值为 <code>NULL</code>。</li></ul><h4 id="局部变量"><a href="#局部变量" class="headerlink" title="局部变量"></a>局部变量</h4><p>局部变量是根据需要定义的在局部生效的变量，访问之前，需要 <code>DECLARE</code> 声明。可用作存储过程内的局部变量和输入参数，局部变量的范围是在其内声明的 <code>BEGIN...END</code> 块。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 声明</span></span><br><span class="line"><span class="keyword">DECLARE</span> 变量名 变量类型[<span class="keyword">DEFAULT</span> ...];</span><br></pre></td></tr></table></figure><p>变量类型就是数据库字段类型：INT、BIGINT、CHAR、VARCHAR、DATE、TIME等。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 赋值</span></span><br><span class="line"><span class="keyword">SET</span> 变量名<span class="operator">=</span>值;</span><br><span class="line"><span class="keyword">SET</span> 变量名:<span class="operator">=</span>值;</span><br><span class="line"><span class="keyword">SELECT</span> 字段名 <span class="keyword">INTO</span> 变量名 <span class="keyword">FROM</span> 表名...;</span><br></pre></td></tr></table></figure><p>举例</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> p2()</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">DECLARE</span> user_count <span class="type">INT</span> <span class="keyword">DEFAULT</span> <span class="number">0</span>;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">INTO</span> user_count <span class="keyword">FROM</span> tb_user;</span><br><span class="line"><span class="keyword">SELECT</span> user_count;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CALL</span> p2();</span><br></pre></td></tr></table></figure><h3 id="if-判断"><a href="#if-判断" class="headerlink" title="if 判断"></a>if 判断</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">IF 条件<span class="number">1</span> <span class="keyword">THEN</span></span><br><span class="line">...</span><br><span class="line">ELSEIF 条件<span class="number">2</span> <span class="keyword">THEN</span></span><br><span class="line">...</span><br><span class="line"><span class="keyword">ELSE</span></span><br><span class="line">...</span><br><span class="line"><span class="keyword">END</span> IF;</span><br></pre></td></tr></table></figure><p>举例</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 判断分数的等级</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> p3()</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">DECLARE</span> score <span class="type">INT</span> <span class="keyword">DEFAULT</span> <span class="number">58</span>;</span><br><span class="line"><span class="keyword">DECLARE</span> <span class="keyword">result</span> <span class="type">VARCHAR</span>(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">IF score <span class="operator">&gt;=</span> <span class="number">85</span> <span class="keyword">THEN</span></span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">result</span> :<span class="operator">=</span> <span class="string">&#x27;优秀&#x27;</span>;</span><br><span class="line">ELSEIF score <span class="operator">&gt;=</span> <span class="number">60</span> <span class="keyword">THEN</span></span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">result</span> :<span class="operator">=</span> <span class="string">&#x27;及格&#x27;</span>;</span><br><span class="line"><span class="keyword">ELSE</span></span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">result</span> :<span class="operator">=</span> <span class="string">&#x27;不及格&#x27;</span>;</span><br><span class="line"><span class="keyword">END</span> IF;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="keyword">result</span>;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CALL</span> p3();</span><br></pre></td></tr></table></figure><h3 id="参数"><a href="#参数" class="headerlink" title="参数"></a>参数</h3><table><thead><tr><th>类型</th><th>含义</th><th>备注</th></tr></thead><tbody><tr><td>IN</td><td>该类参数作为输入，也就是需要调用时传入值</td><td>默认</td></tr><tr><td>OUT</td><td>该类参数作为输出，也就是该参数可以作为返回值</td><td></td></tr><tr><td>INOUT</td><td>既可以作为输入参数，也可以作为输出参数</td><td></td></tr></tbody></table><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 用法</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> 存储过程名称([<span class="keyword">IN</span><span class="operator">/</span><span class="keyword">OUT</span><span class="operator">/</span><span class="keyword">INOUT</span> 参数名 参数类型])</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="comment">-- SQL</span></span><br><span class="line"><span class="keyword">END</span>;</span><br></pre></td></tr></table></figure><p>举例</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> p4(<span class="keyword">IN</span> score <span class="type">INT</span>, <span class="keyword">OUT</span> <span class="keyword">result</span> <span class="type">VARCHAR</span>(<span class="number">10</span>))</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line">IF score <span class="operator">&gt;=</span> <span class="number">85</span> <span class="keyword">THEN</span></span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">result</span> :<span class="operator">=</span> <span class="string">&#x27;优秀&#x27;</span>;</span><br><span class="line">ELSEIF score <span class="operator">&gt;=</span> <span class="number">60</span> <span class="keyword">THEN</span></span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">result</span> :<span class="operator">=</span> <span class="string">&#x27;及格&#x27;</span>;</span><br><span class="line"><span class="keyword">ELSE</span></span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">result</span> :<span class="operator">=</span> <span class="string">&#x27;不及格&#x27;</span>;</span><br><span class="line"><span class="keyword">END</span> IF;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CALL</span> p4(<span class="number">58</span>, <span class="variable">@result</span>); # 把结果赋值给用户变量</span><br><span class="line"><span class="keyword">SELECT</span> <span class="variable">@result</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 将输入数据除以2返回</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> p5(<span class="keyword">INOUT</span> score <span class="type">INT</span>)</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">SET</span> score :<span class="operator">=</span> score <span class="operator">/</span> <span class="number">2</span>;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">SET</span> <span class="variable">@score</span> :<span class="operator">=</span> <span class="number">78</span>;</span><br><span class="line"><span class="keyword">CALL</span> p5(<span class="variable">@score</span>);</span><br><span class="line"><span class="keyword">SELECT</span> <span class="variable">@score</span>; # <span class="number">39</span></span><br></pre></td></tr></table></figure><h3 id="case"><a href="#case" class="headerlink" title="case"></a>case</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 语法1</span></span><br><span class="line"><span class="keyword">CASE</span> case_value</span><br><span class="line"><span class="keyword">WHEN</span> when_value1 <span class="keyword">THEN</span> statement_list1</span><br><span class="line">[<span class="keyword">WHEN</span> when_value2 <span class="keyword">THEN</span> statement_list1]...</span><br><span class="line">[<span class="keyword">ELSE</span> statement_list]</span><br><span class="line"><span class="keyword">END</span> <span class="keyword">CASE</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 语法2</span></span><br><span class="line"><span class="keyword">CASE</span></span><br><span class="line"><span class="keyword">WHEN</span> search_condition1 <span class="keyword">THEN</span> statement_list1</span><br><span class="line">[<span class="keyword">WHEN</span> search_condition2 <span class="keyword">THEN</span> statement_list1]...</span><br><span class="line">[<span class="keyword">ELSE</span> statement_list]</span><br><span class="line"><span class="keyword">END</span> <span class="keyword">CASE</span>;</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> p6(<span class="keyword">IN</span> <span class="keyword">month</span> <span class="type">INT</span>)</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">DECLARE</span> <span class="keyword">result</span> <span class="type">VARCHAR</span>(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">CASE</span> </span><br><span class="line"><span class="keyword">WHEN</span> <span class="keyword">month</span> <span class="operator">&gt;=</span><span class="number">1</span> <span class="keyword">AND</span> <span class="keyword">month</span> <span class="operator">&lt;=</span> <span class="number">3</span> <span class="keyword">THEN</span></span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">result</span> :<span class="operator">=</span> <span class="string">&#x27;第一季度&#x27;</span>;</span><br><span class="line"><span class="keyword">WHEN</span> <span class="keyword">month</span> <span class="operator">&gt;=</span><span class="number">4</span> <span class="keyword">AND</span> <span class="keyword">month</span> <span class="operator">&lt;=</span> <span class="number">6</span> <span class="keyword">THEN</span></span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">result</span> :<span class="operator">=</span> <span class="string">&#x27;第二季度&#x27;</span>;</span><br><span class="line"><span class="keyword">WHEN</span> <span class="keyword">month</span> <span class="operator">&gt;=</span><span class="number">7</span> <span class="keyword">AND</span> <span class="keyword">month</span> <span class="operator">&lt;=</span> <span class="number">9</span> <span class="keyword">THEN</span></span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">result</span> :<span class="operator">=</span> <span class="string">&#x27;第三季度&#x27;</span>;</span><br><span class="line"><span class="keyword">WHEN</span> <span class="keyword">month</span> <span class="operator">&gt;=</span><span class="number">10</span> <span class="keyword">AND</span> <span class="keyword">month</span> <span class="operator">&lt;=</span> <span class="number">12</span> <span class="keyword">THEN</span></span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">result</span> :<span class="operator">=</span> <span class="string">&#x27;第四季度&#x27;</span>;</span><br><span class="line"><span class="keyword">ELSE</span></span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">result</span> :<span class="operator">=</span> <span class="string">&#x27;非法参数&#x27;</span>;</span><br><span class="line"><span class="keyword">END</span> <span class="keyword">CASE</span>;</span><br><span class="line"><span class="keyword">SELECT</span> CONCAT(<span class="string">&#x27;您输入的月份为: &#x27;</span>, <span class="keyword">month</span>, <span class="string">&#x27;, 所属的季度为: &#x27;</span>,<span class="keyword">result</span>);</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CALL</span> p6(<span class="number">5</span>); # 您输入的月份为: <span class="number">5</span>, 所属的季度为: 第二季度</span><br></pre></td></tr></table></figure><h3 id="循环"><a href="#循环" class="headerlink" title="循环"></a>循环</h3><h4 id="while"><a href="#while" class="headerlink" title="while"></a>while</h4><p>while 循环是有条件的循环控制语句。满足条件后，再执行循环体中的SQL语句。具体语法为：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"># 先判断条件，再DO</span><br><span class="line">WHILE 条件 DO</span><br><span class="line"><span class="keyword">SQL</span>逻辑...</span><br><span class="line"><span class="keyword">END</span> WHILE;</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 计算 1,,,n 的加和</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> p7(<span class="keyword">IN</span> n <span class="type">INT</span>)</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">DECLARE</span> total <span class="type">INT</span> <span class="keyword">DEFAULT</span> <span class="number">0</span>;</span><br><span class="line">WHILE n <span class="operator">&gt;</span> <span class="number">0</span> DO</span><br><span class="line"><span class="keyword">SET</span> total :<span class="operator">=</span> total <span class="operator">+</span> n;</span><br><span class="line"><span class="keyword">SET</span> n :<span class="operator">=</span> n <span class="operator">-</span> <span class="number">1</span>;</span><br><span class="line"><span class="keyword">END</span> WHILE;</span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> total;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CALL</span> p7(<span class="number">5</span>); # <span class="number">15</span></span><br></pre></td></tr></table></figure><h4 id="repeat"><a href="#repeat" class="headerlink" title="repeat"></a>repeat</h4><p><code>repeat</code> 是有条件的循环控制语句，当满足条件的时候退出循环。具体语法为</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"># 先执行一次逻辑，然后判定逻辑是否满足，如果满足，则退出。不满足再循环</span><br><span class="line">REPEAT</span><br><span class="line"><span class="keyword">SQL</span>逻辑</span><br><span class="line">UNTIL 条件</span><br><span class="line"><span class="keyword">END</span> REPEAT;</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"># 计算 <span class="number">1</span>,,,n 的加和</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> p8(<span class="keyword">IN</span> n <span class="type">INT</span>)</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">DECLARE</span> total <span class="type">INT</span> <span class="keyword">DEFAULT</span> <span class="number">0</span>;</span><br><span class="line">REPEAT</span><br><span class="line"><span class="keyword">SET</span> total :<span class="operator">=</span> total <span class="operator">+</span> n;</span><br><span class="line"><span class="keyword">SET</span> n :<span class="operator">=</span> n <span class="operator">-</span> <span class="number">1</span>;</span><br><span class="line">UNTIL n <span class="operator">&lt;=</span> <span class="number">0</span></span><br><span class="line"><span class="keyword">END</span> REPEAT;</span><br><span class="line"><span class="keyword">END</span>;</span><br></pre></td></tr></table></figure><h4 id="loop"><a href="#loop" class="headerlink" title="loop"></a>loop</h4><p>LOOP 实现简单的循环，如果不在SQL逻辑中增加退出循环的条件，可以用其来实现简单的死循环。L00P可以配合一下两个语句使用：</p><ul><li>LEAVE：配合循环使用，退出循环。</li><li>ITERATE：必须用在循环中，作用是跳过当前循环剩下的语句，直接进入下一次循环。</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">[begin_label:] LOOP</span><br><span class="line"><span class="keyword">SQL</span>逻辑...</span><br><span class="line"><span class="keyword">END</span> LOOP [end_label];</span><br><span class="line"></span><br><span class="line">LEAVE label; <span class="comment">-- 退出指定标记的循环体</span></span><br><span class="line">ITERATE label; <span class="comment">-- 直接进入下一次循环</span></span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 计算 1,,,n 的和</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> p9(<span class="keyword">IN</span> n <span class="type">INT</span>)</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">DECLARE</span> total <span class="type">INT</span> <span class="keyword">DEFAULT</span> <span class="number">0</span>;</span><br><span class="line">sum_n: LOOP</span><br><span class="line"><span class="keyword">SET</span> total :<span class="operator">=</span> total <span class="operator">+</span> n;</span><br><span class="line"><span class="keyword">SET</span> n :<span class="operator">=</span> n <span class="operator">-</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">  IF n <span class="operator">&lt;=</span> <span class="number">0</span> <span class="keyword">THEN</span></span><br><span class="line">  LEAVE sum_n; </span><br><span class="line">  <span class="keyword">END</span> IF; </span><br><span class="line">  <span class="keyword">END</span> LOOP sum_n;</span><br><span class="line"><span class="keyword">SELECT</span> total;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CALL</span> p9(<span class="number">5</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 计算 1~n 的偶数和</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> p10(<span class="keyword">IN</span> n <span class="type">INT</span>)</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">DECLARE</span> total <span class="type">INT</span> <span class="keyword">DEFAULT</span> <span class="number">0</span>;</span><br><span class="line">sum_n: LOOP</span><br><span class="line"></span><br><span class="line">IF n <span class="operator">&lt;=</span> <span class="number">0</span> <span class="keyword">THEN</span></span><br><span class="line">  LEAVE sum_n; </span><br><span class="line">  <span class="keyword">END</span> IF; </span><br><span class="line">IF n <span class="operator">%</span> <span class="number">2</span> <span class="operator">=</span> <span class="number">1</span> <span class="keyword">THEN</span></span><br><span class="line"><span class="keyword">SET</span> n :<span class="operator">=</span> n <span class="operator">-</span> <span class="number">1</span>; # 需要单独减一次 <span class="number">1</span></span><br><span class="line">ITERATE sum_n;</span><br><span class="line"><span class="keyword">END</span> IF;</span><br><span class="line"><span class="keyword">SET</span> total :<span class="operator">=</span> total <span class="operator">+</span> n;</span><br><span class="line"><span class="keyword">SET</span> n :<span class="operator">=</span> n <span class="operator">-</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">END</span> LOOP sum_n;</span><br><span class="line"><span class="keyword">SELECT</span> total;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CALL</span> p10(<span class="number">5</span>); # <span class="number">6</span></span><br></pre></td></tr></table></figure><h3 id="游标"><a href="#游标" class="headerlink" title="游标"></a>游标</h3><p>游标（CURSOR）是用来存储查询结果集的&#x3D;&#x3D;数据类型&#x3D;&#x3D;（和 INT、BIGINT一种的），在存储过程和函数中可以使用游标对结果集进行循环的处理。游标的使用包括游标的声明、OPEN、FETCH 和CLOSE，其语法分别如下。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 声明游标</span></span><br><span class="line"><span class="keyword">DECLARE</span> 游标名称 <span class="keyword">CURSOR</span> <span class="keyword">FOR</span> 查询语句;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 打开游标</span></span><br><span class="line"><span class="keyword">OPEN</span> 游标名称;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 获取游标记录</span></span><br><span class="line"><span class="keyword">FETCH</span> 游标名称 <span class="keyword">INTO</span> 变量[,变量];</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 关闭游标</span></span><br><span class="line"><span class="keyword">CLOSE</span> 游标名称;</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 根据传入的参数 uage，来查询用户表 t_user 中，所有的用户年龄小于等于 uage 的用户姓名（name） 和专业（profession），并将用户的姓名和专业插入到所创建的一张新表（id, name, profession）中。</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> p11(<span class="keyword">IN</span> uage <span class="type">INT</span>)</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">DECLARE</span> uname <span class="type">VARCHAR</span>(<span class="number">100</span>);</span><br><span class="line"><span class="keyword">DECLARE</span> upro <span class="type">VARCHAR</span>(<span class="number">100</span>); # 定义变量</span><br><span class="line"><span class="keyword">DECLARE</span> u_cursor <span class="keyword">CURSOR</span> <span class="keyword">FOR</span> <span class="keyword">SELECT</span> name, profession <span class="keyword">FROM</span> tb_user <span class="keyword">WHERE</span> age <span class="operator">&lt;=</span> uage; # 创建游标</span><br><span class="line"><span class="keyword">DECLARE</span> EXIT HANDLER <span class="keyword">FOR</span> <span class="keyword">SQLSTATE</span> <span class="string">&#x27;02000&#x27;</span> <span class="keyword">CLOSE</span> u_cursor; # 定义报错时该怎么处理</span><br><span class="line"></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> IF <span class="keyword">EXISTS</span> tb_user_pro;</span><br><span class="line"><span class="keyword">CREATE TABLE</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> tb_user_pro(</span><br><span class="line">id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span> auto_increment,</span><br><span class="line">name <span class="type">VARCHAR</span>(<span class="number">100</span>),</span><br><span class="line">profession <span class="type">VARCHAR</span>(<span class="number">100</span>)</span><br><span class="line">); # 创建表</span><br><span class="line"></span><br><span class="line"><span class="keyword">OPEN</span> u_cursor; # 打开游标</span><br><span class="line">WHILE <span class="literal">TRUE</span> DO</span><br><span class="line"><span class="keyword">FETCH</span> u_cursor <span class="keyword">INTO</span> uname, upro; # 获取游标数据</span><br><span class="line"><span class="keyword">INSERT INTO</span> tb_user_pro <span class="keyword">VALUES</span>(<span class="keyword">null</span>, uname, upro); # 插入数据</span><br><span class="line"><span class="keyword">END</span> WHILE;</span><br><span class="line"><span class="keyword">CLOSE</span> u_cursor; # 关闭游标</span><br><span class="line"></span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CALL</span> p11(<span class="number">25</span>);</span><br></pre></td></tr></table></figure><p>如果我们不写 <code>DECLARE EXIT HANDLER FOR SQLSTATE &#39;02000&#39; CLOSE u_cursor;</code> 会发生什么问题？</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190250762.png"><br>会报错，因为在循环里面判断条件时 <code>WHILE TRUE</code>，所以 <code>FETCH</code> 会一直读取数据，直到读取不到，然后报错，这个时候程序就中断了，所以会在 <code>INSERT</code> 完最后一条记录，这个函数终止。为了捕获这个报错，所以有了下一节的条件处理程序，这里就是状态码 <code>02000</code>。</p><p>另外，游标定义要在变量定义后面（为什么不知道）</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190303146.png"></p><p>当前的</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">DECLARE</span> EXIT HANDLER <span class="keyword">FOR</span> <span class="keyword">SQLSTATE</span> <span class="string">&#x27;02000&#x27;</span> <span class="keyword">CLOSE</span> u_cursor;</span><br><span class="line">也可以写为</span><br><span class="line"><span class="keyword">DECLARE</span> EXIT HANDLER <span class="keyword">FOR</span> <span class="keyword">NOT</span> FOUND <span class="keyword">CLOSE</span> u_cursor;</span><br></pre></td></tr></table></figure><h3 id="条件处理程序"><a href="#条件处理程序" class="headerlink" title="条件处理程序"></a>条件处理程序</h3><p>条件处理程序（Handler）可以用来定义在流程控制结构执行过程中遇到问题时相应的处理步骤。具体语法为：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">DECLARE</span> handler_action HANDLER <span class="keyword">FOR</span> condition_value [,condition_value]...statement;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190308518.png"></p><h2 id="存储函数"><a href="#存储函数" class="headerlink" title="存储函数"></a>存储函数</h2><p>存储函数是有返回值的存储过程，存储函数的参数只能是IN类型的。具体语法如下：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">FUNCTION</span> 存储函数名称([参数列表]) # 只能是 <span class="keyword">IN</span></span><br><span class="line"><span class="keyword">RETURNS</span> type [characteristic...]</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="comment">-- SQL</span></span><br><span class="line"><span class="keyword">RETURN</span> ...;</span><br><span class="line"><span class="keyword">END</span>;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190314702.png"><br>还必须写一个，不写还会报错，所以做一个说明</p><p>当你创建一个存储函数时，数据库需要知道这个函数的行为性质，以便进行性能优化或安全检查。以下是这三个声明的具体含义：</p><ul><li><strong>DETERMINISTIC（确定的）</strong>：指相同的输入参数总是产生相同的结果。如果函数在任何时候用同样的参数调用，返回的值都一样（且不依赖外部数据），就声明为这个。</li><li><strong>NO SQL（不包含 SQL）</strong>：声明该函数体内部不包含任何 SQL 语句（比如只进行简单的数学运算或字符串处理）。</li><li><strong>READS SQL DATA（读取 SQL 数据）</strong>：声明该函数包含读取数据的语句（如 SELECT），但不会修改数据（即不包含 INSERT、UPDATE 或 DELETE）。<br>在 MySQL 中，如果你开启了二进制日志，为了保证主从复制的一致性，系统通常要求你必须明确声明函数的性质。如果你的函数既不改变数据又是确定的，声明为 DETERMINISTIC 可以让数据库更放心地在不同环境下执行它。如果你不写这些，有时候在创建函数时会报错：*”This function has none of DETERMINISTIC, NO SQL, or READS SQL DATA in its declaration…”*。</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">FUNCTION</span> fun1(n <span class="type">INT</span>) # 可以不用指定 <span class="keyword">IN</span><span class="operator">/</span><span class="keyword">OUT</span><span class="operator">/</span><span class="keyword">INOUT</span> 了，因为只能是 <span class="keyword">OUT</span></span><br><span class="line"><span class="keyword">RETURNS</span> <span class="type">INT</span> <span class="keyword">DETERMINISTIC</span></span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">DECLARE</span> total <span class="type">INT</span> <span class="keyword">DEFAULT</span> <span class="number">0</span>;</span><br><span class="line">WHILE n <span class="operator">&gt;</span> <span class="number">0</span> DO</span><br><span class="line"><span class="keyword">SET</span> total :<span class="operator">=</span> total <span class="operator">+</span> n;</span><br><span class="line"><span class="keyword">SET</span> n :<span class="operator">=</span> n <span class="operator">-</span> <span class="number">1</span>;</span><br><span class="line"><span class="keyword">END</span> WHILE;</span><br><span class="line"></span><br><span class="line"><span class="keyword">RETURN</span> total;</span><br><span class="line"><span class="keyword">END</span>; </span><br></pre></td></tr></table></figure><ul><li>存储函数能做的存储过程都能做，而且存储过程还不会要求必须有返回值</li></ul><h2 id="触发器"><a href="#触发器" class="headerlink" title="触发器"></a>触发器</h2><p>触发器是与表有关的数据库对象，指在 <code>insert/update/delete</code> 之前或之后，触发并执行触发器中定义的 SQL 语句集合。触发器的这种特性可以协助应用在数据库端确保数据的完整性，日志记录，数据校验等操作。使用别名 <code>OLD</code> 和 <code>NEW</code> 来引用触发器中发生变化的记录内容，这与其他的数据库是相似的。&#x3D;&#x3D;现在触发器还只支持行级触发，不支持语句级触发。&#x3D;&#x3D;</p><blockquote><p>行级触发器就是比如语句 <code>UPDATE</code> </p></blockquote><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190346677.png"></p><h3 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 创建触发器</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TRIGGER</span> trigger_name</span><br><span class="line">BEFORE<span class="operator">/</span>AFTER <span class="keyword">INSERT</span><span class="operator">/</span><span class="keyword">UPDATE</span><span class="operator">/</span><span class="keyword">DELETE</span></span><br><span class="line"><span class="keyword">ON</span> tbl_name <span class="keyword">FOR</span> <span class="keyword">EACH</span> <span class="type">ROW</span> <span class="comment">-- 行级触发器</span></span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line">trigger_stmt</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看</span></span><br><span class="line"><span class="keyword">SHOW</span> TRIGGERS;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 删除</span></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TRIGGER</span> [schema_name.]trigger_name; <span class="comment">-- 如果没有指定schema_name,默认为当前数据库。</span></span><br></pre></td></tr></table></figure><h3 id="练习-1"><a href="#练习-1" class="headerlink" title="练习"></a>练习</h3><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190404139.png"></p><ul><li>操作的ID是指 <code>tb_user</code> 表里的用户 ID</li><li>操作参数是指对哪些参数进行了修改</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">create table</span> user_logs(</span><br><span class="line">id <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">not null</span> auto_increment,</span><br><span class="line">operation <span class="type">varchar</span>(<span class="number">20</span>) <span class="keyword">not null</span> comment <span class="string">&#x27;insert/update/delete&#x27;</span>,</span><br><span class="line">operate_time datetime <span class="keyword">not null</span>,</span><br><span class="line">  operate_id <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">not null</span>,</span><br><span class="line">operate_params <span class="type">varchar</span>(<span class="number">500</span>),</span><br><span class="line"><span class="keyword">primary key</span>(`id`)</span><br><span class="line">)engine<span class="operator">=</span>innodb <span class="keyword">default</span> charset<span class="operator">=</span>utf8;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 创建触发器</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TRIGGER</span> tb_user_insert_trigger</span><br><span class="line">AFTER <span class="keyword">INSERT</span> <span class="keyword">ON</span> tb_user <span class="keyword">FOR</span> <span class="keyword">EACH</span> <span class="type">ROW</span></span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> user_logs(id, operation, operation_time, operate_id, operate_params) <span class="keyword">VALUES</span></span><br><span class="line">(<span class="keyword">null</span>, <span class="string">&#x27;insert&#x27;</span>, NOW(), new.id, CONCAT(<span class="string">&#x27;插入对数据内容为: id=&#x27;</span>, new.id, <span class="string">&#x27;, name=&#x27;</span>, new.name));</span><br><span class="line"><span class="keyword">END</span>;</span><br></pre></td></tr></table></figure><p>查看触发器</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190412149.png"></p><p>更新操作</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TRIGGER</span> tb_user_update_trigger</span><br><span class="line">AFTER <span class="keyword">UPDATE</span> <span class="keyword">ON</span> tb_user <span class="keyword">FOR</span> <span class="keyword">EACH</span> <span class="type">ROW</span></span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> user_logs(id, operation, operate_time, operate_id, operate_params) <span class="keyword">VALUES</span></span><br><span class="line">(<span class="keyword">null</span>, <span class="string">&#x27;update&#x27;</span>, NOW(), new.id, CONCAT(<span class="string">&#x27;更新之后的数据: id=&#x27;</span>, new.id, <span class="string">&#x27;, name=&#x27;</span>, new.name, <span class="string">&#x27;| 更新之前的数据: id=&#x27;</span>, old.id, <span class="string">&#x27;,name=&#x27;</span>, old.name));</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">UPDATE</span> tb_user <span class="keyword">SET</span> name<span class="operator">=</span><span class="string">&#x27;狗子&#x27;</span> <span class="keyword">WHERE</span> name<span class="operator">=</span><span class="string">&#x27;傻狗&#x27;</span>;</span><br></pre></td></tr></table></figure><p>删除操作</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TRIGGER</span> tb_user_delete_trigger</span><br><span class="line">AFTER <span class="keyword">DELETE</span> <span class="keyword">ON</span> tb_user <span class="keyword">FOR</span> <span class="keyword">EACH</span> <span class="type">ROW</span></span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> user_logs(id, operation, operate_time, operate_id, operate_params) <span class="keyword">VALUES</span></span><br><span class="line">(<span class="keyword">null</span>, <span class="string">&#x27;delete&#x27;</span>, NOW(), old.id, CONCAT(<span class="string">&#x27;删除的数据为: id=&#x27;</span>, old.id, <span class="string">&#x27;,name=&#x27;</span>, old.name));</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> tb_user <span class="keyword">WHERE</span> name<span class="operator">=</span><span class="string">&#x27;狗子&#x27;</span>;</span><br></pre></td></tr></table></figure><h2 id="锁"><a href="#锁" class="headerlink" title="锁"></a>锁</h2><p>锁是计算机协调多个进程或线程并发访问某一资源的机制。在数据库中，除传统的计算资源（CPU、RAM、I&#x2F;O）的争用以外，数据也是一种供许多用户共享的资源。如何保证数据并发访问的一致性、有效性是所有数据库必须解决的一个问题，锁冲突也是影响数据库并发访问性能的一个重要因素。从这个角度来说，锁对数据库而言显得尤其重要，也更加复杂。</p><p>分类： MySQL中的锁，按照锁的粒度分，分为以下三类：<br>1 ﻿﻿﻿全局锁：锁定数据库中的所有表。<br>2 ﻿﻿﻿表级锁：每次操作锁住整张表。<br>3 ﻿﻿﻿行级锁：每次操作锁住对应的行数据。</p><h3 id="全局锁"><a href="#全局锁" class="headerlink" title="全局锁"></a>全局锁</h3><p>全局锁就是对整个数据库实例加锁，&#x3D;&#x3D;加锁后整个实例就处于只读状态，后续的DML的写语句，DDL 语句，已经更新操作的事务提交语句都将被阻塞&#x3D;&#x3D;。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190421077.png"><br>其典型的使用场景是做全库的逻辑备份，对所有的表进行锁定，从而获取一致性视图，保证数据的完整性。</p><p>假如不加全局锁可能出现的问题</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190425025.png"></p><ul><li>备份 <code>tb_stock</code> 商品库存表</li><li>这个时候有人下单，会先减库存</li><li>然后又去生成订单</li><li>备份 <code>tb_order</code> 订单表</li><li>插入订单日志</li><li>备份订单日志<br>最后生成的日志文件会出现问题：商品库存和订单、订单日志对不上，出现数据不一致性。</li></ul><h4 id="执行数据库备份"><a href="#执行数据库备份" class="headerlink" title="执行数据库备份"></a>执行数据库备份</h4><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190429944.png"></p><h5 id="方法一"><a href="#方法一" class="headerlink" title="方法一"></a>方法一</h5><p>步骤：</p><ol><li><p>对数据库加全局锁</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">FLUSH TABLES <span class="keyword">WITH</span> READ LOCK;</span><br></pre></td></tr></table></figure><p>此时其他用户只能查询，不能更新、写入了</p></li><li><p>进行数据库备份</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysqldump -uroot -pHliu1026. [-h ip(数据库所在ip)] demo &gt; /Users/ice/Desktop/demo.sql</span><br></pre></td></tr></table></figure><p>这个命令是在终端执行的，在 <code>macos</code> 命令行执行， 因为它不是数据库语句。命令里面包含用户名、密码、要备份的数据库、保存的位置。</p></li><li><p>解开锁</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">UNLOCK TABLES;</span><br></pre></td></tr></table></figure><p>数据库中加全局锁，是一个比较重的操作，存在以下问题：</p></li><li><p>如果在主库上备份，那么在备份期间都不能执行更新，业务基本上就得停摆。</p></li><li><p>如果在从库上备份，那么在备份期间从库不能执行主库同步过来的二进制日志（binlog），会导致主从延迟。<br>所以推荐方法二。</p></li></ol><h5 id="方法二"><a href="#方法二" class="headerlink" title="方法二"></a>方法二</h5><p>在InnoDB引擎中，我们可以在备份时加上参数 <code>--single-transaction</code> 参数来完成不加锁的一致性数据备份。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysqldump <span class="operator">-</span>uroot <span class="operator">-</span>pHliu1026. <span class="comment">--single-transaction demo &gt; /Users/ice/Desktop/demo.sql</span></span><br></pre></td></tr></table></figure><p>推荐使用这种方式， 利用 InnoDB 的MVCC（多版本并发控制）特性。在备份开始瞬间开启一个事务，拿到一个“一致性快照”。几乎不锁表，备份期间可以正常进行增删改查操作！！！</p><h3 id="表级锁"><a href="#表级锁" class="headerlink" title="表级锁"></a>表级锁</h3><p>表级锁，每次操作锁住整张表。锁定粒度大，发生锁冲突的概率最高，并发度最低。应用在MyISAM、InnoDB、BDB等存储引擎中。<br>对于表级锁，主要分为以下三类：</p><ol><li>表锁</li><li>元数据锁（meta data lock,MDL）</li><li>意向锁<br>对于表锁，分为两类</li><li>表共享读锁</li><li>表独占写锁</li></ol><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 加锁</span></span><br><span class="line">LOCK TABLES 表<span class="number">1</span> [,表<span class="number">2</span>] READ<span class="operator">/</span>WRITE # 加读锁或写锁</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 释放锁</span></span><br><span class="line">UNLOCK TABLES <span class="operator">/</span>或者客户端断开连接</span><br></pre></td></tr></table></figure><h4 id="表锁"><a href="#表锁" class="headerlink" title="表锁"></a>表锁</h4><p><strong>读锁</strong></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190444216.png"><br>客户端1 <code>LOCAK TABLES score READ;</code> ( 举例 <code>score</code> 为一个表)<br>此后客户端1，2都只能对表数据进行读，无法写入</p><ul><li>但是还不一样，如果是上锁的那个去修改，会直接报错 <code>ERROR 1099(HY000): Tables ‘score’ was locked with a READ lock and can’t be updated</code>，但是客户端 <code>2</code> 去修改直接阻塞，不会报错。（等到锁释放了，该操作成功了就可以了）<br>客户端1 <code>UNLOCK TABLES;</code> 之后，锁释放</li></ul><p><strong>写锁</strong></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190449099.png"><br>客户端1 <code>LOCAK TABLES score WRITE;</code><br>客户端1可读可写<br>客户端2不可读不可写，读写都会被阻塞，阻塞到这个表的表锁释放。</p><h4 id="元数据锁"><a href="#元数据锁" class="headerlink" title="元数据锁"></a>元数据锁</h4><p>MDL（元数据锁）加锁过程是系统自动控制，无需显式使用，在访问一张表的时候会自动加上。MDL锁主要作用是维护&#x3D;&#x3D;表元数据&#x3D;&#x3D;（也就是表结构数据）的数据一致性，在表上有活动事务的时候，不可以对元数据进行写入操作。&#x3D;&#x3D;为了避免DML与DDL冲突，保证读写的正确性&#x3D;&#x3D;。</p><blockquote><p>DML 是数据操作语言，用于对表的数据增删改<br>DDL 是数据库定义语句，用来定义或修改数据库结构、表结构</p></blockquote><p>在MySQL5.5中引入了MDL，当对一张表进行增删改查的时候，加MDL读锁（共享）；当对表结构进行变更操作的时候，加MDL写锁（排他）。<br><img src="/MySQL/image%20122.png"></p><ul><li><code>SHARED_READ</code> 和 <code>SHARED_WRITE</code> 是互相兼容的，可以一块出现。但是他们两个都和 <code>EXCLUSIVE</code> 互斥。<br>可以通过以下语句查看锁情况</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> object_type, object_schema, object_name, lock_type, lock_duration <span class="keyword">FROM</span> performance_schema.metadata_locks;</span><br></pre></td></tr></table></figure><p>我们下面进行验证，开启了两个 MySQL 终端，执行顺序如下</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190525760.png"><br>1，2，3 —&gt; 证明开启事务并不会出现锁<br>4，5 —&gt; 查询语句会开启 <code>SHARED_READ</code> 锁</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190532891.png"><br>之后我们再执行 <code>UPDATE</code>，之后，发现 <code>tb_user</code> 表还出现了 <code>SHARED_WRITE</code> 锁（<code>user_logs</code> 也出现了，因为之前的触发器没删除)</p><ul><li><p>注意，我们是先开启事务了，为什么？如果你不开启事务，只执行一个查询语句会上锁加解锁，你查表结构你都看不到，所以开个事务能看到。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190537667.png"><br>加入我们的查询和更新在终端1，然后更新表结构的操作在终端2，看红字 <code>5</code>，是被阻塞的。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190544290.png"><br>只有终端1 <code>commit</code> 操作后，才能解开锁，让 <code>ALTER</code> 继续执行，如红字7，8，等待了40多秒。</p></li></ul><p>为什么我们在终端1进行查询、更新，终端2来执行表结构更新呢？因为我最一开始试着在终端1开启事务，然后进行查询、更新操作之后（1，2，3），再执行表结构更新，发现更新成功了（4，5）。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190549445.png"><br>原因是因为在同&#x3D;&#x3D;一个事务里面，你在一个还没有提交的事务里面执行DDL语句的时候，MySQL会在执行这个DDL之前，自动隐式的提交当前事务，所以表结构更新成功了&#x3D;&#x3D;</p><h4 id="意向锁"><a href="#意向锁" class="headerlink" title="意向锁"></a>意向锁</h4><p>为了避免DML在执行时，加的行锁与表锁的冲突，在InnoDB中引入了意向锁，使得表锁不用检查每行数据是否加锁，使用意向锁来减少表锁的检查。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190557135.png"><br>比如上面这个<br>线程A开启事务，并且更新 <code>id=3</code> 的数据，这个时候会对数据上一个行锁<br>此时线程B想对表上表锁，但是你想，表锁是不是和行锁冲突，如果有一行有锁，那表锁就上不了了，所以需要检查有没有行锁，就需要遍历整张表，看有没有行锁，这个时候就太麻烦了，所以引入了意向锁。&#x3D;&#x3D;当一个事务准备给某一行加锁时，必须先给整张表加一个意向锁，所以意向锁是表级锁，但是表达的是行级意向&#x3D;&#x3D;。</p><p>意向锁有两种</p><ol><li>﻿﻿﻿意向共享锁（<code>IS</code>）：由语句 <code>select... lock in share mode</code> 添加。</li><li>﻿﻿﻿意向排他锁（<code>IX</code>）：由 <code>insert、update、delete、select... for update</code> 添加。</li></ol><hr><p>单独介绍一下这两个东西</p><h5 id="SELECT…LOCK-IN-SHARE-MODE"><a href="#SELECT…LOCK-IN-SHARE-MODE" class="headerlink" title="SELECT…LOCK IN SHARE MODE"></a>SELECT…LOCK IN SHARE MODE</h5><p>MySQL8.0更推荐语法 <code>SELECT… FOR SHARE</code>。<br>默认情况下， MySQL 的普通 SELECT 查询是不加任何锁的， 但在很多业务场景下，我们需要在读取数据的同时把它锁住，防止其他事务修改它<br><strong>典型使用场景</strong>：<strong>确认数据是否存在，并确保在后续操作期间它不被改变。</strong><br>比如说： 你要在子表插入一条记录，前提是父表必须存在对应的 ID。你可以先 SELECT … LOCK IN SHARE MODE 查一下父表，确认存在后再插入子表。这样能防止在你查询确认后、插入子表前，有其他事务把父表里的那条记录删了。</p><p>加上之后，别的事务可以进行普通的 <code>SELECT</code>，也可以加 <code>LOCK IN SHARE MODE</code>，加共享锁。大家都可以读，但是其他事务不能再更新、删除。</p><h5 id="SELECT…FOR-UPDATE"><a href="#SELECT…FOR-UPDATE" class="headerlink" title="SELECT…FOR UPDATE"></a>SELECT…FOR UPDATE</h5><p>加上之后，别的事务职能进行普通的 <code>SELECT</code>，不能加共享锁 <code>LOCK IN SHARE MODE</code>，不能 <code>SELECT…FOR UPDATE</code>，更不能修改、删除。<br><strong>典型使用场景</strong>：<strong>准备更新数据，并且绝对不允许别人同时修改（防止丢失更新）。</strong></p><p>比如说： 电商扣减库存、银行转账。你先 <code>SELECT stock FROM table WHERE id=1 FOR UPDATE</code> 把这件商品的库存锁住，然后在程序里计算 <code>new_stock = stock - 1</code>，最后再 UPDATE 回去。如果你只用普通 SELECT，可能有两个并发请求同时读到库存为 10，然后都更新为 9，导致超卖。</p><p>回归正传，两个锁特性如下</p><ol><li>意向共享锁（IS）：与表锁共享锁（read）兼容，与表锁排它锁（write） 互斥。</li><li>意向排他锁（IX）：与表锁共享锁（read）及排它锁（write）都互斥。意向锁之间不会互斥。</li></ol><blockquote><p>你想，意向共享锁是怎么添加的？<code>SELECT…LOCK IN SHARE MODE</code>，也就是说它的作用是保证读不会出错，所以不允许修改，所以表锁共享锁可以加，但是不能加表锁排它锁。</p><p>意向排他锁是更新、插入、删除的时候都会加行锁，生成意向排他锁，也就是说在锁期间会在什么时机更改是不知道的，表锁想拿到控制只能等这个行锁解除才可以，所以和表锁读&#x2F;写都互斥</p><p>为什么不会互斥呢？<br>因为你想，意向锁其实只是个标志 （计数器），如果对某一行上锁了，意向锁就存在了，如果我对另一行也上锁，行锁之间不会互斥，意向锁也会多一个，就也不能互斥<br>可以通过以下SQL，查看意向锁及行锁的加锁情况：</p></blockquote><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> object_schema, object_name, index_name, lock_type, lock_mode, lock_data <span class="keyword">FROM</span> performance_schema.data_locks;</span><br></pre></td></tr></table></figure><p>下面演示意向共享锁</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190624332.png"></p><ol><li>开启事务</li><li>对某一行添加了行锁，同时也加上了意向共享锁</li><li>查看锁情况，可以看到两条，第一个是 IS 表锁，第二个是 <code>RECORD</code>，也就是行锁</li><li>然后当加表锁READ的时候是可以成功的</li><li>释放表读锁</li><li>添加表写锁（阻塞） 也就是添加失败</li><li>提交事务</li><li>表写锁从阻塞变为成功</li></ol><p>下面演示意向排他锁</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190638813.png"></p><ol><li>开启事务</li><li>对某一行进行修改，也就添加了行锁和意向排他锁</li><li>查看锁</li><li>添加表读锁（阻塞）</li><li>提交事务</li><li>表读锁从阻塞变为添加成功<br>表读锁都加不上，表写锁更加不上了。</li></ol><h3 id="行级锁"><a href="#行级锁" class="headerlink" title="行级锁"></a>行级锁</h3><p>行级锁，每次操作锁住对应的行数据。锁定粒度最小，发生锁冲突的概率最低，并发度最高。应用在InnoDB存储引擎中。&#x3D;&#x3D;还记得吗，行级锁是InnDB存储引擎的一大特性&#x3D;&#x3D;<br>InnoDB的数据是基于索引组织的，行锁是通过对索引上的索引项加锁来实现的，而不是对记录加的锁。对于行级锁，主要分为以下三类：</p><ol><li><p>行锁（Record Lock）：锁定单个行记录的锁，防止其他事务对此行进行update和delete。在RC、RR隔离级别下都支持。（Read committed 和 Repeatable Read）。<code>5</code>。单个数据</p></li><li><p>间隙锁（Gap Lock）：锁定索引记录间隙（不含该记录），确保索引记录间隙不变，防止其他事务在这个间隙进行insert，产生幻读。在RR隔离级别下都支持。<code>(3,5)</code></p></li><li><p>临键锁（Next-Key Lock）：行锁和间隙锁组合，同时锁住数据，并锁住数据前面的间隙Gap。在RR隔离级别下支持。<code>(3,5]</code>，左开右闭</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190705706.png"></p></li></ol><p>InnoDB实现了以下两种类型的行锁：</p><ol><li><p>共享锁（S）：允许一个事务去读一行，阻止其他事务获得相同数据集的排它锁。</p></li><li><p>排它锁（X）：允许获取排它锁的事务更新数据，阻止其他事务获得相同数据集的共享锁和排它锁。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190713688.png"><br>&#x3D;&#x3D;SQL语句加锁&#x3D;&#x3D;情况如下</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190720718.png"></p></li></ol><p>默认情况下，InnoDB 在 <code>REPEATABLE READ</code> 事务隔离级别运行，InnoDB 使用 <code>next-key</code> 锁进行搜索和索引扫描，以防止幻读。</p><ol><li>针对唯一索引进行检索时，对已存在的记录进行等值匹配时，将会自动优化为行锁。</li><li>InnoDB的行锁是针对于索引加的锁，不通过索引条件检索数据，那么InnoDB将对表中的所有记录加锁，此时就会升级为表锁。</li></ol><p>怎么有幻读呢？如下场景<br>假设你的表 stu 中只有两条记录：id&#x3D;3 和 id&#x3D;8。</p><ol><li>事务 A 执行：<code>SELECT * FROM stu WHERE id &gt; 3 FOR UPDATE;</code></li><li>如果没有 Next-Key 锁，事务 A 只锁住了现有的 id&#x3D;8。</li><li>事务 B 此时插入一条新记录：INSERT INTO stu (id) VALUES (5);（成功插入，因为 id&#x3D;5 没人锁）。</li><li>事务 A 再次执行同样的查询，发现多出了 id&#x3D;5 的数据！—— <strong>这就是“幻读”</strong>。<br>为了解决这个问题，InnoDB 使用 Next-Key 锁，把 (3, 8] 甚至 (8, +∞) 的所有空间都封死。这样事务 B 想在 id&#x3D;5 的位置插入时，会被挡在门外。</li></ol><h4 id="演示"><a href="#演示" class="headerlink" title="演示"></a>演示</h4><h5 id="共享锁"><a href="#共享锁" class="headerlink" title="共享锁"></a>共享锁</h5><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190728186.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190736701.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190742124.png"></p><ol><li>开启事务A</li><li>普通查询</li><li>查看锁（锁为空，因为普通查询不会开启锁）</li><li>查询，但是加上了共享锁</li><li>查看锁（首先有意向锁 <code>IS</code>，其次对于这个数据加了行锁，类型 <code>S, REC_NOT_GAP</code> 含义一个是代表共享锁，后面代表行锁不是间隙锁）</li><li>另一个事务B对同一个数据加上共享锁（是成功的，因为共享锁不互斥）</li><li>查询锁结构（同一个记录有两个行锁、同时有两个 IS 意向锁）</li><li>提交事务B</li><li>查看锁（事务B的行锁释放、意向锁释放，这样只剩下事务A的行锁、意向锁）</li></ol><h5 id="排他锁"><a href="#排他锁" class="headerlink" title="排他锁"></a>排他锁</h5><p>另外一个示例很简单，说一下就行<br>就是事务A开始事务，更新 <code>id=1</code> 的数据，然后事务B也开启事务，更新 <code>id=1</code> 的数据（会阻塞），因为事务A对这个数据加了行锁，还是排它锁。只有事务A提交了，事务B才会继续。</p><p>还有一个也演示过（见SQL优化，UPDATE优化章节）</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190755715.png"><br>就是行锁升级为表锁的情况，解决方法就是更新的时候根据索引列去更新。</p><h5 id="间隙锁-临键锁"><a href="#间隙锁-临键锁" class="headerlink" title="间隙锁&#x2F;临键锁"></a>间隙锁&#x2F;临键锁</h5><p>默认情况下，InnoDB在 REPEATABLE READ 事务隔离级别运行，InnoDB使用 next-key 锁进行搜索和索引扫描，以防止幻读（不只要锁住已经存在的数据，还要锁住不存在的数据所在的区间）。</p><ol><li>索引上的等值查询（唯一索引），给不存在的记录加锁时，优化为间隙锁。如果存在就变成行锁了。</li><li>索引上的等值查询（普通索引），向右遍历时最后一个值不满足查询需求时，next-key lock 退化为间隙锁。</li><li>索引上的范围查询（唯一索引）–会访问到不满足条件的第一个值为止。<br>注意：&#x3D;&#x3D;间隙锁唯一目的是防止其他事务插入间隙。间隙锁可以共存，一个事务采用的间隙锁不会阻止另一个事务在同一间隙上采用间隙锁&#x3D;&#x3D;。</li></ol><p>间隙锁演示（演示第一个）<br>索引上的等值查询（唯一索引），给不存在的记录加锁时，优化为间隙锁。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190806071.png"></p><ol><li>先查询有哪些数据</li><li>开启事务 <code>A</code>，并且对不存在的记录 <code>id=5</code> 进行更新，影响行数是 <code>0</code>，说明没有对实际的数据进行更新</li><li>查看锁，因为有更新操作，所以有意向排他锁和行锁，行锁类型是 <code>X, GAP</code>，排他锁，而且是间隙锁，<code>lock_data=8</code> 的含义是对 <code>id=8</code> 之前的数据（一直到<code>id=3</code>，不包含<code>3</code>）进行上锁（不包含 <code>8</code>）。</li><li>开启事务B</li><li>插入 <code>id=7</code> 的数据（阻塞）</li><li>提交事务A，锁释放，事务B由阻塞变为执行成功。</li></ol><p>&#x3D;&#x3D;？&#x3D;&#x3D; 你可能会好奇，只要对 <code>id=5</code> 的加锁不就行了，因为如果出现幻读，也是别的事务插入了 <code>id=5</code> 的才可能出现幻读，对 <code>(3,8)</code> 都加干嘛？<br>因为数据库底层索引是 B+ 数，不存在的行是没有物理实体的，没办法给一个不存在的东西加锁，所以只能搞间隙锁，但是缺点显而易见，跟事务没关系的 <code>id</code> 数据也不能插入了，同时也带来死锁问题。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190814753.png"></p><p>演示第二个<br>索引上的等值查询（普通索引），向右遍历时最后一个值不满足查询需求时，next-key lock 退化为间隙锁。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190820521.png"></p><ol><li><p>查看数据</p></li><li><p>为 <code>age</code> 建立普通索引（值可以不唯一）</p></li><li><p>开启事务</p></li><li><p>查询并加锁</p></li><li><p>查看锁的情况。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190836200.png"><br>说一下 <code>lock_mode</code> 的行锁含义</p></li></ol><ul><li><code>S</code> 后面没有后缀，代表 <code>Next-Key Lock</code>，代表记录锁+前面的间隙锁</li><li><code>S, GAP</code> 代表纯粹的 <code>Gap Lock</code>，只锁前面的间隙，不锁记录本身</li><li><code>S, REC_NOT_GAP</code> 纯粹的行锁，不锁间隙，只锁本身<br><code>lock_data</code> 含义，在这里，第一个值代表 <code>age</code>，第二个值代表 <code>id</code>。（二级索引值，主键值）</li></ul><p>详细来看，第一个 <code>IS</code> 为意向共享锁<br>第二个是在二级索引上加了临键锁，这里是锁住了 <code>(1, 3]</code><br>第三个是需要对主键上锁，就是行锁，锁住了 <code>3</code><br>第四个是对二级索引上了间隙锁，<code>(3, 7)</code><br>这样覆盖的就全面了，在之前之后插入 <code>age=3</code> 的都不会有问题了。</p><p>&#x3D;&#x3D;?&#x3D;&#x3D; <code>age</code> 上怎么加了两个锁？为什么加两个？<br>因为二级索引是 <code>B+</code> 数，排序规则是 <code>age, id</code>，所以相等的 <code>age</code>，可能之前也存在小于 <code>age</code> 的 <code>id</code>，之后也有大于它的 <code>id</code>，所以之前之后都要锁。实际范围已经是 <code>(1, 7)</code> 了。<br>继续提问 &#x3D;&#x3D;?&#x3D;&#x3D; 那为什么不直接在 <code>7</code> 上加间隙锁？从 <code>(1,7)</code>。<br>要知道锁的原理，锁是需要放在实体上的，比如放在 <code>(3, 3)</code>，也就是 <code>age=3,id=3</code> 这个实体上，放的如果是行锁那就代表只锁这一个，放的是间隙锁就代表从这个节点到之前的一个节点中间不能插入数据了，如果放的是临键锁，说明当前和之前的都不能插入了。所以在 <code>(7,7)</code> 上没办法只插入一个锁来实现。</p><p>&#x3D;&#x3D;?&#x3D;&#x3D; 为什么还会对主键加锁？<br>行锁，临键锁都会对主键加锁，间隙锁不会。<br>因为加锁也有防止修改数据的因素，如果别人通过主键修改怎么办？所以要对主键加锁。间隙锁不包含存在的数据，所以不用对主键加锁，也没法加（没有实体）</p><p>这里对主键加锁是为了防止对数据修改和删除，对二级索引加锁是为了防止数据插入，因为你看如果想插入 <code>age=3</code> 的，那必须修改二级索引，这个时候就违背了间隙锁或者临键锁，不能插入，如果想修改 <code>age=3</code> 的，就得去聚集索引那个地方改，但是也加锁了，不能改，这样数据就安全了！！！！真厉害。</p><p>演示第三个<br>索引上的范围查询（唯一索引）–会访问到不满足条件的第一个值为止。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190852964.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190906441.png"></p><ol><li>开启事务</li><li>查询 <code>id &gt;= 19</code> 的数据</li><li>详细看有什么锁<ul><li><code>id=19</code> 的一个行锁，只锁这个数据</li><li>从 25 往前，包含 <code>25</code> 的临键锁 <code>(19, 25]</code></li><li>到正无穷的临键锁 <code>(25, 正无穷)</code> —&gt; 第二个</li></ul></li></ol><h2 id="InnoDB-引擎"><a href="#InnoDB-引擎" class="headerlink" title="InnoDB 引擎"></a>InnoDB 引擎</h2><h3 id="逻辑存储结构-1"><a href="#逻辑存储结构-1" class="headerlink" title="逻辑存储结构"></a>逻辑存储结构</h3><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190913430.png"><br>这个表空间就是数据库里面每张表都对应一个 <code>idb</code> 文件。</p><h3 id="架构"><a href="#架构" class="headerlink" title="架构"></a>架构</h3><p>MySQL5.5版本开始，默认使用InnoDB存储引擎，它擅长事务处理，具有崩溃恢复特性，在日常开发中使用非常广泛。下面是InnoDB架构图，左侧为内存结构，右侧为磁盘结构。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190920258.png"></p><h4 id="内存架构"><a href="#内存架构" class="headerlink" title="内存架构"></a>内存架构</h4><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190927316.png"></p><ul><li>Buffer Pool：缓冲池是主内存中的一个区域，里面可以缓存磁盘上经常操作的真实数据，在执行增删改查操作时，先操作缓冲池中的数据（若缓冲池没有数据，则从磁盘加载并缓存），然后再以一定频率刷新到磁盘，从而减少磁盘IO，加快处理速度。<br>缓冲池以Page页为单位，底层采用链表数据结构管理Page。根据状态，将Page分为三种类型（对应图中三种颜色）：<ul><li>﻿﻿free page： 空闲page，未被使用。</li><li>﻿﻿clean page：被使用page，数据没有被修改过。</li><li>﻿﻿dirty page：脏页，被使用page，数据被修改过，页中数据与磁盘的数据产生了不一致。</li></ul></li></ul><ul><li><p>Change Buffer： 更改缓冲区（针对于非唯一二级索引页，唯一索引和聚集索引都不会操作这个），在执行DML语句时，如果这些数据Page没有在Buffer Pool中，不会直接操作磁盘，而会将数据变更存在更改缓冲区 Change Buffer 中，在未来数据被读取时，再将数据合并恢复到Buffer Pool中，再将合并后的数据刷新到磁盘中。<br>&#x3D;&#x3D;?&#x3D;&#x3D; Change Buffer的意义是什么？<br>与聚集索引不同，二级索引通常是非唯一的，并且以相对随机的顺序插入二级索引（聚集索引通常是按顺序增大的，auto_increment）。同样，删除和更新可能会影响索引树中不相邻的二级索引页，如果每一次都操作磁盘，会造成大量的磁盘IO。有了ChangeBuffer之后，我们可以在缓冲池中进行合并处理，减少磁盘IO。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190938477.png"></p></li><li><p>Adaptive Hash Index：自适应hash索引，用于优化对Buffer Pool数据的查询。InnoDB存储引擎会监控对表上各索引页的查询，如果观察到hash索引可以提升速度，则建立hash索引，称之为自适应hash索引。（哈希索引按值查询快，但是不能进行范围检索）<br>&#x3D;&#x3D;自适应哈希索引，无需人工干预，是系统根据情况自动完成&#x3D;&#x3D;。<br>参数: adaptive_hash_index</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190947387.png"><br>默认系统是开启自适应哈希索引的。</p></li><li><p>Log Buffer：日志缓冲区，用来保存要写入到磁盘中的log日志数据（redo log、undo log），默认大小为 16MB，日志缓冲区的日志会定期刷新到磁盘中。如果需要更新、插入或删除许多行的事务，增加日志缓冲区的大小可以节省磁盘I&#x2F;O。<br>参数：<br>innodb_log_buffer_size: 缓冲区大小<br>innodb_flush_log_at_trx_commit：日志刷新到磁盘时机</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330190953859.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191003678.png"></p><ul><li><code>1</code> 日志在每次事务提交时写入并刷新到磁盘</li><li><code>0</code> 每秒将日志写入并刷新到磁盘一次</li><li>日志在每次事务提交后写入，并每秒刷新到磁盘一次</li></ul></li></ul><p>如果有一台专门的 MySQL 服务器，一般 80% 的内存都会分配给缓冲区。</p><h4 id="磁盘结构"><a href="#磁盘结构" class="headerlink" title="磁盘结构"></a>磁盘结构</h4><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191013171.png"></p><ul><li><p>System Tablespace：系统表空间是更改缓冲区的存储区域。如果表是在系统表空间而不是每个表文件或通用表空间中创建的，它也可能包含表和索引数据。（在MySQL5.x版本中还包含InnoDB数据字典、undolog等）<br>也就是说它存放了更改缓冲区的数据，如果说表不是一个表一个文件，而是一个共享空间，那就是放在这里面的。<br>参数: innodb_data_file_path</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191021774.png"><br>默认是存放在 ibdata1 文件里面的</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191043655.png"></p></li><li><p>File-Per-Table Tablespaces：每个表的文件表空间包含单个InnoDB表的数据和索引，并存储在文件系统上的单个数据文件中。（比如 course.ibd，student.ibd）<code>表名.ibd</code>。<br>参数: innodb_file_per_table（默认是开启的，也就是说一个表一个文件，如果关闭，所有的表数据和索引都会放在上面这个系统表空间）</p></li><li><p>General Tablespaces：通用表空间，需要通过 <code>CREATE TABLESPACE</code> 语法创建通用表空间，在创建表时，可以指定该表空间。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> TABLESPACE xxx <span class="keyword">ADD</span> # 逻辑名称，用于在 MySQL 命令里</span><br><span class="line">DATAFILE <span class="string">&#x27;file_name&#x27;</span> # 指定关联的表空间文件，物理文件名</span><br><span class="line">ENGINE<span class="operator">=</span>engine_name</span><br><span class="line"></span><br><span class="line"># 创建通用表空间</span><br><span class="line"><span class="keyword">CREATE</span> TABLESPACE ts_itheima <span class="keyword">ADD</span> DATAFILE <span class="string">&#x27;myitheima.ibd&#x27;</span> ENGINE<span class="operator">=</span>INNODB;</span><br><span class="line"># 将表数据放入该通用表空间</span><br><span class="line"><span class="keyword">CREATE TABLE</span> a(id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span> AUTO_INCREMENT, name <span class="type">VARCHAR</span>(<span class="number">10</span>)) ENGINE<span class="operator">=</span>INNODB TABLESPACE ts_itheima;</span><br></pre></td></tr></table></figure><p>&#x3D;&#x3D;？&#x3D;&#x3D; 这个通用表空间干什么用的呢？<br>系统表空间是什么？一个数据库一个，必须创建的<br>独立表空间，每个表一个的<br>通用表空间是什么？我自己创建一个表空间，我可以指定谁放进去，我也可以指定这个文件的位置（可以放到读写更快的SSD）。比如有些表数据很少，一个表只有几条数据的，我可以把这些表放在一个表空间。并且可以跨数据库存表（不同数据库的表可以放在一个文件夹里面，但是不能跨引擎）</p></li><li><p>Undo Tablespaces：撤销表空间，MySQL实例在初始化时会自动创建两个默认的undo表空间（undo001, undo002，初始大小16M），用于存储undo log日志。</p></li><li><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191059679.png"></p></li><li><p>Temporary Tablespaces: InnoDB使用会话临时表空间和全局临时表空间。存储用户创建的临时表等数据。</p></li><li><p>Doublewrite Buffer Files： 双写缓冲区，innoDB引擎将数据页从Buffer Pool刷新到磁盘前，先将数据页写入双写缓冲区文件中，便于系统异常时恢复数据。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191108603.png"></p></li><li><p>Redo Log：重做日志，是用来实现事务的持久性。该日志文件由两部分组成：重做日志缓冲（redo log buffer）以及重做日志文件（redo log），前者是在内存中，后者在磁盘中。当事务提交之后会把所有修改信息都会存到该日志中，用于在刷新脏页到磁盘时，发生错误时，进行数据恢复使用。<br>不会永久保存，每隔一段时间会清理之前的 redo log。<br>以循环方式写入重做日志文件，涉及两个文件 <code>ib_logfile0</code>，<code>ib_logfile1</code>。</p></li></ul><h4 id="后台线程"><a href="#后台线程" class="headerlink" title="后台线程"></a>后台线程</h4><p>用来在合适的时机将缓冲池的数据刷新到磁盘</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191118479.png"></p><ol><li><p>Master Thread<br>核心后台线程，负责调度其他线程，还负责将缓冲池中的数据异步刷新到磁盘中，保持数据的一致性，还包括脏页的刷新、合并插入缓存、undo页的回收。</p></li><li><p>IO Thread<br>在InnoDB存储引擎中大量使用了AIO（异步IO）来处理IO请求，这样可以极大地提高数据库的性能，而IO Thread主要负责这些IO请求的回调。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191127298.png"><br>可以通过如下指令查看状态</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> ENGINE INNODB STATUS;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191133127.png"><br>aio reads 全部 4 个 0，代表都在等待。</p></li><li><p>Purge Thread<br>主要用于回收事务已经提交了的undo log，在事务提交之后，undo log可能不用了，就用它来回收。</p></li><li><p>Page Cleaner Thread<br>协助 Master Thread 刷新脏页到磁盘的线程，它可以减轻 Master Thread 的工作压力，减少阻塞。</p></li></ol><h3 id="事务原理"><a href="#事务原理" class="headerlink" title="事务原理"></a>事务原理</h3><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191146695.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191155435.png"><br>事务的四个特性，原子性、一致性、持久性由 <code>redo log</code>，<code>undo log</code> 文件维护，隔离性由锁、MVCC机制实现</p><h4 id="redo-log"><a href="#redo-log" class="headerlink" title="redo log"></a>redo log</h4><p>重做日志，记录的是事务提交时数据页的物理修改，是用来实现事务的持久性。<br>该日志文件由两部分组成：重做日志缓冲（redo log buffer）以及重做日志文件（redo log file），前者是在内存中，后者在磁盘中。当事务提交之后会把所有修改信息都存到该日志文件中，用于在刷新脏页到磁盘，发生错误时，进行数据恢复使用。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191159591.png"><br>比如说有一个事务，里面有很多操作，在操作时会先去 buffer pool 看有没有该页的数据，如果没有，就会通过后台线程将数据从磁盘读取出来缓存在内存中，修改后缓存区的页就变成了脏页。改完之后，数据页的变化会存储在 Redolog buffer 里面，当事务提交的时候，这个数据页变化信息会刷新到磁盘里面，等后面脏页数据刷新到磁盘的时候，如果出现错误，这个时候就可以根据 ib_logfile0&#x2F;1 来进行恢复（这个恢复到意思是把事务恢复到事务提交之后的状态，不是之前的，因为事务已经提交了，出错了也不能去撤回了)。当脏页数据刷新到磁盘之后，redo log 磁盘文件记录的也就没用了，所以 ib_logfile0&#x2F;1 是循环写入的，不会永久保存。</p><p>&#x3D;&#x3D;?&#x3D;&#x3D; 事务提交的时候为什么不直接刷新到磁盘？<br>因为通常事务是包含很多条语句，通常是随机读取I&#x2F;O的，性能低。如果有 Redolog，先把数据页变化异步刷新到磁盘，因为日志文件都是顺序记录的，所以I&#x2F;O速度较快，这种机制叫WAL。这样过一段时间之后再把脏页数据刷新到磁盘里面。<br>&#x3D;&#x3D;?&#x3D;&#x3D; ib_logfile0&#x2F;1怎么用的？<br>顺序写满 0，0写满后，切换到1，1写满了，跳回0开头重新覆盖写。<br>&#x3D;&#x3D;?&#x3D;&#x3D; 那如果刷新很慢怎么办？两个日志文件都满了，但是脏数据还没刷过去<br>MySQL就会阻塞所有的更新操作，把脏页全部刷到磁盘，腾出Redo Log空间，这也是著名的由于日志写满导致的性能抖动。 </p><h4 id="undo-log"><a href="#undo-log" class="headerlink" title="undo log"></a>undo log</h4><p>用来保证原子性。</p><p>回滚日志，用于记录数据被修改前的信息，作用包含两个：提供回滚和 MVCC（多版本并发控制）。<br>undo log和redo log记录物理日志不一样，它是逻辑日志（redo log记录的是里面有什么数据，叫物理日志，undo log记录的是什么操作，叫逻辑日志）。可以认为当delete一条记录时，undo log中会记录一条对应的insert记录，反之亦然，当update一条记录时，它记录一条对应相反的update记录。当执行rollback时，就可以从undolog中的逻辑记录读取到相应的内容并进行回滚。</p><p>Undolog销毁：undo log在事务执行时产生，事务提交时，并不会立即删除undo log，因为这些日志可能还用于MVCC。<br>Undo log存储：undo log采用段的方式进行管理和记录，存放在前面介绍的 rollback segment 回滚段中，内部包含1024个undo log segment.</p><h3 id="MVCC"><a href="#MVCC" class="headerlink" title="MVCC"></a>MVCC</h3><p><strong>当前读</strong><br>&#x3D;&#x3D;读取的是记录的最新版本，读取时还要保证其他并发事务不能修改当前记录，会对读取的记录进行加锁&#x3D;&#x3D;。对于我们日常的操作，如：<code>select...lock in share mode</code>（共享锁），<code>select..for update</code>、<code>update</code>、<code>insert</code>、<code>delete</code>（排他锁）都是一种当前读。<br><strong>快照读</strong><br>简单的select（不加锁）就是快照读，快照读，读取的是记录数据的可见版本，有可能是历史数据，不加锁，是非阻塞读。</p><ul><li>Read Committed：每次select，都生成一个快照读。</li><li>Repeatable Read：开启事务后第一个select语句才是快照读的地方。</li></ul><ul><li>Serializable：快照读会退化为当前读。<br><strong>MVCC</strong><br>全称 Multi-Version Concurrency Control，多版本并发控制。指维护一个数据的多个版本，使得读写操作没有冲突，快照读为MySQL实现MVCC提供了一个非阻塞读功能。MVCC的具体实现，还需要依赖于数据库记录中的三个隐式字段、undo log日志、readView。</li></ul><h4 id="实现原理"><a href="#实现原理" class="headerlink" title="实现原理"></a>实现原理</h4><h5 id="记录中的隐藏字段"><a href="#记录中的隐藏字段" class="headerlink" title="记录中的隐藏字段"></a>记录中的隐藏字段</h5><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191209798.png"><br>我们可以验证一下有没有</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191213965.png"><br>去到数据库存放的位置下面，通过如下命令查看 ibd 文件</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ibd2sdi user.ibd</span><br></pre></td></tr></table></figure><p>看 <code>columns</code> 列里面，除了自己定义的一些列外，还有其他两种隐藏字段（然后就没别的了，<code>columns</code> 列里面就只有自己定义的列和这两个隐藏字段），<code>DB_ROW_ID</code> 没有，因为这个表有主键。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191218096.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191226288.png"><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191301541.png"><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191314154.png"></p><h5 id="undo-log-1"><a href="#undo-log-1" class="headerlink" title="undo log"></a>undo log</h5><p>回滚日志，在insert、update、delete的时候产生的便于数据回滚的日志。<br>当insert的时候，产生的undolog日志只在回滚时需要，在事务提交后，可被立即删除。<br>而update、delete的时候，产生的undo log日志不仅在回滚时需要，在快照读时也需要，不会立即被删除。<br>那么何时删除？</p><ul><li><strong>事务提交后</strong>：<ul><li>对于INSERT操作，事务提交后，undo log可以被立即删除，因为不再需要用于回滚。</li><li>对于UPDATE和DELETE操作，undo log不会立即被删除，因为它们可能在后续的快照读取中被使用。</li></ul></li><li><strong>快照读取结束</strong>：<ul><li>当所有依赖于该undo log的快照读取操作结束后，undo log才会被删除。这意味着如果有一个事务正在进行快照读取，并且依赖于某个undo log，那么这个undo log会一直保留直到该事务结束。</li></ul></li></ul><p><strong>undo log版本链</strong><br>这是事务列表和修改的记录</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191320155.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191328553.png"></p><p>在执行事务2，修改id为30的记录，age 改为 3 的时候，先把原来的记录保存一份到 undo log，然后再更新这个记录，修改 <code>age</code>，修改 <code>DB_TRX_ID</code> 为修改事务的 <code>id</code>，然后修改 <code>DB_ROLL_PTR</code> 指针。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191335613.png"><br>执行完事务3又如下</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191345335.png"><br>执行完事务4如下</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191349474.png"><br>不同事务或相同事务对同一条记录进行修改，会导致该记录的undo log生成一条记录版本链表，链表的头部是最新的旧记录，链表尾部是最早的旧记录。<br>所以如果某一个事务对记录修改两次，那再 undo log 里面会有两个 <code>DB_TRX_ID</code> 相同的记录。</p><p>这么多 undo log 记录，里面每条数据都不同，快照读的时候读取哪个版本？？？</p><h5 id="readview"><a href="#readview" class="headerlink" title="readview"></a>readview</h5><p>为什么有 readview？<br>当你执行一个普通的 SELECT 时，Undo Log 版本链上可能堆了一长串不同事务修改过的记录。</p><ul><li>有的是已经提交的。</li><li>有的是还没提交的。</li><li>有的是在你查询之后才开始的。</li></ul><p>ReadView（读视图）是快照读 SQL执行时MVCC提取数据的依据，记录并维护系统当前活跃的事务（未提交的）id。<br>ReadView中包含了四个核心字段：</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191354881.png"></p><ul><li><code>max_trx_id</code> 不是活跃事务最大ID+1，有个事务ID可能更大，但是不活跃了。</li><li><code>creator_trx_id</code> 意思是当一个事务进行查询的时候，他会生成一个 readview，哪个事务成的这个 readview 呢？就记录在这个字段里面</li></ul><p>图中的 <code>trx_id</code> 代表 undo log 里面的 <code>DB_TRX_ID</code>。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191359525.png"><br>不同的隔离级别，生成ReadView的时机不同：</p><ul><li>﻿﻿READ COMMITTED：在事务中每一次执行快照读时生成ReadView。</li><li>﻿﻿REPEATABLE READ：仅在事务中第一次执行快照读时生成ReadView，后续复用该ReadView。</li></ul><p>我们先来看一下 RC 隔离级别下的读取情况</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191406152.png"><br>图里面是事务5两次快照读的 ReadView。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191410656.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191417289.png"><br>从记录的数据到 undo log 里面的记录，一个个去匹配规则，第一个成立的就是本次读的。</p><p>在RR隔离级别下</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191423396.png"></p><p>回头看MVCC第一个视频快照读怎么回事？没看明白</p><h2 id="MySQL-管理"><a href="#MySQL-管理" class="headerlink" title="MySQL 管理"></a>MySQL 管理</h2><h3 id="系统数据库"><a href="#系统数据库" class="headerlink" title="系统数据库"></a>系统数据库</h3><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191430019.png"><br>mysql 数据库</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191435431.png"></p><p>information_schema 数据库<br>引擎类型</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191441764.png"><br>数据库表的字段信息</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191447048.png"><br>定义的表信息</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191453214.png"><br>InnoDB 下的表、表空间信息</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191456893.png"><br>定义的函数、存储过程</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191502837.png"><br>数据库信息</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191507252.png"><br>触发器</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191511744.png"></p><p>performance_schema 数据库，之前提到的锁信息在这个里面</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191517918.png"><br>还有metadata_locks。</p><h3 id="常用工具"><a href="#常用工具" class="headerlink" title="常用工具"></a>常用工具</h3><h4 id="命令"><a href="#命令" class="headerlink" title="命令"></a>命令</h4><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191522571.png"><br>上面这个语法，一般在 Shell 脚本里面用</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191528254.png"><br>比如可以查看数据库版本信息、数据库变量配置，创建数据库，删除数据库，查看有哪些数据库等的。这些命令可以直接在终端使用（连接数据库、执行、然后断开)，就不用登陆到mysql里面去执行语句。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191533744.png"><br>比如如下</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191538186.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191544741.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191550542.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191555073.png"><br><code>-i</code> 就是现实数据库或者数据库表的状态信息，不是统计信息了。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191559304.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191602340.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191606498.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191610846.png"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191614594.png"><br>如果是 <code>-n, -t</code> 那么结果只有数据，如果是 <code>-d</code> 那只有数据库、表结构</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysqldump <span class="operator">-</span>uroot <span class="operator">-</span>pHliu1026. <span class="operator">-</span>T <span class="operator">/</span>root demo tb_user # <span class="operator">/</span>root 指保存文件的位置</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330191621545.png"></p>]]>
    </content>
    <id>http://example.com/undefined/4250883613/</id>
    <link href="http://example.com/undefined/4250883613/"/>
    <published>2026-03-30T11:18:04.000Z</published>
    <summary>
      <![CDATA[<h1 id="MySQL"><a href="#MySQL" class="headerlink" title="MySQL"></a>MySQL</h1><p>数据库（DataBase），存储数据的仓库，数据是有组织的进行存储。<br>数据库管理系统（DataBase Man]]>
    </summary>
    <title>MySQL笔记</title>
    <updated>2026-03-30T11:18:23.326Z</updated>
  </entry>
  <entry>
    <author>
      <name>John Doe</name>
    </author>
    <content>
      <![CDATA[<h1 id="基础篇"><a href="#基础篇" class="headerlink" title="基础篇"></a>基础篇</h1><h2 id="1-初识Redis"><a href="#1-初识Redis" class="headerlink" title="1 初识Redis"></a>1 初识Redis</h2><p>Redis是一种键值型的NoSQL数据库，这里有两个关键字</p><ul><li>键值型</li><li>NoSQL</li></ul><p>其中<code>键值型</code>是指Redis中存储的数据都是以Key-Value键值对的形式存储，而Value的形式多种多样，可以使字符串、数值甚至Json</p><p>而NoSQL则是相对于传统关系型数据库而言，有很大差异的一种数据库</p><h3 id="1-1-认识NoSQL"><a href="#1-1-认识NoSQL" class="headerlink" title="1.1 认识NoSQL"></a>1.1 认识NoSQL</h3><p><code>NoSql</code>可以翻译做Not Only Sql（不仅仅是SQL），或者是No Sql（非Sql的）数据库。是相对于传统关系型数据库而言，有很大差异的一种特殊的数据库，因此也称之为<code>非关系型数据库</code>。</p><h4 id="结构化与非结构化"><a href="#结构化与非结构化" class="headerlink" title="结构化与非结构化"></a>结构化与非结构化</h4><p>传统关系型数据库是结构化数据，每张表在创建的时候都有严格的约束信息，如字段名、字段数据类型、字段约束等，插入的数据必须遵循这些约束</p><p>而NoSQL则对数据库格式没有约束，可以是键值型（Redis），也可以是文档型（MongoDB），列类型（HBase）甚至是图格式（Neo4j）</p><h4 id="关联与非关联"><a href="#关联与非关联" class="headerlink" title="关联与非关联"></a>关联与非关联</h4><p>传统数据库的表与表之间往往存在关联，例如外键约束<br>而非关系型数据库不存在关联关系，要维护关系要么靠代码中的业务逻辑，要么靠数据之间的耦合</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  id<span class="punctuation">:</span> <span class="number">1</span><span class="punctuation">,</span></span><br><span class="line">  name<span class="punctuation">:</span> <span class="string">&quot;张三&quot;</span><span class="punctuation">,</span></span><br><span class="line">  orders<span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">       id<span class="punctuation">:</span> <span class="number">1</span><span class="punctuation">,</span></span><br><span class="line">       item<span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">   id<span class="punctuation">:</span> <span class="number">10</span><span class="punctuation">,</span> title<span class="punctuation">:</span> <span class="string">&quot;荣耀6&quot;</span><span class="punctuation">,</span> price<span class="punctuation">:</span> <span class="number">4999</span></span><br><span class="line">       <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">       id<span class="punctuation">:</span> <span class="number">2</span><span class="punctuation">,</span></span><br><span class="line">       item<span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">   id<span class="punctuation">:</span> <span class="number">20</span><span class="punctuation">,</span> title<span class="punctuation">:</span> <span class="string">&quot;小米11&quot;</span><span class="punctuation">,</span> price<span class="punctuation">:</span> <span class="number">3999</span></span><br><span class="line">       <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>例如此处要维护张三与两个手机订单的关系，不得不冗余的将这两个商品保存在张三的订单文档中，不够优雅，所以建议使用业务逻辑来维护关联关系。</p><h4 id="查询方式"><a href="#查询方式" class="headerlink" title="查询方式"></a>查询方式</h4><p>传统关系型数据库会基于Sql语句做查询，语法有统一的标准</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT id, age FROM tb_user WHERE id = 1</span><br></pre></td></tr></table></figure><p>而不同的非关系型数据库查询语法差异极大</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Redis:  get user:1</span><br><span class="line">MongoDB: db.user.find(&#123;_id: 1&#125;)</span><br><span class="line">elasticsearch:  GET http://localhost:9200/users/1</span><br></pre></td></tr></table></figure><h4 id="事务"><a href="#事务" class="headerlink" title="事务"></a>事务</h4><p>传统关系型数据库能满足事务的ACID原则(原子性、一致性、独立性及持久性)<br>而非关系型数据库汪汪不支持事务，或者不能要个保证ACID的特性，只能实现计本的一致性</p><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><table><thead><tr><th align="center"></th><th align="center">SQL</th><th align="center">NoSQL</th></tr></thead><tbody><tr><td align="center">数据结构</td><td align="center">结构化(Structured)</td><td align="center">非结构化</td></tr><tr><td align="center">数据关联</td><td align="center">关联的(Relational)</td><td align="center">无关联的</td></tr><tr><td align="center">查询方式</td><td align="center">SQL查询</td><td align="center">非SQL</td></tr><tr><td align="center">事务特性</td><td align="center">ACID</td><td align="center">BASE</td></tr><tr><td align="center">存储方式</td><td align="center">磁盘</td><td align="center">内存</td></tr><tr><td align="center">扩展性</td><td align="center">垂直</td><td align="center">水平</td></tr><tr><td align="center">使用场景</td><td align="center">1）数据结构固定 2)相关业务对数据安全性、一致性要求较高</td><td align="center">1)数据结构不固定 2)对一致性、安全性要求不高 3）对性能要求</td></tr></tbody></table><ul><li><p>存储方式</p><ul><li>关系型数据库基于磁盘进行存储，会有大量的磁盘IO，对性能有一定影响</li><li>非关系型数据库，他们的操作更多的是依赖于内存来操作，内存的读写速度会非常快，性能自然会好一些</li></ul></li><li><p>扩展性</p><ul><li>关系型数据库集群模式一般是主从，主从数据一致，起到数据备份的作用，称为垂直扩展。</li><li>非关系型数据库可以将数据拆分，存储在不同机器上，可以保存海量数据，解决内存大小有限的问题。称为水平扩展。</li><li>关系型数据库因为表之间存在关联关系，如果做水平扩展会给数据查询带来很多麻烦</li></ul></li></ul><h3 id="1-2-认识Redis"><a href="#1-2-认识Redis" class="headerlink" title="1.2 认识Redis"></a>1.2 认识Redis</h3><p>Redis诞生于2009年，全称是Remote Dictionary Server远程词典服务器，是一个基于内存的键值型NoSQL数据库。</p><p>特征：</p><ul><li>键值(Key-Value)型，Value支持多种不同的数据结构，功能丰富</li><li>&#x3D;&#x3D;单线程，每个命令具有原子性&#x3D;&#x3D;</li><li>低延迟，速度快(基于内存&#x3D;&#x3D;主要&#x3D;&#x3D;、IO多路复用、良好的编码（基于C语言）)</li><li>支持数据持久化</li><li>支持主从集群、分片集群</li><li>支持多语言客户端</li></ul><p>作者：Antirez</p><p>Redis官网：<a href="https://redis.io/">https://redis.io/</a></p><h3 id="1-3-安装Redis"><a href="#1-3-安装Redis" class="headerlink" title="1.3 安装Redis"></a>1.3 安装Redis</h3><p>windows 安装参考</p><p><a href="https://cyborg2077.github.io/2022/10/17/ReggieRedis/">https://cyborg2077.github.io/2022/10/17/ReggieRedis/</a></p><p>Redis 官网有关于 macos 系统安装的教程</p><p><a href="https://redis.io/docs/latest/operate/oss_and_stack/install/archive/install-redis/install-redis-on-mac-os/">https://redis.io/docs/latest/operate/oss_and_stack/install/archive/install-redis/install-redis-on-mac-os/</a></p><p><code>redis.conf</code> 配置文件在 <code>/opt/homebrew/etc/redis.conf</code></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/202603181625441.png" alt="image-20260318162514673"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/202603181627765.png" alt="image-20260318162740591"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/202603181628193.png" alt="image-20260318162848020"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/202603181629358.png" alt="image-20260318162959179"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/202603181625788.png" alt="image-20260318162543632"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/202603181630697.png" alt="image-20260318163030521"></p><p>我们修改完 redis.conf 保存</p><p>！！！实际开发需要设置开机自启，需要一个脚本</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260328165808917.png" alt="image-20260328165808747"></p><p>同时我们在 IDEA 里面配置我们 homebrew 安装的 maven</p><h3 id="1-4-Redis桌面客户端"><a href="#1-4-Redis桌面客户端" class="headerlink" title="1.4 Redis桌面客户端"></a>1.4 Redis桌面客户端</h3><p>安装完成Redis，我们就可以操作Redis，实现数据的CRUD了。这需要用到Redis客户端，包括：</p><ul><li>命令行客户端</li><li>图形化桌面客户端</li><li>编程客户端</li></ul><h4 id="Redis命令行客户端"><a href="#Redis命令行客户端" class="headerlink" title="Redis命令行客户端"></a>Redis命令行客户端</h4><p>Redis安装完成后就自带了命令行客户端：redis-cli，使用方式如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">redis-cli [options] [commonds]</span><br></pre></td></tr></table></figure><p>其中常见的options有：</p><ul><li><code>-h 127.0.0.1</code>：指定要连接的redis节点的IP地址，默认是127.0.0.1</li><li><code>-p 6379</code>：指定要连接的redis节点的端口，默认是6379</li><li><code>-a 123321</code>：指定redis的访问密码</li></ul><p>其中的commonds就是Redis的操作命令，例如：</p><ul><li><code>ping</code>：与redis服务端做心跳测试，服务端正常会返回&#96;pong</li></ul><h4 id="图形化桌面客户端"><a href="#图形化桌面客户端" class="headerlink" title="图形化桌面客户端"></a>图形化桌面客户端</h4><p>安装包：<a href="https://github.com/lework/RedisDesktopManager-Windows/releases">https://github.com/lework/RedisDesktopManager-Windows/releases</a></p><p>Redis默认有16个仓库，编号从0至15. 通过配置文件可以设置仓库数量，但是不超过16，并且不能自定义仓库名称。</p><p>如果是基于redis-cli连接Redis服务，可以通过select命令来选择数据库：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">## 选择0号数据库</span></span><br><span class="line"><span class="keyword">select</span> 0</span><br></pre></td></tr></table></figure><h2 id="2-Redis常用命令"><a href="#2-Redis常用命令" class="headerlink" title="2 Redis常用命令"></a>2 Redis常用命令</h2><p>Redis是典型的key-value数据库，key一般是字符串，而value包含很多不同的数据类型<br><a href="https://pic1.imgdb.cn/item/63527eb616f2c2beb12beb85.jpg"><img src="https://cool-tea.oss-cn-qingdao.aliyuncs.com/63527eb616f2c2beb12beb85.jpg" alt="img"></a></p><h3 id="2-1-Redis通用命令"><a href="#2-1-Redis通用命令" class="headerlink" title="2.1 Redis通用命令"></a>2.1 Redis通用命令</h3><p>常用的通用命令有以下几个</p><table><thead><tr><th align="center">命令</th><th align="center">描述</th></tr></thead><tbody><tr><td align="center">KEYS pattern</td><td align="center">查找所有符合给定模式(pattern)的key</td></tr><tr><td align="center">EXISTS key</td><td align="center">检查给定key是否存在</td></tr><tr><td align="center">TYPE key</td><td align="center">返回key所储存的值的类型</td></tr><tr><td align="center">TTL key</td><td align="center">返回给定key的剩余生存时间(TTL, time to live)，以秒为单位</td></tr><tr><td align="center">DEL key</td><td align="center">该命令用于在key存在是删除key</td></tr></tbody></table><ul><li><p><code>KEYS</code>：查看符合模板的所有key</p><ul><li>不建议在生产环境设备上使用，因为Redis是单线程的，执行查询的时候会阻塞其他命令，当数据量很大的时候，使用KEYS进行模糊查询，效率很差</li><li>在查询的时候，<code>*</code> 代表任意数量字符，<code>?</code> 代表单个字符</li></ul></li><li><p><code>DEL</code>：删除一个指定的key</p><ul><li>也可以删除多个key，<code>DEL name age</code>，会将name和age都删掉</li><li>返回值代表删除的key数量</li></ul></li><li><p><code>EXISTS</code>：判断key是否存在</p><ul><li><code>EXISTS name</code>，如果存在返回1，不存在返回0</li><li>也可以判断多个key是否存在，比如 <code>EXISTS age name</code>，返回2代表都存在，返回1代表只有一个存在</li></ul></li><li><p><code>EXPIRE</code>：给一个key设置有效期，有效期到期时该key会被自动删除</p><ul><li><p><code>EXPIRE name 20</code>，给name设置20秒有效期，到期自动删除</p></li><li><p><code>PERSIST age</code>，设置永久有效</p></li></ul></li><li><p><code>TTL</code>：查看一个key的剩余有效期(Time-To-Live)</p><ul><li><code>TTL name</code>，查看name的剩余有效期，如果未设置有效期，则返回-1，代表永久有效期</li><li><code>-2</code> 代表这个键不存在</li></ul></li></ul><h3 id="2-2-String类型"><a href="#2-2-String类型" class="headerlink" title="2.2 String类型"></a>2.2 String类型</h3><p>String类型，也就是字符串类型，是Redis中最简单的存储类型<br>其value是字符串，不过根据字符串的格式不同，又可以分为3类</p><ul><li><code>string</code>：普通字符串</li><li><code>int</code>：整数类型，可以做自增、自减操作</li><li><code>float</code>：浮点类型，可以做自增、自减操作<br>不管是哪种格式，底层都是字节数组形式存储，只不过是编码方式不同，字符串类型的最大空间不能超过512M</li></ul><h4 id="String的常用命令"><a href="#String的常用命令" class="headerlink" title="String的常用命令"></a>String的常用命令</h4><p>String的常用命令有</p><table><thead><tr><th align="center">命令</th><th align="center">描述</th></tr></thead><tbody><tr><td align="center">SET key value</td><td align="center">添加或者修改一个已经存在的String类型的键值对</td></tr><tr><td align="center">GET key</td><td align="center">根据key获取String类型的value</td></tr><tr><td align="center">MSET</td><td align="center">批量添加多个String类型的键值对</td></tr><tr><td align="center">MGET</td><td align="center">根据多个key获取多个String类型的value，返回的是一个数组</td></tr><tr><td align="center">INCR key</td><td align="center">让一个整型的key自增1</td></tr><tr><td align="center">INCRBY key value</td><td align="center">让一个整型的key自增并指定步长值，例如：incrby num 2，让num值自增2</td></tr><tr><td align="center">INCRBYFLOAT key value</td><td align="center">让一个浮点类型的数字自增并指定步长值</td></tr><tr><td align="center">SETNX</td><td align="center">添加一个String类型的键值对，前提是这个key不存在，否则不执行，可以理解为真正的<code>新</code>增，添加成功返回1，失败返回0</td></tr><tr><td align="center">SETEX</td><td align="center">添加一个String类型的键值对，并指定有效期</td></tr></tbody></table><p>规范做法是 <code>SET name 18</code>，其实和 <code>SET name &quot;18&quot;</code> 效果一样，都会当成数字，都能 INCR，但是规范一点，数字通常不加引号，字符串加引号</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/202603182113072.png" alt="image-20260318211328975"></p><p><code>SETNX</code> 和 <code>SETEX</code> 效果可以等价的用 <code>SET</code> 后面的可选参数 <code>NX</code>、<code>EX</code> 来做。</p><h4 id="Key结构"><a href="#Key结构" class="headerlink" title="Key结构"></a>Key结构</h4><ul><li><p>Redis没有类似MySQL中Table的概念，那么我们该如何区分不同类型的Key呢？</p></li><li><p>例如：需要存储用户、商品信息到Redis，有一个用户的id是1，有一个商品的id恰好也是1，如果此时使用id作为key，那么就回冲突，该怎么办？</p></li><li><p>我们可以通过给key添加前缀加以区分，不过这个前缀不是随便加的，有一定的规范</p><ul><li><p>Redis的key允许有多个单词形成层级结构，多个单词之间用<code>:</code>隔开，格式如下</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">项目名:业务名:类型:id</span><br></pre></td></tr></table></figure></li><li><p>这个格式也并非是固定的，可以根据自己的需求来删除&#x2F;添加词条，这样我们就可以把不同数据类型的数据区分开了，从而避免了key的冲突问题</p></li><li><p>例如我们的项目名叫reggie，有user和dish两种不同类型的数据，我们可以这样定义key</p><ul><li>user相关的key：<code>reggie:user:1</code></li><li>dish相关的key：<code>reggie:dish:1</code></li></ul></li></ul></li><li><p>如果value是一个Java对象，例如一个User对象，则可以将对象序列化为JSON字符串后存储</p></li></ul><table><thead><tr><th align="center">KEY</th><th align="center">VALUE</th></tr></thead><tbody><tr><td align="center">reggie:user:1</td><td align="center">{“id”:1, “name”: “Jack”, “age”: 21}</td></tr><tr><td align="center">reggie:dish:1</td><td align="center">{“id”:1, “name”: “鲟鱼火锅”, “price”: 4999}</td></tr></tbody></table><ul><li>并且在Redis的桌面客户端中，也会以相同前缀作为层次结构，让数据看起来层次分明，关系清晰</li></ul><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/202603182121179.png" alt="image-20260318212125125"></p><h3 id="2-3-Hash类型"><a href="#2-3-Hash类型" class="headerlink" title="2.3 Hash类型"></a>2.3 Hash类型</h3><ul><li>Hash类型，也叫散列，其中value是一个无序字典，类似于Java中的HashMap结构</li><li>String结构是将对象序列化为JSON字符串后存储，当我们要修改对象的某个属性值的时候很不方便，并且呢，因为它本身携带着 <code>&#123;&#125; &quot;&quot; ,</code> 这些东西也都是占用内存的。</li></ul><table><thead><tr><th align="center">KEY</th><th align="center">VALUE</th></tr></thead><tbody><tr><td align="center">reggie:user:1</td><td align="center">{“id”:1, “name”: “Jack”, “age”: 21}</td></tr><tr><td align="center">reggie:dish:1</td><td align="center">{“id”:1, “name”: “鲟鱼火锅”, “price”: 4999}</td></tr></tbody></table><ul><li><p>Hash结构可以将对象中的每个字段独立存储，可以针对单个字段做CRUD</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/202603182125172.png" alt="image-20260318212536093"></p></li><li><p>Hash的常用命令有</p></li></ul><table><thead><tr><th align="center">命令</th><th align="center">描述</th></tr></thead><tbody><tr><td align="center">HSET key field value</td><td align="center">添加或者修改hash类型key的field的值</td></tr><tr><td align="center">HGET key field</td><td align="center">获取一个hash类型key的field的值</td></tr><tr><td align="center">HMSET key field1 value1 [field2 value2]</td><td align="center">批量添加多个hash类型key的field的值</td></tr><tr><td align="center">HMGET key field1 [field2]</td><td align="center">批量获取多个hash类型key的field的值</td></tr><tr><td align="center">HGETALL key</td><td align="center">获取一个hash类型的key中的所有的field和value</td></tr><tr><td align="center">HKEYS key</td><td align="center">获取一个hash类型的key中的所有的field</td></tr><tr><td align="center">HVALS key</td><td align="center">获取一个hash类型的key中的所有的value</td></tr><tr><td align="center">HINCRBY key field number</td><td align="center">让一个hash类型key的字段值自增并指定步长</td></tr><tr><td align="center">HSETNX key field value</td><td align="center">添加一个hash类型的key的field值，前提是这个field不存在，否则不执行</td></tr></tbody></table><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/202603182138034.png" alt="image-20260318213857939"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/202603182139526.png" alt="image-20260318213929471"></p><h3 id="2-4-List类型"><a href="#2-4-List类型" class="headerlink" title="2.4 List类型"></a>2.4 List类型</h3><ul><li>Redis中的List类型与Java中的LinkedList类似，可以看做是一个双向链表结构。既可以支持正向检索和也可以支持反向检索。</li><li>特征也与LinkedList类似：<ul><li>有序</li><li>元素可以重复</li><li>插入和删除快</li><li>查询速度一般</li></ul></li><li>常用来存储一个有序数据，例如：朋友圈点赞列表，评论列表等。</li><li>List的常见命令有：</li></ul><table><thead><tr><th align="center">命令</th><th align="center">描述</th></tr></thead><tbody><tr><td align="center">LPUSH key element …</td><td align="center">向列表左侧插入一个或多个元素</td></tr><tr><td align="center">LPOP key [count]</td><td align="center">移除并返回列表左侧的第一个元素，没有则返回nil，也可以指定数量</td></tr><tr><td align="center">RPUSH key element …</td><td align="center">向列表右侧插入一个或多个元素</td></tr><tr><td align="center">RPOP key</td><td align="center">移除并返回列表右侧的第一个元素</td></tr><tr><td align="center">LRANGE key star end</td><td align="center">返回一段角标范围内的所有元素，下标从 <code>0</code> 开始</td></tr><tr><td align="center">BLPOP&#x2F;BRPOP key time</td><td align="center">与LPOP和RPOP类似，只不过在key为空的时候等待指定时间time，而不是直接返回nil</td></tr></tbody></table><p>如果你执行命令</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">LPUSH user 1 2 3; 那么链表是 [3, 2, 1]</span><br><span class="line">RPUSH user 1 2 3; 那么链表是 [1, 2, 3]</span><br></pre></td></tr></table></figure><h3 id="2-5-Set类型"><a href="#2-5-Set类型" class="headerlink" title="2.5 Set类型"></a>2.5 Set类型</h3><ul><li>Redis的Set结构与Java中的HashSet类似，可以看做是一个value为null的HashMap。因为也是一个hash表，因此具备与HashSet类似的特征：<ul><li>无序</li><li>元素不可重复</li><li>查找快</li><li>支持交集、并集、差集等功能</li></ul></li><li>Set的常见命令有：</li></ul><table><thead><tr><th align="center">命令</th><th align="center">描述</th></tr></thead><tbody><tr><td align="center">SADD key member …</td><td align="center">向set中添加一个或多个元素</td></tr><tr><td align="center">SREM key member …</td><td align="center">移除set中的指定元素</td></tr><tr><td align="center">SCARD key</td><td align="center">返回set中元素的个数</td></tr><tr><td align="center">SISMEMBER key member</td><td align="center">判断一个元素是否存在于set中</td></tr><tr><td align="center">SMEMBERS</td><td align="center">获取set中的所有元素</td></tr><tr><td align="center">SINTER key1 key2 …</td><td align="center">求key1与key2的交集</td></tr><tr><td align="center">SUNION key1 key2 …</td><td align="center">求key1与key2的并集</td></tr><tr><td align="center">SDIFF key1 key2 …</td><td align="center">求key1与key2的差集</td></tr></tbody></table><p>练习题：</p><ol><li><p>将下列数据用Redis的Set集合来存储：</p><ul><li><p>张三的好友有：李四、王五、赵六</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; sadd zhangsan lisi wangwu zhaoliu</span><br><span class="line">(integer) 3</span><br></pre></td></tr></table></figure></li><li><p>李四的好友有：王五、麻子、二狗</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; sadd lisi wangwu mazi ergou</span><br><span class="line">(integer) 3</span><br></pre></td></tr></table></figure></li></ul></li><li><p>利用Set的命令实现下列功能：</p><ul><li><p>计算张三的好友有几人</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; scard zhangsan</span><br><span class="line">(integer) 3</span><br></pre></td></tr></table></figure></li><li><p>计算张三和李四有哪些共同好友</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; sinter zhangsan lisi</span><br><span class="line">1) &quot;wangwu&quot;</span><br></pre></td></tr></table></figure></li><li><p>查询哪些人是张三的好友却不是李四的好友</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; sdiff zhangsan lisi</span><br><span class="line">1) &quot;zhaoliu&quot;</span><br><span class="line">2) &quot;lisi&quot;</span><br></pre></td></tr></table></figure></li><li><p>查询张三和李四的好友总共有哪些人</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; sunion zhangsan lisi</span><br><span class="line">1) &quot;wangwu&quot;</span><br><span class="line">2) &quot;zhaoliu&quot;</span><br><span class="line">3) &quot;ergou&quot;</span><br><span class="line">4) &quot;lisi&quot;</span><br><span class="line">5) &quot;mazi&quot;</span><br></pre></td></tr></table></figure></li><li><p>判断李四是否是张三的好友</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; sismember zhangsan lisi</span><br><span class="line">(integer) 1</span><br></pre></td></tr></table></figure></li><li><p>判断张三是否是李四的好友</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; sismember lisi zhangsan</span><br><span class="line">(integer) 0</span><br></pre></td></tr></table></figure></li><li><p>将李四从张三的好友列表中移除</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; srem zhangsan lisi</span><br><span class="line">(integer) 1</span><br></pre></td></tr></table></figure></li></ul></li></ol><h3 id="2-6-SortedSet类型"><a href="#2-6-SortedSet类型" class="headerlink" title="2.6 SortedSet类型"></a>2.6 SortedSet类型</h3><ul><li>Redis的SortedSet是一个可排序的set集合，与Java中的TreeSet有些类似，但底层数据结构却差别很大。SortedSet中的每一个元素都带有一个score属性，可以基于score属性对元素排序，底层的实现是一个跳表（SkipList）加 hash表。</li><li>SortedSet具备下列特性：<ul><li>可排序</li><li>元素不重复</li><li>查询速度快</li></ul></li><li>因为SortedSet的可排序特性，经常被用来实现排行榜这样的功能。</li><li>SortedSet的常见命令有：</li></ul><table><thead><tr><th align="center">命令</th><th align="center">描述</th></tr></thead><tbody><tr><td align="center">ZADD key score member</td><td align="center">添加一个或多个元素到sorted set ，如果已经存在则更新其score值</td></tr><tr><td align="center">ZREM key member</td><td align="center">删除sorted set中的一个指定元素</td></tr><tr><td align="center">ZSCORE key member</td><td align="center">获取sorted set中的指定元素的score值</td></tr><tr><td align="center">ZRANK key member</td><td align="center">获取sorted set 中的指定元素的排名</td></tr><tr><td align="center">ZCARD key</td><td align="center">获取sorted set中的元素个数</td></tr><tr><td align="center">ZCOUNT key min max</td><td align="center">统计score值在给定范围内的所有元素的个数，这个最大最小是分数</td></tr><tr><td align="center">ZINCRBY key increment member</td><td align="center">让sorted set中的指定元素自增，步长为指定的increment值</td></tr><tr><td align="center">ZRANGE key min max</td><td align="center">按照score排序后，获取指定排名范围内的元素，这个最大最小是排名<br /><code>0</code> 代表第一个 <code>-1</code> 代表最后一个</td></tr><tr><td align="center">ZRANGEBYSCORE key min max</td><td align="center">按照score排序后，获取指定score范围内的元素，这个最大最小是分数</td></tr><tr><td align="center">ZDIFF、ZINTER、ZUNION</td><td align="center">求差集、交集、并集</td></tr></tbody></table><p>注意：所有的排名默认都是升序，如果要降序则在命令的Z后面添加REV即可，例如：</p><ul><li><p><code>升序</code>获取sorted set 中的指定元素的排名：ZRANK key member</p></li><li><p><code>降序</code>获取sorted set 中的指定元素的排名：ZREVRANK key memeber</p></li><li><p>练习题：</p><ul><li><p>将班级的下列学生得分存入Redis的SortedSet中：Jack 85, Lucy 89, Rose 82, Tom 95, Jerry 78, Amy 92, Miles 76</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; zadd stu 85 Jack 89 Lucy 82 Rose 95 Tom 78 Jerry 92 Amy 76 Miles</span><br><span class="line">(integer) 7</span><br></pre></td></tr></table></figure></li><li><p>并实现下列功能：</p><ul><li><p>删除Tom同学</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; zrem stu Tom</span><br><span class="line">(integer) 1</span><br></pre></td></tr></table></figure></li><li><p>获取Amy同学的分数</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; zscore stu Amy</span><br><span class="line">&quot;92&quot;</span><br></pre></td></tr></table></figure></li><li><p>获取Rose同学的排名</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; zrank stu Rose</span><br><span class="line">(integer) 2</span><br></pre></td></tr></table></figure></li><li><p>查询80分以下有几个学生</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; zcount stu 0 80</span><br><span class="line">(integer) 2</span><br></pre></td></tr></table></figure></li><li><p>给Amy同学加2分</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; zincrby stu 2 Amy</span><br><span class="line">&quot;94&quot;</span><br></pre></td></tr></table></figure></li><li><p>查出成绩前3名的同学</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; zrange stu 0 2</span><br><span class="line">1) &quot;Miles&quot;</span><br><span class="line">2) &quot;Jerry&quot;</span><br><span class="line">3) &quot;Rose&quot;</span><br></pre></td></tr></table></figure></li><li><p>查出成绩80分以下的所有同学</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; zrangebyscore stu 0 80</span><br><span class="line">1) &quot;Miles&quot;</span><br><span class="line">2) &quot;Jerry&quot;</span><br></pre></td></tr></table></figure></li></ul></li></ul></li></ul><h2 id="3-Redis的Java客户端"><a href="#3-Redis的Java客户端" class="headerlink" title="3 Redis的Java客户端"></a>3 Redis的Java客户端</h2><ul><li>目前主流的Redis的Java客户端有三种<img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/202603191024671.png" alt="image-20260319102444498"><ul><li>Jedis和Lettuce：这两个主要是提供了Redis命令对应的API，方便我们操作Redis，而SpringDataRedis又对这两种做了抽象和封装，因此我们后期会直接以SpringDataRedis来学习。</li><li>Redisson：是在Redis基础上实现了&#x3D;&#x3D;分布式&#x3D;&#x3D;的可伸缩的java数据结构，例如Map、Queue等，而且支持跨进程的同步机制：Lock、Semaphore等待，比较适合用来实现特殊的功能需求。</li></ul></li></ul><h3 id="3-1-Jedis客户端"><a href="#3-1-Jedis客户端" class="headerlink" title="3.1 Jedis客户端"></a>3.1 Jedis客户端</h3><h4 id="快速入门"><a href="#快速入门" class="headerlink" title="快速入门"></a>快速入门</h4><ul><li><p>使用Jedis的步骤</p><ol><li><p>导入Jedis的maven坐标</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!--jedis--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>redis.clients<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>jedis<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>7.1.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--单元测试--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-test<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">scope</span>&gt;</span>test<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure></li><li><p>新建一个单元测试类</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@SpringBootTest</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">JedisDemoApplicationTests</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Jedis jedis;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@BeforeEach</span></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">setUp</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 1.建立连接</span></span><br><span class="line">        jedis = <span class="keyword">new</span> <span class="title class_">Jedis</span>(<span class="string">&quot;127.0.0.1&quot;</span>, <span class="number">6379</span>);</span><br><span class="line">        <span class="comment">// 2.设置密码</span></span><br><span class="line">        jedis.auth(<span class="string">&quot;123456&quot;</span>);</span><br><span class="line">        <span class="comment">// 3.选择库</span></span><br><span class="line">        jedis.select(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">testString</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> jedis.set(<span class="string">&quot;name&quot;</span>, <span class="string">&quot;ergou&quot;</span>);</span><br><span class="line">        System.out.println(result);</span><br><span class="line">        <span class="type">String</span> <span class="variable">name</span> <span class="operator">=</span> jedis.get(<span class="string">&quot;name&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;name = &quot;</span> + name);</span><br><span class="line">    &#125;</span><br><span class="line">   </span><br><span class="line">    <span class="comment">// 关闭连接</span></span><br><span class="line">    <span class="meta">@AfterEach</span></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">tearDown</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">if</span>(jedis != <span class="literal">null</span>) &#123;</span><br><span class="line">            jedis.close();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol></li></ul><p>方法名称就是命令的名称，所以用起来没啥说的</p><p>&#x3D;&#x3D;上述方式中，每一次调用都会连接一次redis，用完之后就释放&#x3D;&#x3D;</p><h4 id="连接池"><a href="#连接池" class="headerlink" title="连接池"></a>连接池</h4><p><code>Jedis</code>本身是<font color=red>线程不安全</font>的，并且频繁的创建和销毁连接会有性能损耗，因此推荐使用Jedis连接池代替Jedis的直连方式。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// jedispool 工具类</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">JedisConnectionFactory</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> JedisPool jedisPool;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        <span class="comment">// 配置连接池</span></span><br><span class="line">        <span class="type">JedisPoolConfig</span> <span class="variable">poolConfig</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">JedisPoolConfig</span>();</span><br><span class="line">        <span class="comment">// 连接池最多能同时存在的连接数，同时可以有8个人连接，第9个人来得等</span></span><br><span class="line">        poolConfig.setMaxTotal(<span class="number">8</span>);</span><br><span class="line">        <span class="comment">// 连接池最多可以保留多少空闲连接</span></span><br><span class="line">        poolConfig.setMaxIdle(<span class="number">8</span>);</span><br><span class="line">        <span class="comment">// 长时间没人用时，最少缩短到几个连接</span></span><br><span class="line">        poolConfig.setMinIdle(<span class="number">0</span>);</span><br><span class="line">        <span class="comment">// 排队时限，如果连接全被占，新来的请求最多等多久(ms)，超时就报错</span></span><br><span class="line">        poolConfig.setMaxWaitMillis(<span class="number">1000</span>); <span class="comment">// 1s</span></span><br><span class="line">        <span class="comment">// 创建连接池对象</span></span><br><span class="line">        jedisPool = <span class="keyword">new</span> <span class="title class_">JedisPool</span>(poolConfig,</span><br><span class="line">                <span class="string">&quot;127.0.0.1&quot;</span>, <span class="number">6379</span>, <span class="number">1000</span>, <span class="string">&quot;123456&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Jedis <span class="title function_">getJedis</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> jedisPool.getResource();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 连接</span></span><br><span class="line"><span class="meta">@BeforeEach</span></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">setUp</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="comment">// 1.建立连接</span></span><br><span class="line">    jedis = JedisConnectionFactory.getJedis();</span><br><span class="line">    <span class="comment">// 2.设置密码</span></span><br><span class="line">    jedis.auth(<span class="string">&quot;123456&quot;</span>);</span><br><span class="line">    <span class="comment">// 3.选择库</span></span><br><span class="line">    jedis.select(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 关闭</span></span><br><span class="line"><span class="meta">@AfterEach</span></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">tearDown</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">if</span>(jedis != <span class="literal">null</span>) &#123;</span><br><span class="line">        jedis.close();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里可以看一下 <code>jedis.close()</code> 方法源码，其实并不是真正的关闭，而是返回连接池</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">close</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">this</span>.dataSource != <span class="literal">null</span>) &#123;</span><br><span class="line">        Pool&lt;Jedis&gt; pool = <span class="built_in">this</span>.dataSource;</span><br><span class="line">        <span class="built_in">this</span>.dataSource = <span class="literal">null</span>;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">this</span>.isBroken()) &#123;</span><br><span class="line">            pool.returnBrokenResource(<span class="built_in">this</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            pool.returnResource(<span class="built_in">this</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.connection.close();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里可以看一下 <code>jedis.close()</code> 方法源码，其实并不是真正的关闭，而是返回连接池</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">close</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">this</span>.dataSource != <span class="literal">null</span>) &#123;</span><br><span class="line">        Pool&lt;Jedis&gt; pool = <span class="built_in">this</span>.dataSource;</span><br><span class="line">        <span class="built_in">this</span>.dataSource = <span class="literal">null</span>;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">this</span>.isBroken()) &#123;</span><br><span class="line">            pool.returnBrokenResource(<span class="built_in">this</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            pool.returnResource(<span class="built_in">this</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.connection.close();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>[!CAUTION]</p><p>为什么说jedis是线程不安全的？！</p></blockquote><h4 id="多线程的使用"><a href="#多线程的使用" class="headerlink" title="多线程的使用"></a>多线程的使用</h4><p>在数据库中如果多个线程复用一个连接会存在数据库事务问题，那么在<code>Redis</code>中我们先来看下在<em>多线程环境下使用一个连接会产生什么问题？</em></p><p>首先启动两个线程，共同操作同一个 <code>Jedis</code> 实例，每一个线程循环 500 次，分别读取  Key 为 a 和 b 的值</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">Jedis</span> <span class="variable">jedis</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Jedis</span>(<span class="string">&quot;127.0.0.1&quot;</span>, <span class="number">6379</span>);</span><br><span class="line"><span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt; &#123;</span><br><span class="line">   <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">500</span>; i++) &#123;</span><br><span class="line">       <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> jedis.get(<span class="string">&quot;a&quot;</span>);</span><br><span class="line">       System.out.println(result);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;).start();</span><br><span class="line"><span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt; &#123;</span><br><span class="line">   <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">500</span>; i++) &#123;</span><br><span class="line">       <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> jedis.get(<span class="string">&quot;b&quot;</span>);</span><br><span class="line">       System.out.println(result);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;).start();</span><br></pre></td></tr></table></figure><p>执行程序多次，可以看到日志中出现了各种奇怪的异常信息，有的未知答复错误，还有的是连接关闭异常等</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">错误1：redis.clients.jedis.exceptions.JedisConnectionException: Unknown reply: 1</span><br><span class="line">错误2：java.io.IOException: Socket Closed</span><br></pre></td></tr></table></figure><p>  那我们先来看下 <code>Jedis</code>常用的（3.x）版本的源码</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Jedis</span> <span class="keyword">extends</span> <span class="title class_">BinaryJedis</span> <span class="keyword">implements</span> <span class="title class_">JedisCommands</span>, MultiKeyCommands,</span><br><span class="line">    AdvancedJedisCommands, ScriptingCommands, BasicCommands, ClusterCommands, SentinelCommands,</span><br><span class="line">    ModuleCommands&#123;&#125;</span><br><span class="line">    </span><br><span class="line"> <span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BinaryJedis</span> <span class="keyword">implements</span> <span class="title class_">BasicCommands</span>, BinaryJedisCommands, MultiKeyBinaryCommands,</span><br><span class="line">    AdvancedBinaryJedisCommands, BinaryScriptingCommands, Closeable &#123;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">protected</span> <span class="keyword">final</span> Client client;</span><br><span class="line">  </span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Client</span> <span class="keyword">extends</span> <span class="title class_">BinaryClient</span> <span class="keyword">implements</span> <span class="title class_">Commands</span> &#123;&#125;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BinaryClient</span> <span class="keyword">extends</span> <span class="title class_">Connection</span> &#123;&#125;</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Connection</span> <span class="keyword">implements</span> <span class="title class_">Closeable</span> &#123;</span><br><span class="line">      <span class="keyword">private</span> Socket socket;</span><br><span class="line">      <span class="keyword">private</span> RedisOutputStream outputStream;</span><br><span class="line">      <span class="keyword">private</span> RedisInputStream inputStream;</span><br><span class="line">  &#125;    </span><br></pre></td></tr></table></figure><p>  首先<code>Jedis</code> 继承了 <code>BinaryJedis</code>，<code>BinaryJedis</code> 中保存了单个 <code>Client</code> 的实例，<code>Client</code>最终继承了 <code>Connection</code>，<code>Connection</code> 中保存了单个 <code>Socket</code> 的实例以及对应的两个读写流一个 <code>RedisOutputStream</code> 一个是 <code>RedisInputStream</code>。</p><p><img src="https://cool-tea.oss-cn-qingdao.aliyuncs.com/image-20251213191024727.png" alt="image-20251213191024727"></p><p>  <code>BinaryClient</code> 封装了各种 <code>Redis</code> 命令，其最终会调用的是 <code>sendCommand</code> 方法，发现其发送命令时是直接操作 <code>RedisOutputStream</code> 写入字节。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">sendCommand</span><span class="params">(<span class="keyword">final</span> RedisOutputStream os, <span class="keyword">final</span> <span class="type">byte</span>[] command,</span></span><br><span class="line"><span class="params">      <span class="keyword">final</span> <span class="type">byte</span>[]... args)</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      os.write(ASTERISK_BYTE);</span><br><span class="line">      os.writeIntCrLf(args.length + <span class="number">1</span>);</span><br><span class="line">      os.write(DOLLAR_BYTE);</span><br><span class="line">      os.writeIntCrLf(command.length);</span><br><span class="line">      os.write(command);</span><br><span class="line">      os.writeCrLf();</span><br><span class="line"></span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">final</span> <span class="type">byte</span>[] arg : args) &#123;</span><br><span class="line">        os.write(DOLLAR_BYTE);</span><br><span class="line">        os.writeIntCrLf(arg.length);</span><br><span class="line">        os.write(arg);</span><br><span class="line">        os.writeCrLf();</span><br><span class="line">      &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">JedisConnectionException</span>(e);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure><p>  所以在多线程环境下使用 <code>Jedis</code> ，其实就是在复用<code>RedisOutputStream</code>。如果多个线程在执行操作，那么无法保证整条命令是原子写入 <code>Socket</code>。比如，写操作互相干扰，多条命令相互穿插的话，必然不是合法的 <code>Redis</code> 命令也就导致等等各种问题。</p><p>  这也说明了<code>Jedis</code>是非线程安全。但是可以通过<code>JedisPool</code>连接池去管理实例，在多线程情况下让每个线程有自己独立的<code>Jedis</code>实例，可变为线程安全。</p><ul><li><p>新建一个<code>com.blog.util</code>，用于存放我们编写的工具类</p></li><li><p>但后面我们使用<code>SpringDataRedis</code>的时候，可以直接在<code>yml</code>配置文件里配置这些内容</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">JedisConnectionFactory</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> JedisPool jedisPool;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        <span class="comment">// 配置连接池</span></span><br><span class="line">        <span class="type">JedisPoolConfig</span> <span class="variable">poolConfig</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">JedisPoolConfig</span>();</span><br><span class="line">        poolConfig.setMaxTotal(<span class="number">8</span>);  <span class="comment">// 表示最多创建8个连接</span></span><br><span class="line">        poolConfig.setMaxIdle(<span class="number">8</span>);   <span class="comment">// 表示最多存在8个空闲连接</span></span><br><span class="line">        poolConfig.setMinIdle(<span class="number">0</span>);   <span class="comment">// 表示最少存在0个空闲连接</span></span><br><span class="line">        poolConfig.setMaxWaitMillis(<span class="number">1000</span>);  <span class="comment">// 没有空闲连接时进行等待，最多等待1000单位时间，若还没有，就报错</span></span><br><span class="line">        <span class="comment">// 创建连接池对象，参数：连接池配置、服务端ip、服务端端口、超时时间、密码</span></span><br><span class="line">        jedisPool = <span class="keyword">new</span> <span class="title class_">JedisPool</span>(poolConfig, <span class="string">&quot;101.42.225.160&quot;</span>, <span class="number">6379</span>, <span class="number">1000</span>, <span class="string">&quot;root&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 获取jedis对象</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Jedis <span class="title function_">getJedis</span><span class="params">()</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> jedisPool.getResource();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>之后我们的测试类就可以修改为如下</p><p>&#x3D;&#x3D;这里的代码和课程中展示的有出入！！！&#x3D;&#x3D;</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@SpringBootTest</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">RedisTestApplicationTests</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="type">Jedis</span> <span class="variable">jedis</span> <span class="operator">=</span> JedisConnectionFactory.getJedis();</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">testString</span><span class="params">()</span>&#123;</span><br><span class="line">        jedis.set(<span class="string">&quot;name&quot;</span>,<span class="string">&quot;Kyle&quot;</span>);</span><br><span class="line">        <span class="type">String</span> <span class="variable">name</span> <span class="operator">=</span> jedis.get(<span class="string">&quot;name&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;name = &quot;</span> + name);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">testHash</span><span class="params">()</span>&#123;</span><br><span class="line">        jedis.hset(<span class="string">&quot;reggie:user:1&quot;</span>,<span class="string">&quot;name&quot;</span>,<span class="string">&quot;Jack&quot;</span>);</span><br><span class="line">        jedis.hset(<span class="string">&quot;reggie:user:2&quot;</span>,<span class="string">&quot;name&quot;</span>,<span class="string">&quot;Rose&quot;</span>);</span><br><span class="line">        jedis.hset(<span class="string">&quot;reggie:user:3&quot;</span>,<span class="string">&quot;name&quot;</span>,<span class="string">&quot;Kyle&quot;</span>);</span><br><span class="line">        jedis.hset(<span class="string">&quot;reggie:user:1&quot;</span>,<span class="string">&quot;age&quot;</span>,<span class="string">&quot;21&quot;</span>);</span><br><span class="line">        jedis.hset(<span class="string">&quot;reggie:user:2&quot;</span>,<span class="string">&quot;age&quot;</span>,<span class="string">&quot;18&quot;</span>);</span><br><span class="line">        jedis.hset(<span class="string">&quot;reggie:user:3&quot;</span>,<span class="string">&quot;age&quot;</span>,<span class="string">&quot;18&quot;</span>);</span><br><span class="line">        Map&lt;String, String&gt; map = jedis.hgetAll(<span class="string">&quot;reggie:user:1&quot;</span>);</span><br><span class="line">        System.out.println(map);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@AfterEach</span></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">tearDown</span><span class="params">()</span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (jedis != <span class="literal">null</span>)&#123;</span><br><span class="line">            jedis.close();  <span class="comment">// 由于根据连接池创建，所以这个时候不是销毁，还是归还连接池，可看源码</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="3-2-SpringDataRedis客户端"><a href="#3-2-SpringDataRedis客户端" class="headerlink" title="3.2 SpringDataRedis客户端"></a>3.2 SpringDataRedis客户端</h3><ul><li>SpringData是Spring中数据操作的模块，包含对各种数据库的集成，其中对Redis的集成模块就叫做SpringDataRedis</li><li>官网地址：<a href="https://spring.io/projects/spring-data-redis">https://spring.io/projects/spring-data-redis</a><ul><li>提供了对不同Redis客户端的整合（Lettuce和Jedis）&#x3D;&#x3D;默认整合Lettuce&#x3D;&#x3D;</li><li>提供了RedisTemplate统一API来操作Redis</li><li>支持Redis的发布订阅模型</li><li>支持Redis哨兵和Redis集群</li><li>支持基于Lettuce的响应式编程</li><li>支持基于JDK、JSON、字符串、Spring对象的数据序列化及反序列化</li><li>支持基于Redis的JDKCollection实现</li></ul></li><li>SpringDataRedis中提供了RedisTemplate工具类，其中封装了各种对Redis的操作。并且将不同数据类型的操作API封装到了不同的类型中：</li></ul><table><thead><tr><th align="center">API</th><th align="center">返回值类型</th><th align="center">说明</th></tr></thead><tbody><tr><td align="center">redisTemplate.opsForValue()</td><td align="center">ValueOperations</td><td align="center">操作String类型数据</td></tr><tr><td align="center">redisTemplate.opsForHash()</td><td align="center">HashOperations</td><td align="center">操作Hash类型数据</td></tr><tr><td align="center">redisTemplate.opsForList()</td><td align="center">ListOperations</td><td align="center">操作List类型数据</td></tr><tr><td align="center">redisTemplate.opsForSet()</td><td align="center">SetOperations</td><td align="center">操作Set类型数据</td></tr><tr><td align="center">redisTemplate.opsForzSet()</td><td align="center">ZSetOperations</td><td align="center">操作SortedSet类型数据</td></tr><tr><td align="center">redisTemplate</td><td align="center"></td><td align="center">通用的命令</td></tr></tbody></table><h4 id="快速入门-1"><a href="#快速入门-1" class="headerlink" title="快速入门"></a>快速入门</h4><p>SpringBoot已经提供了对SpringDataRedis的支持，使用起来非常简单</p><ol><li><p>导入依赖，</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!--redis依赖--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-data-redis<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--连接池依赖 common-pool--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.commons<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>commons-pool2<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--Jackson依赖 springmvc里面会有，如果加了这个依赖，jackson就不用加了--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.fasterxml.jackson.core<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>jackson-databind<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure></li><li><p>配置Redis</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">data:</span> <span class="comment"># 需要有 data</span></span><br><span class="line">    <span class="attr">redis:</span></span><br><span class="line">      <span class="attr">host:</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">6379</span></span><br><span class="line">      <span class="attr">password:</span> <span class="number">123456</span></span><br><span class="line">      <span class="attr">lettuce:</span></span><br><span class="line">        <span class="attr">pool:</span></span><br><span class="line">          <span class="attr">max-active:</span> <span class="number">8</span></span><br><span class="line">          <span class="attr">min-idle:</span> <span class="number">0</span></span><br><span class="line">          <span class="attr">max-idle:</span> <span class="number">8</span></span><br><span class="line">          <span class="attr">max-wait:</span> <span class="string">100ms</span></span><br></pre></td></tr></table></figure></li><li><p>注入RedisTemplate<br>因为有了SpringBoot的自动装配，我们可以拿来就用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">private</span> RedisTemplate redisTemplate;</span><br></pre></td></tr></table></figure></li><li><p>编写测试方法</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">stringTest</span><span class="params">()</span>&#123;</span><br><span class="line">    redisTemplate.opsForValue().set(<span class="string">&quot;name&quot;</span>,<span class="string">&quot;二狗&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">username</span> <span class="operator">=</span> (String) redisTemplate.opsForValue().get(<span class="string">&quot;name&quot;</span>);</span><br><span class="line">    System.out.println(username);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><p>输出结果如下</p><figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">二狗</span><br></pre></td></tr></table></figure><p>但是从 Redis 客户端来看存入的是乱码</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260328171321965.png" alt="image-20260328171321777"></p><h4 id="自定义序列化"><a href="#自定义序列化" class="headerlink" title="自定义序列化"></a>自定义序列化</h4><ul><li><p>RedisTemplate 可以接收任意 Object 作为值写入 Redis (&#x3D;&#x3D;无论是key还是value都是Object对象，所以在存储时会序列化&#x3D;&#x3D;)，只不过写入前会把 Object 序列化为字节形式，默认是采用JDK序列化，得到的结果如上面那个图，一堆码。</p></li><li><p>缺点：</p><ul><li>可读性差</li><li>内存占用较大（&#x3D;&#x3D;实际存的很短，想要存的其实没那么长，因为多存了一些和实际内容没关的东西&#x3D;&#x3D;）</li></ul></li></ul><p>我们尝试从客户端这里取值看看</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260328171736644.png" alt="image-20260328171736468"></p><p>第一次 <code>get name</code> 并没有取到，发现里面存入的 <code>name</code> 并不是我们从 Java 里面存入的 <code>name</code>，Java 里面存入的 <code>name</code> 变成了很长的一个东西，最后有个 <code>name</code>。</p><blockquote><p>[!Caution]</p><p>:question: 为什么出现这种现象？</p><p>因为没有手动配置序列化器的时候，<code>RedisTemplate</code> 默认使用 <code>JdkSerializationRedisSerializer</code>，这种序列化器在转对象的时候，不仅会包含数据，还会包含Java类的元数据、版本号、签名等信息。所以图片 key 前面那个长串就是 JDK 序列化后的对象头标识。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// RedisTemplate 类中</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">afterPropertiesSet</span><span class="params">()</span> &#123;</span><br><span class="line"></span><br><span class="line"><span class="built_in">super</span>.afterPropertiesSet();</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (defaultSerializer == <span class="literal">null</span>) &#123; <span class="comment">// 默认的序列化器</span></span><br><span class="line"></span><br><span class="line"> defaultSerializer = <span class="keyword">new</span> <span class="title class_">JdkSerializationRedisSerializer</span>(</span><br><span class="line">     classLoader != <span class="literal">null</span> ? classLoader : <span class="built_in">this</span>.getClass().getClassLoader());</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我们通过调试的方式看看，进入 <code>set</code> 方法</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260328172818427.png" alt="image-20260328172818241"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260328172836846.png" alt="image-20260328172836746"></p><p>它会对 <code>key</code> 和 <code>value</code> 值进行封装，进入看看</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260328172954201.png" alt="image-20260328172954019"></p><p>这里有序列化的方法，进去看看，这里我们就进入了 <code>Jdk</code> 序列化工具</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260328173103467.png" alt="image-20260328173103376"></p><p>一路走进去</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260328173155063.png" alt="image-20260328173154978"></p><p>最后可以看到底层是用的 ObjectOutputStream 来写对象，把对象转为字节，在这个里面执行完就出现了乱码。那JDK序列化为什么这么做呢？序列化和反序列化就是为了Java对象传出去，还有接收的时候能知道这是哪个Java对象。所以不是说它不好，而是不适合在这里用。</p><p>比如上面那个序列化后的字符串 <code>name</code>，Java序列化协议写入了一系列控制字符。</p><p><strong>魔数 (Magic Number):</strong> <code>\xAC\xED</code>。这是 Java 序列化流的固定开头，告诉 JVM，后面跟着的是一个 Java 对象。</p><p><strong>版本号:</strong> <code>\x00\x05</code>。</p><p><strong>类型标记:</strong> <code>t</code> (在十六进制中对应 <code>\x74</code>)，代表后面跟着的是一个 <code>String</code> 类型。</p><p><strong>长度描述:</strong> 紧跟着的字节会记录这个字符串有多长。</p><p><strong>内容:</strong> 最后才是真正的 <code>name</code>。</p><p>但是 redis 读取到这个的时候就当成字节数组了，get 不到这个信息。</p><p>下面这个就是 <code>value</code> 的。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260328174839919.png" alt="image-20260328174839818"></p></blockquote><p>RedisTemplate 源码里面，对于 key、value、hashKey、hashValue 里面序列化器定义变量如下</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@SuppressWarnings(&quot;rawtypes&quot;)</span> <span class="keyword">private</span> <span class="meta">@Nullable</span> <span class="type">RedisSerializer</span> <span class="variable">keySerializer</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"><span class="meta">@SuppressWarnings(&quot;rawtypes&quot;)</span> <span class="keyword">private</span> <span class="meta">@Nullable</span> <span class="type">RedisSerializer</span> <span class="variable">valueSerializer</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"><span class="meta">@SuppressWarnings(&quot;rawtypes&quot;)</span> <span class="keyword">private</span> <span class="meta">@Nullable</span> <span class="type">RedisSerializer</span> <span class="variable">hashKeySerializer</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"><span class="meta">@SuppressWarnings(&quot;rawtypes&quot;)</span> <span class="keyword">private</span> <span class="meta">@Nullable</span> <span class="type">RedisSerializer</span> <span class="variable">hashValueSerializer</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br></pre></td></tr></table></figure><p>不指定默认就是 jdk 序列化，所以我们需要指定。</p><ul><li><p>我们可以自定义RedisTemplate的序列化方式，代码如下<br>在<code>com.lh.config</code>包下编写对应的配置类</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RedisConfig</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span> <span class="comment">// 对默认 RedisTemplate 的覆盖</span></span><br><span class="line">    <span class="keyword">public</span> RedisTemplate&lt;String, Object&gt; <span class="title function_">redisTemplate</span><span class="params">(RedisConnectionFactory redisConnectionFactory)</span> &#123;</span><br><span class="line">        <span class="comment">// 创建RedisTemplate对象</span></span><br><span class="line">        RedisTemplate&lt;String, Object&gt; template = <span class="keyword">new</span> <span class="title class_">RedisTemplate</span>&lt;&gt;();</span><br><span class="line">        <span class="comment">// 设置连接工厂</span></span><br><span class="line">        template.setConnectionFactory(redisConnectionFactory);</span><br><span class="line">        <span class="comment">// 创建JSON序列化工具</span></span><br><span class="line">        <span class="type">GenericJackson2JsonRedisSerializer</span> <span class="variable">genericJackson2JsonRedisSerializer</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">GenericJackson2JsonRedisSerializer</span>();</span><br><span class="line">        <span class="comment">// 设置Key的序列化</span></span><br><span class="line">        template.setKeySerializer(RedisSerializer.string());    <span class="comment">// 其实就是StringRedisSerializer.UTF_8</span></span><br><span class="line">        template.setHashKeySerializer(RedisSerializer.string());</span><br><span class="line">        <span class="comment">// 设置value的序列化</span></span><br><span class="line">        template.setValueSerializer(genericJackson2JsonRedisSerializer);</span><br><span class="line">        template.setHashValueSerializer(genericJackson2JsonRedisSerializer);</span><br><span class="line">        <span class="comment">// 返回</span></span><br><span class="line">        <span class="keyword">return</span> template;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>我们编写一个User类，并尝试将其创建的对象存入Redis，看看是什么效果</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@AllArgsConstructor</span></span><br><span class="line"><span class="meta">@NoArgsConstructor</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> Integer age;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>测试方法</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">stringTest</span><span class="params">()</span>&#123;</span><br><span class="line">    redisTemplate.opsForValue().set(<span class="string">&quot;user&quot;</span>, <span class="keyword">new</span> <span class="title class_">User</span>(<span class="string">&quot;二狗&quot;</span>, <span class="number">18</span>));</span><br><span class="line">    <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> (User) redisTemplate.opsForValue().get(<span class="string">&quot;user&quot;</span>); <span class="comment">// User(name=二狗, age=18)</span></span><br><span class="line">    System.out.println(user);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>这里采用了JSON序列化来代替默认的JDK序列化方式。最终结果如下：</p></li><li><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260328192649683.png" alt="image-20260328192649493"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260328192612267.png" alt="image-20260328192612062"></p></li><li><p>整体可读性有了很大提升，并且&#x3D;&#x3D;能将Java对象自动的序列化为JSON字符串，并且查询时能自动把 JSON 反序列化为Java对象&#x3D;&#x3D;。不过，其中记录了序列化时对应的class名称，目的是为了查询时实现自动反序列化。这会带来&#x3D;&#x3D;额外的内存开销&#x3D;&#x3D;。所以肯定会有更好的方法</p></li></ul><h4 id="StringRedisTemplate"><a href="#StringRedisTemplate" class="headerlink" title="StringRedisTemplate"></a>StringRedisTemplate</h4><ul><li><p>为了节省内存空间，我们并不不使用 JSON 序列化器来处理value，而是统一使用String序列化器，要求只能存储String类型的key和value。当需要存储Java对象时，手动完成对象的序列化和反序列化。</p></li><li><p>因为存入和读取时的序列化及反序列化都是我们自己实现的，SpringDataRedis就不会将class信息写入Redis了</p></li><li><p>这种用法比较普遍，因此SpringDataRedis就提供了RedisTemplate的子类：StringRedisTemplate，它的key和value的序列化方式默认就是String方式。源码如下</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">StringRedisTemplate</span> <span class="keyword">extends</span> <span class="title class_">RedisTemplate</span>&lt;String, String&gt; &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">StringRedisTemplate</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.setKeySerializer(RedisSerializer.string());</span><br><span class="line">        <span class="built_in">this</span>.setValueSerializer(RedisSerializer.string());</span><br><span class="line">        <span class="built_in">this</span>.setHashKeySerializer(RedisSerializer.string());</span><br><span class="line">        <span class="built_in">this</span>.setHashValueSerializer(RedisSerializer.string());</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></li><li><p>省去了我们自定义RedisTemplate的序列化方式的步骤（可以将之前配置的RedisConfig删除掉），而是直接使用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">private</span> StringRedisTemplate stringRedisTemplate;</span><br><span class="line"><span class="comment">// JOSN工具(用别的也可以，比如fastJson)</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">ObjectMapper</span> <span class="variable">mapper</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>(); <span class="comment">// 这个是 springmvc 默认的工具</span></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">stringTest</span><span class="params">()</span> <span class="keyword">throws</span> JsonProcessingException &#123;</span><br><span class="line">    <span class="comment">//创建对象</span></span><br><span class="line">    <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>(<span class="string">&quot;张三&quot;</span>, <span class="number">18</span>);</span><br><span class="line">    <span class="comment">//手动序列化</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">json</span> <span class="operator">=</span> mapper.writeValueAsString(user);</span><br><span class="line">    <span class="comment">//写入数据</span></span><br><span class="line">    stringRedisTemplate.opsForValue().set(<span class="string">&quot;userdata&quot;</span>, json);</span><br><span class="line">    <span class="comment">//获取数据</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">userdata</span> <span class="operator">=</span> stringRedisTemplate.opsForValue().get(<span class="string">&quot;userdata&quot;</span>);</span><br><span class="line">    <span class="comment">//手动反序列化</span></span><br><span class="line">    <span class="type">User</span> <span class="variable">readValue</span> <span class="operator">=</span> mapper.readValue(userdata, User.class); <span class="comment">// 需要手动指定类型！相当于原来自动转为对象，现在需要你指定对象类型了。</span></span><br><span class="line">    System.out.println(readValue);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>存入Redis中是这样的</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;张三&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;age&quot;</span><span class="punctuation">:</span> <span class="number">18</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure></li></ul><p><img src="https://cool-tea.oss-cn-qingdao.aliyuncs.com/image-20251213201218679.png" alt="image-20251213201218679"></p><p>对于 Hash 类型也类似</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span> <span class="title function_">testHash</span><span class="params">()</span> &#123;</span><br><span class="line">    stringRedisTemplate.opsForHash().put(<span class="string">&quot;user:100&quot;</span>, <span class="string">&quot;name&quot;</span>, <span class="string">&quot;二狗&quot;</span>);</span><br><span class="line">    stringRedisTemplate.opsForHash().put(<span class="string">&quot;user:100&quot;</span>, <span class="string">&quot;age&quot;</span>, <span class="string">&quot;18&quot;</span>);</span><br><span class="line"></span><br><span class="line">    Map&lt;Object, Object&gt; entries = stringRedisTemplate.opsForHash().entries(<span class="string">&quot;user:100&quot;</span>);</span><br><span class="line">    System.out.println(<span class="string">&quot;entries = &quot;</span> + entries); <span class="comment">// entries = &#123;name=二狗, age=18&#125;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260328194034451.png" alt="image-20260328194034345"></p><h1 id="实战篇"><a href="#实战篇" class="headerlink" title="实战篇"></a>实战篇</h1><h2 id="1-项目导入"><a href="#1-项目导入" class="headerlink" title="1 项目导入"></a>1 项目导入</h2><p>项目架构如下</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260328195738719.png" alt="image-20260328195738621"></p><p>项目是和 Java8 配对的，下面是 Java17+MySQL8的配置</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-data-redis<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.commons<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>commons-pool2<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.mysql<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mysql-connector-j<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.projectlombok<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>lombok<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">optional</span>&gt;</span>true<span class="tag">&lt;/<span class="name">optional</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-test<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">scope</span>&gt;</span>test<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.baomidou<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mybatis-plus-spring-boot3-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.5.16<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 分页插件被单独弄出来了 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.baomidou<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mybatis-plus-jsqlparser<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.5.16<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--hutool--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>cn.hutool<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>hutool-all<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>5.8.44<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><p>需要注意的点</p><ol><li>新版本的 <code>mybatis-plus</code> 把分页插件单独拿出来了</li><li>SpringBoot3 版本的 <code>springmvc</code> 的 <code>javax</code> 包改成了 <code>jakarta</code></li><li><code>mybatis-plus-boot-starter</code> 要改为 <code>mybatis-plus-spring-boot3-starter</code></li></ol><h2 id="2-短信登录"><a href="#2-短信登录" class="headerlink" title="2 短信登录"></a>2 短信登录</h2><h3 id="2-1-基于-Session-实现流程"><a href="#2-1-基于-Session-实现流程" class="headerlink" title="2.1 基于 Session 实现流程"></a>2.1 基于 Session 实现流程</h3><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260328210012306.png" alt="image-20260328210012247"></p><ol><li>发送验证码<br>用户在提交手机号后，会校验手机号是否合法，如果不合法，则要求用户重新输入手机号<br>如果手机号合法，后台此时生成对应的验证码，同时将验证码进行保存，然后再通过短信的方式将验证码发送给用户</li><li>短信验证码登录、注册<br>用户将验证码和手机号进行输入，后台从session中拿到当前验证码，然后和用户输入的验证码进行校验，如果不一致，则无法通过校验，如果一致，则后台根据手机号查询用户，如果用户不存在，则为用户创建账号信息，保存到数据库，无论是否存在，都会将用户信息保存到session中，方便后续获得当前登录信息</li><li>校验登录状态<br>用户在请求的时候，会从cookie中携带JsessionId到后台，后台通过JsessionId从session中拿到用户信息，如果没有session信息，则进行拦截，如果有session信息，则将用户信息保存到threadLocal中，并放行</li></ol><blockquote><p>[!Caution]</p><p>当用户登录之后进行一系列操作产生数据的时候，一般都需要用户的信息，比如用户ID，用户手机号等来代表谁在操作数据，比如下单、评论什么的，都需要这个数据信息。</p><p>:question: 那么之前都怎么做过呢？</p><p>之前做过的项目里面尝试过下面这种方法</p><ul><li>在JSP里面，是把这个信息存到会话里面，HttpSession（从用户第一次访问网站开始，到用户退出登录、浏览器长时间不访问、或者 Session 失效为止，这一段期间内的交互过程）。为什么有这个东西呢？因为同一个人发起的两次请求，比如 <code>/login</code>，<code>/user/me</code>，服务器是根本不知道两次请求是同一个人发过来的，所以Web开发里就需要会话跟踪，当用户第一次请求网站时，就会新建一个 HttpSession，并且为这个会话分配一个 JsessionId，这个ID也会返回给浏览器，浏览器把这个存起来，往后每次请求都发一个。用户的数据都是保存在服务端的，占据内存</li><li>小程序还有前端可以存到本地，比如 <code>localStorage</code>、<code>sessionStorage</code>、<code>Cookie</code>，这种方法简单。</li></ul><p>但是这些都有缺点</p><ul><li>HttpSession，如果项目采用集群方式，有多个服务器，但是 Session 只存在一台服务器，用户换台服务器就得重新登录，所以分布式场景下需要 Spring Session+Redis 来共享会话。并且 Session 数据保存在服务器，会占用内存，用户越多，会话越多，服务器压力也会更大。</li><li>前端本地存储安全性差，容易被窃取和受到XSS等攻击影响。并且后端时间无法主动控制，比如管理员封禁了用户，但前端数据不会因此变化，用户浏览器里面原来的信息还在，就依然能发起带身份的请求。这个时候后端就必须每次请求都做额外检验，检查用户状态、token 是否过期等。</li></ul></blockquote><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329083938534.png" alt="image-20260329083938401"></p><h4 id="实现发送短信验证码功能"><a href="#实现发送短信验证码功能" class="headerlink" title="实现发送短信验证码功能"></a>实现发送短信验证码功能</h4><blockquote><p>发送验证码</p></blockquote><p>controller 层方法定义如下</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 发送手机验证码</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@PostMapping(&quot;code&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Result <span class="title function_">sendCode</span><span class="params">(<span class="meta">@RequestParam(&quot;phone&quot;)</span> String phone, HttpSession session)</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> userService.sendCode(phone, session);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>service 层实现</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Slf4j</span></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserServiceImpl</span> <span class="keyword">extends</span> <span class="title class_">ServiceImpl</span>&lt;UserMapper, User&gt; <span class="keyword">implements</span> <span class="title class_">IUserService</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Result <span class="title function_">sendCode</span><span class="params">(String phone, HttpSession session)</span> &#123;</span><br><span class="line">        <span class="comment">// 1 校验手机号</span></span><br><span class="line">        <span class="keyword">if</span> (RegexUtils.isPhoneInvalid(phone)) &#123;</span><br><span class="line">            <span class="comment">// 返回校验信息</span></span><br><span class="line">            <span class="keyword">return</span> Result.fail(<span class="string">&quot;手机号格式错误&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 2 生成验证码</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">code</span> <span class="operator">=</span> RandomUtil.randomNumbers(<span class="number">6</span>);</span><br><span class="line">        <span class="comment">// 3 保存到 session</span></span><br><span class="line">        session.setAttribute(<span class="string">&quot;code&quot;</span>, code);</span><br><span class="line">        <span class="comment">// 4 发送验证码</span></span><br><span class="line">        log.debug(<span class="string">&quot;发送验证码成功，验证码: &#123;&#125;&quot;</span>, code);</span><br><span class="line">        <span class="keyword">return</span> Result.ok();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><p>校验手机号规则如下</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 手机号正则</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">PHONE_REGEX</span> <span class="operator">=</span> <span class="string">&quot;^1([38][0-9]|4[579]|5[0-3,5-9]|6[6]|7[0135678]|9[89])\\d&#123;8&#125;$&quot;</span>;</span><br></pre></td></tr></table></figure><p>校验工具类</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 是否是无效手机格式</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> phone 要校验的手机号</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> true:符合，false：不符合</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="type">boolean</span> <span class="title function_">isPhoneInvalid</span><span class="params">(String phone)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> mismatch(phone, RegexPatterns.PHONE_REGEX);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>hutool 工具包</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">RandomUtil.randomNumbers(<span class="number">6</span>); <span class="comment">// 生成一个包含 6 位数字的字符串</span></span><br></pre></td></tr></table></figure></li></ul><blockquote><p>登录如下</p></blockquote><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> Result <span class="title function_">login</span><span class="params">(LoginFormDTO loginForm, HttpSession session)</span> &#123;</span><br><span class="line">    <span class="comment">// 1. 校验手机号 (防止发送验证码和登录的时候手机号不是同一个)</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">phone</span> <span class="operator">=</span> loginForm.getPhone();</span><br><span class="line">    <span class="keyword">if</span> (RegexUtils.isPhoneInvalid(phone)) &#123;</span><br><span class="line">        <span class="comment">// 返回校验信息</span></span><br><span class="line">        <span class="keyword">return</span> Result.fail(<span class="string">&quot;手机号格式错误&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 2 校验验证码</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">cacheCode</span> <span class="operator">=</span> (String) session.getAttribute(<span class="string">&quot;code&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">code</span> <span class="operator">=</span> loginForm.getCode();</span><br><span class="line">    <span class="keyword">if</span> (cacheCode == <span class="literal">null</span> || !cacheCode.equals(code)) &#123; <span class="comment">// 可能为空，直接点击的登录</span></span><br><span class="line">        <span class="keyword">return</span> Result.fail(<span class="string">&quot;验证码错误&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 3 查询用户</span></span><br><span class="line">    <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> query().eq(<span class="string">&quot;phone&quot;</span>, phone).one();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 4 判断用户是否存在</span></span><br><span class="line">    <span class="keyword">if</span> (user == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="comment">// 5 创建用户</span></span><br><span class="line">        user = createUserWithPhone(phone);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 6 保存用户到 session</span></span><br><span class="line">    session.setAttribute(<span class="string">&quot;user&quot;</span>, user);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> Result.ok();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> User <span class="title function_">createUserWithPhone</span><span class="params">(String phone)</span> &#123;</span><br><span class="line">    <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">    user.setPhone(phone);</span><br><span class="line">    <span class="comment">// 随机10位字符，包括大写、小写字符和数字</span></span><br><span class="line">    user.setNickName(USER_NICK_NAME_PREFIX + RandomUtil.randomString(<span class="number">10</span>));</span><br><span class="line">    <span class="comment">// 保存用户</span></span><br><span class="line">    save(user);</span><br><span class="line">    <span class="keyword">return</span> user;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="实现登录拦截功能"><a href="#实现登录拦截功能" class="headerlink" title="实现登录拦截功能"></a>实现登录拦截功能</h4><p>定义登录拦截器</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LoginInterceptor</span> <span class="keyword">implements</span> <span class="title class_">HandlerInterceptor</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">preHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="comment">// 获取 session</span></span><br><span class="line">        <span class="type">HttpSession</span> <span class="variable">session</span> <span class="operator">=</span> request.getSession();</span><br><span class="line">        <span class="comment">// 获取 session 中的用户</span></span><br><span class="line">        <span class="type">Object</span> <span class="variable">user</span> <span class="operator">=</span> session.getAttribute(<span class="string">&quot;user&quot;</span>);</span><br><span class="line">        <span class="comment">// 判断用户是否存在</span></span><br><span class="line">        <span class="keyword">if</span> (user == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 不存在，拦截，返回状态吗</span></span><br><span class="line">            response.setStatus(<span class="number">401</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 存在，保存用户信息到 ThreadLocal</span></span><br><span class="line">        UserHolder.saveUser((User) user);</span><br><span class="line">        <span class="comment">// 放行</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">afterCompletion</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="comment">// 移除用户</span></span><br><span class="line">        UserHolder.removeUser();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>配置 MvcConfig</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MvcConfig</span> <span class="keyword">implements</span> <span class="title class_">WebMvcConfigurer</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addInterceptors</span><span class="params">(InterceptorRegistry registry)</span> &#123; <span class="comment">// 添加拦截器</span></span><br><span class="line">        registry.addInterceptor(<span class="keyword">new</span> <span class="title class_">LoginInterceptor</span>())</span><br><span class="line">                .excludePathPatterns(</span><br><span class="line">                        <span class="string">&quot;/user/login&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/user/code&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/shop/**&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/blog/hot&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/shop-type/**&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/voucher/**&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/upload/**&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>UserHolder 定义如下</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserHolder</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> ThreadLocal&lt;User&gt; tl = <span class="keyword">new</span> <span class="title class_">ThreadLocal</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">saveUser</span><span class="params">(User user)</span>&#123;</span><br><span class="line">        tl.set(user);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> User <span class="title function_">getUser</span><span class="params">()</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> tl.get();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">removeUser</span><span class="params">()</span>&#123;</span><br><span class="line">        tl.remove();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>实现 <code>/user/me</code> 功能，用来前端查看我的时候，返回个人信息</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@GetMapping(&quot;/me&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Result <span class="title function_">me</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> UserHolder.getUser();</span><br><span class="line">    <span class="keyword">return</span> Result.ok(user);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>[!Caution]</p><p>:question: ThreadLocal 怎么用的？为什么能保证线程安全？</p><p>:question: 拦截器里面为什么用完要移除？说是防止内存泄漏，啥意思。</p><p>:question: Tomcat 到底怎么用的？程序怎么部署到上面的？别人访问又是如何呢？</p></blockquote><h4 id="隐藏用户敏感信息"><a href="#隐藏用户敏感信息" class="headerlink" title="隐藏用户敏感信息"></a>隐藏用户敏感信息</h4><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329115555946.png" alt="image-20260329115555865"></p><p>可以通过浏览器看到此时用户的全部信息都在，这很不靠谱，容易泄漏隐私、敏感信息。并且还有不必要的数据传输，比如这里面 <code>createTime</code>、<code>updateTime</code>、<code>password</code> 没必要传输，传输也需要时间，同时这个数据也是存到服务器的 Session 的，所以也会占用服务器内存。所以综上，怎么都应该只保留必要信息。</p><p>从哪里开始改？从保存这个用户信息的地方开始下手，也就是 <code>login</code>，我们定义一个 UserDTO 类，用来表示必要的用户信息</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserDTO</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> Long id;</span><br><span class="line">    <span class="keyword">private</span> String nickName;</span><br><span class="line">    <span class="keyword">private</span> String icon;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>login</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">session.setAttribute(<span class="string">&quot;user&quot;</span>, BeanUtil.copyProperties(user, UserDTO.class)); <span class="comment">// 存 UserDTO 就够了</span></span><br></pre></td></tr></table></figure><p>之后 LoginInterceptor，UserHolder 类以及 <code>/user/me</code> 控制器内容都要调整，之后再登录，敏感信息就没有了</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329120716579.png" alt="image-20260329120716501"></p><h4 id="session-共享问题分析"><a href="#session-共享问题分析" class="headerlink" title="session 共享问题分析"></a>session 共享问题分析</h4><p>每个 tomcat 中都有一份属于自己的 session,假设用户第一次访问第一台 tomcat，并且把自己的信息存放到第一台服务器的 session中，但是第二次这个用户访问到了第二台 tomcat，那么在第二台服务器上，肯定没有第一台服务器存放的 session，所以此时 整个登录拦截功能就会出现问题，我们能如何解决这个问题呢？早期的方案是 session 拷贝，就是说虽然每个 tomcat 上都有不同的 session，但是每当任意一台服务器的 session 修改时，都会同步给其他的 Tomcat 服务器的 session，这样的话，就可以实现 session 的共享了</p><ul><li>但是这种方案具有两个大问题<ol><li>每台服务器中都有完整的一份 session 数据，服务器压力过大。</li><li>session拷贝数据时，可能会出现延迟</li></ol></li><li>所以我们后面都是基于 Redis 来完成，我们把 session 换成 Redis，Redis 数据本身就是共享的，就可以避免 session 共享的问题了</li></ul><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329124346818.png" alt="image-20260329124346669"></p><h3 id="2-2-基于-Redis-实现共享-session-登录"><a href="#2-2-基于-Redis-实现共享-session-登录" class="headerlink" title="2.2 基于 Redis 实现共享 session 登录"></a>2.2 基于 Redis 实现共享 session 登录</h3><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329132921236.png" alt="image-20260329132921065"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329133420894.png" alt="image-20260329133403472"></p><ul><li>用户发送短信验证码，服务端将生成的验证码保存到 Redis，这个 Key 要有标识性，因为是用手机号发起请求，可以根据这个特性来作为 <code>key</code> 值</li><li>之后在进行登录的时候，以手机号为 <code>key</code> 来取验证码进行结果校验，之后把用户的信息保存到 Redis，并且用一个随机到 token 作为 <code>key</code>，并且把这个 token 返回给前端。<ul><li>前端是存储在本地的，所以不用手机号作为 key，而是随机 token 来保证用户隐私</li><li>前端把 token 保存在本地 <code>sessionStorage</code>，每次发起请求的时候也用拦截器把 token 加到 header 请求头里面一般用 <code>Authorization</code></li></ul></li></ul><p>我们一步步来修改，第一步是保存验证码，在 <code>sendCode</code> 方法里面。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> Result <span class="title function_">sendCode</span><span class="params">(String phone, HttpSession session)</span> &#123;</span><br><span class="line">  </span><br><span class="line">    <span class="keyword">if</span> (RegexUtils.isPhoneInvalid(phone)) &#123;</span><br><span class="line">        <span class="keyword">return</span> Result.fail(<span class="string">&quot;手机号格式错误&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">String</span> <span class="variable">code</span> <span class="operator">=</span> RandomUtil.randomNumbers(<span class="number">6</span>);</span><br><span class="line">  </span><br><span class="line">    <span class="comment">// 保存到 redis</span></span><br><span class="line">    <span class="comment">// session.setAttribute(&quot;code&quot;, code);</span></span><br><span class="line">    stringRedisTemplate.opsForValue().set(<span class="string">&quot;login:code:&quot;</span> + phone, code, <span class="number">2</span>, TimeUnit.MINUTES); <span class="comment">// 过期时间两分钟</span></span><br><span class="line"></span><br><span class="line">    log.debug(<span class="string">&quot;发送验证码成功，验证码: &#123;&#125;&quot;</span>, code);</span><br><span class="line">    <span class="keyword">return</span> Result.ok();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里呢，应该更优雅一点，因为里面有常量  <code>login:code</code>, <code>2</code>，一般这种都要拿出来单独放在一个常量类里面，因为引用比写更不容易出错，并且方便统一管理</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义 Redis 常量类</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RedisConstants</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">LOGIN_CODE_KEY</span> <span class="operator">=</span> <span class="string">&quot;login:code:&quot;</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Long</span> <span class="variable">LOGIN_CODE_TTL</span> <span class="operator">=</span> <span class="number">2L</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>那上面那个也可以改成下面这种</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">stringRedisTemplate.opsForValue().set(LOGIN_CODE_KEY + phone, code, LOGIN_CODE_TTL, TimeUnit.MINUTES);</span><br></pre></td></tr></table></figure><p>然后是登录</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> Result <span class="title function_">login</span><span class="params">(LoginFormDTO loginForm, HttpSession session)</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="type">String</span> <span class="variable">phone</span> <span class="operator">=</span> loginForm.getPhone();</span><br><span class="line">    <span class="keyword">if</span> (RegexUtils.isPhoneInvalid(phone)) &#123;</span><br><span class="line">        <span class="keyword">return</span> Result.fail(<span class="string">&quot;手机号格式错误&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// String cacheCode = (String) session.getAttribute(&quot;code&quot;);</span></span><br><span class="line">    <span class="comment">// 校验验证码 --&gt; 从 redis 里面取出来</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">cacheCode</span> <span class="operator">=</span> stringRedisTemplate.opsForValue().get(LOGIN_CODE_KEY + phone);</span><br><span class="line">  </span><br><span class="line">    <span class="type">String</span> <span class="variable">code</span> <span class="operator">=</span> loginForm.getCode();</span><br><span class="line">    <span class="keyword">if</span> (cacheCode == <span class="literal">null</span> || !cacheCode.equals(code)) &#123; </span><br><span class="line">        <span class="keyword">return</span> Result.fail(<span class="string">&quot;验证码错误&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> query().eq(<span class="string">&quot;phone&quot;</span>, phone).one();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (user == <span class="literal">null</span>) &#123;</span><br><span class="line">        user = createUserWithPhone(phone);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 6 保存用户到 redis</span></span><br><span class="line">    <span class="comment">// 生成 token</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">token</span> <span class="operator">=</span> UUID.randomUUID().toString(<span class="literal">true</span>);<span class="comment">// true 代表不带-,</span></span><br><span class="line">    <span class="type">UserDTO</span> <span class="variable">userDTO</span> <span class="operator">=</span> BeanUtil.copyProperties(user, UserDTO.class);</span><br><span class="line">    <span class="comment">// putAll 需要 map 对象</span></span><br><span class="line">    Map&lt;String, Object&gt; userMap = BeanUtil.beanToMap(userDTO);</span><br><span class="line">  </span><br><span class="line">    stringRedisTemplate.opsForHash().putAll(LOGIN_USER_KEY + token, userMap);</span><br><span class="line">    <span class="comment">// 设置有效期, 同 session 设置有效期为 30 分钟</span></span><br><span class="line">    stringRedisTemplate.expire(LOGIN_USER_KEY + token, LOGIN_USER_TTL, TimeUnit.MINUTES);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> Result.ok(token);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>验证码取出来是从 <code>redis</code> 里面取</li><li>生成 <code>token</code> 我们用 hutool 包里面的 UUID，<code>toString(true)</code> 代表生成的 UUID 里面不带 <code>-</code></li><li><code>BeanUtil.beanToMap</code> 把对象转为 map</li><li>为该键设置有效期，我们同 session 有效期一样，设置为 <code>30</code> 分钟，这样也可以定期清除防止无限占用内存</li></ul><p>然后重写拦截器</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LoginInterceptor</span> <span class="keyword">implements</span> <span class="title class_">HandlerInterceptor</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 这里没有使用 @Autowired！</span></span><br><span class="line">    <span class="keyword">private</span> StringRedisTemplate redisTemplate;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 加了个构造器</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">LoginInterceptor</span><span class="params">(StringRedisTemplate redisTemplate)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.redisTemplate = redisTemplate;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">preHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="comment">// 获取 请求头中的 token</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">token</span> <span class="operator">=</span> request.getHeader(<span class="string">&quot;Authorization&quot;</span>);</span><br><span class="line">        <span class="comment">// 判断是否为空</span></span><br><span class="line">        <span class="keyword">if</span> (StrUtil.isBlank(token)) &#123; </span><br><span class="line">            response.setStatus(<span class="number">401</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> LOGIN_USER_KEY + token;</span><br><span class="line">        <span class="comment">// 获取 redis 中的用户信息</span></span><br><span class="line">        Map&lt;Object, Object&gt; userMap = redisTemplate.opsForHash().entries(key);</span><br><span class="line">        <span class="keyword">if</span> (userMap.isEmpty()) &#123;</span><br><span class="line">            <span class="comment">// 说明过期了或者 token 不对</span></span><br><span class="line">            response.setStatus(<span class="number">401</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 从 Map 转为对象</span></span><br><span class="line">        <span class="type">UserDTO</span> <span class="variable">userDTO</span> <span class="operator">=</span> BeanUtil.fillBeanWithMap(userMap, <span class="keyword">new</span> <span class="title class_">UserDTO</span>(), <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置有效期</span></span><br><span class="line">        redisTemplate.expire(key, LOGIN_USER_TTL, TimeUnit.MINUTES);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 存在，保存用户信息到 ThreadLocal</span></span><br><span class="line">        UserHolder.saveUser(userDTO);</span><br><span class="line">        <span class="comment">// 放行</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">afterCompletion</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="comment">// 移除用户</span></span><br><span class="line">        UserHolder.removeUser();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>修改 MvcConfig</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MvcConfig</span> <span class="keyword">implements</span> <span class="title class_">WebMvcConfigurer</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> StringRedisTemplate redisTemplate;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addInterceptors</span><span class="params">(InterceptorRegistry registry)</span> &#123;</span><br><span class="line">        registry.addInterceptor(<span class="keyword">new</span> <span class="title class_">LoginInterceptor</span>(redisTemplate)) <span class="comment">// 将 redisTemplate 作为参数传入</span></span><br><span class="line">                .excludePathPatterns(</span><br><span class="line">                        <span class="string">&quot;/user/login&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/user/code&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/shop/**&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/blog/hot&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/shop-type/**&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/voucher/**&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/upload/**&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>这里 <code>LoginInterceptor</code> 没有对 <code>redisTemplate</code> 自动注入的原因是因为 <code>LoginInterceptor</code> 并没有被 Spring 接管（它上面没有加任何 <code>@Component</code>、<code>@Service</code> 等这种注解）所以这里的解决方法是通过构造器，然后传进来，所以在 <code>WebMvcConfigurer</code> 里面，因为这个配置类是在容器里面的，所以在这个里面自动注入，然后通过构造器传的方式放进去让登录拦截器去使用</li><li>同时这里有重置有效期的操作，因为 Session 会话是 30 分钟无操作即自动断开连接，所以这里可以模仿他，那么就是每次请求时重置一下有效期。</li></ul><p>运行～发送验证码～没问题～登录～报错～</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329145511619.png" alt="image-20260329145511521"></p><p>报错提示到下面这一行</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Map&lt;String, Object&gt; userMap = BeanUtil.beanToMap(userDTO);</span><br><span class="line">stringRedisTemplate.opsForHash().putAll(LOGIN_USER_KEY + token, userMap); <span class="comment">// 报错</span></span><br></pre></td></tr></table></figure><p>说 Long 类型无法转为 String 类型，原因是因为 UserDTO 里面的 id 是 Long 类型，但是咱们的 StringRedisTemplate 定义的泛型是 <code>&lt;String, String&gt;</code>，也就是键、值都是 String 类型。</p><blockquote><p>[!Caution]</p><p>:question: Integer 里面不是有 toString() 方法吗？为什么不能强转</p><p>这是混淆了强制类型转换（Casting）和类型转换。强制类型转换里面，被转换的对象必须本身就是那个类型或者它的子类。而 <code>Long</code> 和 <code>String</code> 在类继承树上属于平级。</p><p>手动调用的 <code>id.toString()</code> 那是方法调用，是我要根据 Long 里的数字来创建一个 String 对象。</p><p>所以同理，即使是 Integer 类也不能强转为 Long 类。</p><p>但是 int 和 long 之间可以强转，因为他们都是基本数据类型，在内存中是连续的二进制位，所以直接作截断或者提升就可以了。</p><p>强转 <code>(Type) object</code> 必须遵循向上转型（变成爸爸或者爷爷）或者向下转型（爸爸转成儿子），但是大儿子不能强转为二儿子。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329150919948.png" alt="image-20260329150919736"></p><p>上面这个现象，第一个不报错，因为发生了隐式类型提升，int 自动变为 long。第二个报错，<code>10</code> 被装箱成 <code>Integer</code> 对象，然后尝试把 Integer 对象赋值给 Long 引用！！报错。</p></blockquote><p>所以我们修改原来的 <code>BeanUtil.beanToMap</code> 方法，手动调用一下 <code>toString()</code> 方法</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Map&lt;String, Object&gt; userMap = BeanUtil.beanToMap(userDTO,</span><br><span class="line">                        <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;(), <span class="comment">// 目标的Map</span></span><br><span class="line">                        CopyOptions.create() <span class="comment">// 创建拷贝选项</span></span><br><span class="line">                                .ignoreNullValue() <span class="comment">// 设置忽略空值，当源对象的值为null时，忽略而不注入此值</span></span><br><span class="line">                                .setFieldValueEditor((key, value) -&gt; value.toString())); <span class="comment">// 设置字段属性值编辑器，用于自定义属性值转换规则</span></span><br></pre></td></tr></table></figure><p>这里这个目标 Map 我是新建的一个空的，也可以不为空。这里还有一个问题，如果 <code>value</code> 为空调用这个方法岂不是报错，所以需要多一步判断</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Map&lt;String, Object&gt; userMap = BeanUtil.beanToMap(userDTO,</span><br><span class="line">                        <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;(),</span><br><span class="line">                        CopyOptions.create()</span><br><span class="line">                                .ignoreNullValue()</span><br><span class="line">                                .setFieldValueEditor((key, value) -&gt; value == <span class="literal">null</span> ? <span class="literal">null</span> : value.toString()));</span><br></pre></td></tr></table></figure><blockquote><p>[!Warning]</p><p>你可能疑问，这里不是前面加了 <code>ignoreNullValue</code> 了吗，怎么还要判断？</p><p>因为 <code>ignoreNullValue</code> 为当源对象值为 <code>null</code> 时，忽略而不注入。而 <code>setFieldValueEditor</code> 是自定义属性值的转换规则，有可能结果是 <code>null</code>，所以是先改值再忽略 <code>null</code> 值。</p></blockquote><p>然后我们登录，可以看到 token 被存入到本地浏览器的 session storage，并且发送请求的时候会把 token 放入请求头。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329152024021.png" alt="image-20260329152023916"></p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329152044299.png" alt="image-20260329152044097"></p><h4 id="解决状态登录刷新的问题"><a href="#解决状态登录刷新的问题" class="headerlink" title="解决状态登录刷新的问题"></a>解决状态登录刷新的问题</h4><p>前面其实也有个小问题，用户操作是不是应该就刷新时间，无论是在首页浏览还是看个人信息，但是前面那个拦截器只拦截了部分，对于查看首页是没有做拦截的，也就是说 token 有效期不会刷新，所以要解决这个问题，我们可以再加一层拦截器，只用来刷新有效期、存用户信息。</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329153509709.png" alt="image-20260329153509613"></p><p>第一层拦截器</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RefreshTokenInterceptor</span> <span class="keyword">implements</span> <span class="title class_">HandlerInterceptor</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> StringRedisTemplate redisTemplate;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">RefreshTokenInterceptor</span><span class="params">(StringRedisTemplate redisTemplate)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.redisTemplate = redisTemplate;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">preHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="comment">// 获取 请求头中的 token</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">token</span> <span class="operator">=</span> request.getHeader(<span class="string">&quot;Authorization&quot;</span>);</span><br><span class="line">        <span class="keyword">if</span> (StrUtil.isBlank(token)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>; <span class="comment">// 为空就放行，因为用户可能在请求不需要登录的功能，比如首页浏览</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> LOGIN_USER_KEY + token;</span><br><span class="line">        <span class="comment">// 获取 redis 中的用户</span></span><br><span class="line">        Map&lt;Object, Object&gt; userMap = redisTemplate.opsForHash().entries(key);</span><br><span class="line">        <span class="keyword">if</span> (userMap.isEmpty()) &#123;</span><br><span class="line">            <span class="comment">// 说明过期了或者 token 不对，token 即使过期了，用户也可能在请求不需要登录的功能，继续放行</span></span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="type">UserDTO</span> <span class="variable">userDTO</span> <span class="operator">=</span> BeanUtil.fillBeanWithMap(userMap, <span class="keyword">new</span> <span class="title class_">UserDTO</span>(), <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置有效期</span></span><br><span class="line">        redisTemplate.expire(key, LOGIN_USER_TTL, TimeUnit.MINUTES);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 存在，保存用户信息到 ThreadLocal</span></span><br><span class="line">        UserHolder.saveUser(userDTO);</span><br><span class="line">        <span class="comment">// 放行</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">afterCompletion</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="comment">// 移除用户</span></span><br><span class="line">        UserHolder.removeUser();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>它来获取 <code>token</code>，没有就放行（因为可能没有登录，但是也能访问首页什么的），如果有，获取用户信息，然后设置有效期、保存用户信息等。这里相当于做了通用的操作，这里只放行，不拦截，是因为这个拦截器功能是刷新 Token 有效期</li></ul><p>第二个登录拦截器，拦截部分需要登录才能访问的请求，第一个拦截器里面已经把用户信息拿到了，所以这里只要做个判断就可以了。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LoginInterceptor</span> <span class="keyword">implements</span> <span class="title class_">HandlerInterceptor</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">preHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(UserHolder.getUser() == <span class="literal">null</span>) &#123;</span><br><span class="line">            response.setStatus(<span class="number">401</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 放行</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后我们需要修改配置类</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MvcConfig</span> <span class="keyword">implements</span> <span class="title class_">WebMvcConfigurer</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> StringRedisTemplate stringRedisTemplate;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addInterceptors</span><span class="params">(InterceptorRegistry registry)</span> &#123;</span><br><span class="line">        registry.addInterceptor(<span class="keyword">new</span> <span class="title class_">LoginInterceptor</span>()) <span class="comment">// 不需要 stringRedisTemplate 了</span></span><br><span class="line">                .excludePathPatterns(</span><br><span class="line">                        <span class="string">&quot;/user/login&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/user/code&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/shop/**&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/blog/hot&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/shop-type/**&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/voucher/**&quot;</span>,</span><br><span class="line">                        <span class="string">&quot;/upload/**&quot;</span>)</span><br><span class="line">                .order(<span class="number">1</span>);</span><br><span class="line">        registry.addInterceptor(<span class="keyword">new</span> <span class="title class_">RefreshTokenInterceptor</span>(stringRedisTemplate))</span><br><span class="line">                .addPathPatterns(<span class="string">&quot;/**&quot;</span>) <span class="comment">// 默认就是全部拦截</span></span><br><span class="line">                .order(<span class="number">0</span>); ；<span class="comment">// 设置执行顺序，越小优先级越高</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果不设置优先级，默认拦截器的 <code>order</code> 都是 0，那就看定义的时候谁先定义的，先定义的优先级高。</p><p>运行，如果是在首页、个人页都能实现进行请求时就刷新 token 有效期，那么就成功了</p><h2 id="3-商户查询缓存"><a href="#3-商户查询缓存" class="headerlink" title="3 商户查询缓存"></a>3 商户查询缓存</h2><h3 id="3-1-缓存介绍"><a href="#3-1-缓存介绍" class="headerlink" title="3.1 缓存介绍"></a>3.1 缓存介绍</h3><p><strong>缓存</strong> 就是数据交换的缓冲区，是存储数据的临时地方，一般读写性能较高。缓存数据存储于代码中，而代码运行在内存中，内存的读写性能远高于磁盘，缓存可以大大降低用户访问并发量带来的服务器读写压力，但是缓存也会增加代码复杂度和运营成本</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329161245948.png" alt="image-20260329161245842"></p><h4 id="如何使用缓存"><a href="#如何使用缓存" class="headerlink" title="如何使用缓存"></a>如何使用缓存</h4><p>实际开发中，会构筑多级缓存来时系统运行速度进一步提升，例如：本地缓存与 Redis 中的缓存并发使用</p><ul><li>浏览器缓存：主要是存在于浏览器端的缓存</li><li>应用层缓存：可以分为 tomcat 本地缓存，例如之前提到的 map 或者是使用 Redis 作为缓存</li><li>数据库缓存：在数据库中有一片空间是 buffer pool，增改查数据都会先加载到 mysql 的缓存中</li><li>CPU 缓存：当代计算机最大的问题就是 CPU 性能提升了，但是内存读写速度没有跟上，所以为了适应当下的情况，增加了 CPU 的 L1，L2，L3 级的缓存</li></ul><h4 id="缓存的作用"><a href="#缓存的作用" class="headerlink" title="缓存的作用"></a>缓存的作用</h4><ol><li>降低后端负载（减少查数据库的次数，也就是磁盘读写次数）</li><li>提高读写效率，降低响应时间</li></ol><h4 id="缓存的成本"><a href="#缓存的成本" class="headerlink" title="缓存的成本"></a>缓存的成本</h4><ol><li>数据一致性成本</li><li>代码维护成本</li><li>运维成本（一般采用服务器集群，需要多加机器，机器就是钱，还需要人维护）</li></ol><h3 id="3-2-添加商户缓存"><a href="#3-2-添加商户缓存" class="headerlink" title="3.2 添加商户缓存"></a>3.2 添加商户缓存</h3><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329162615010.png" alt="image-20260329162614886" style="zoom:30%;" /><p>原本的逻辑是，比如查询，直接查询数据库，然后返回结果，逻辑代码如下（Mybatis-plus 提供的方法）</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 根据 id 查询商户信息</span></span><br><span class="line"><span class="meta">@GetMapping(&quot;/&#123;id&#125;&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Result <span class="title function_">queryShopById</span><span class="params">(<span class="meta">@PathVariable(&quot;id&quot;)</span> Long id)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> Result.ok(shopService.getById(id));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>优化的思路是在客户端与数据库之间加上一个 Redis 缓存，先从 Redis 中查询，如果没有查到，再去 MySQL 中查询，同时查询完毕之后，将查询到的数据也存入 Redis，这样当下一个用户来进行查询的时候，就可以直接从 Redis 中获取到数据</p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329162938122.png" alt="image-20260329162937800" style="zoom:30%;" /><p>代码流程如下</p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329163107074.png" alt="image-20260329163106969" style="zoom:30%;" /><p>实现如下</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> Result <span class="title function_">queryById</span><span class="params">(Long id)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> CACHE_SHOP_KEY + id; <span class="comment">// cache:shop:id</span></span><br><span class="line">    <span class="comment">// 1 从 Redis 查询商户缓存</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">shopJson</span> <span class="operator">=</span> stringRedisTemplate.opsForValue().get(key);</span><br><span class="line">    <span class="comment">// 2 判断是否存在</span></span><br><span class="line">    <span class="keyword">if</span> (StrUtil.isNotBlank(shopJson)) &#123;</span><br><span class="line">        <span class="comment">// 3 存在，直接返回</span></span><br><span class="line">        <span class="type">Shop</span> <span class="variable">shop</span> <span class="operator">=</span> JSONUtil.toBean(shopJson, Shop.class);</span><br><span class="line">        <span class="keyword">return</span> Result.ok(shop);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 4 不存在，根据 id 查询数据库</span></span><br><span class="line">    <span class="type">Shop</span> <span class="variable">shop</span> <span class="operator">=</span> <span class="built_in">this</span>.getById(id);</span><br><span class="line">    <span class="comment">// 5 不存在，返回错误</span></span><br><span class="line">    <span class="keyword">if</span> (shop == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> Result.fail(<span class="string">&quot;店铺不存在!&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 6 存在，写入 Redis</span></span><br><span class="line">    stringRedisTemplate.opsForValue().set(key, JSONUtil.toJsonStr(shop));</span><br><span class="line">    <span class="comment">// 7 返回</span></span><br><span class="line">    <span class="keyword">return</span> Result.ok(shop);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里应该用 Hash 的，只不过这里用 JSON 来练练手</p><p>我们比较一下带缓存和不带缓存的查询时间，第一个不带缓存，也就是第一次请求</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329164514910.png" alt="image-20260329164514683"></p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329164544370.png" alt="image-20260329164544260" style="zoom:50%;" /><p>数据确实被缓存了，然后再次请求</p><p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329165353048.png" alt="image-20260329165352816"></p><p>速度很快了。</p><p>并且控制台打印没有输出查询商户的SQL，说明这次查询没有走MySQL。</p><blockquote><p>[!Caution]</p><p>其实这里教程说的有问题，上面这个内容没问题，结果的解释有问题。这个加速并不是 Redis 缓存起到的加速。因为视频里面是重启了 SpringBoot，然后再发起的请求，第一次请求时间长是因为 mvc 组建懒加载导致的，并不是因为 MySQL 查的慢。你第二次请求、第三次、第四次请求其实速度就一样了，并且你试一下，用 MySQL 和用 Redis 基本上没差别，我多试了几下，基本上平均 Redis 比 MySQL 能快 1ms 左右吧，偶尔有的时候比 MySQL 慢，优化很小。其实也是因为查询的数据太少了，只有一条数据，优化不明显，并且上下有浮动。</p><p>类似Redis 可能 <code>6~10</code> ms 左右，MySQL <code>7-11</code> ms 左右，平均来说提高了，但是偶尔 MySQL 可能还会快一点。比如 MySQL 查下有时候 7ms，Redis 可能 10ms呢。</p></blockquote><h3 id="3-3-添加商铺类型缓存"><a href="#3-3-添加商铺类型缓存" class="headerlink" title="3.3 添加商铺类型缓存"></a>3.3 添加商铺类型缓存</h3><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260329170413092.png" alt="image-20260329170412965" style="zoom:50%;" /><p>也就是这一块的缓存，原来对应代码如下</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/shop-type&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ShopTypeController</span> &#123;</span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> IShopTypeService typeService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping(&quot;list&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> Result <span class="title function_">queryTypeList</span><span class="params">()</span> &#123;</span><br><span class="line">        List&lt;ShopType&gt; typeList = typeService</span><br><span class="line">                .query().orderByAsc(<span class="string">&quot;sort&quot;</span>).list();</span><br><span class="line">        <span class="keyword">return</span> Result.ok(typeList);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后我们进行改造</p><div class="tabs"><div class="nav-tabs"><button type="button" class="tab active">Controller</button><button type="button" class="tab">Service</button><button type="button" class="tab">ServiceImpl</button></div><div class="tab-contents"><div class="tab-item-content active"><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@GetMapping(&quot;list&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Result <span class="title function_">queryTypeList</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> typeService.queryTypeList();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div><div class="tab-item-content"><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Result <span class="title function_">queryTypeList</span><span class="params">()</span>;</span><br></pre></td></tr></table></figure></div><div class="tab-item-content"><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> Result <span class="title function_">queryTypeList</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> <span class="string">&quot;cache:shopType:list&quot;</span>;</span><br><span class="line">    <span class="comment">// 1 从 Redis 查询缓存</span></span><br><span class="line">    List&lt;String&gt; shopTypes = stringRedisTemplate.opsForList().range(key, <span class="number">0</span>, -<span class="number">1</span>);</span><br><span class="line">    <span class="comment">// 2 缓存存在则返回, 存在且不为空</span></span><br><span class="line">    <span class="keyword">if</span> (CollUtil.isNotEmpty(shopTypes)) &#123;</span><br><span class="line">        ArrayList&lt;ShopType&gt; temp = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        <span class="keyword">for</span> (String shopType : shopTypes) &#123;</span><br><span class="line">            temp.add(JSONUtil.toBean(shopType, ShopType.class));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> Result.ok(temp);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 3 缓存不存在从数据库中查询</span></span><br><span class="line">    List&lt;ShopType&gt; temp = query().orderByAsc(<span class="string">&quot;sort&quot;</span>).list();</span><br><span class="line">    <span class="comment">// 4 转为 JSON 字符串</span></span><br><span class="line">    <span class="keyword">if</span> (CollUtil.isNotEmpty(temp)) &#123;</span><br><span class="line">        <span class="keyword">for</span> (ShopType shopType : temp) &#123;</span><br><span class="line">            <span class="type">String</span> <span class="variable">json</span> <span class="operator">=</span> JSONUtil.toJsonStr(shopType);</span><br><span class="line">            shopTypes.add(json);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 5 存入缓存</span></span><br><span class="line">    stringRedisTemplate.opsForList().leftPushAll(key, shopTypes);</span><br><span class="line">    <span class="comment">// 6 返回</span></span><br><span class="line">    <span class="keyword">return</span> Result.ok(temp);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>也可以用流来实现一下</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> Result <span class="title function_">queryTypeList</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> <span class="string">&quot;cache:shopType:list&quot;</span>;</span><br><span class="line">    <span class="comment">// 1 从 Redis 查询缓存</span></span><br><span class="line">    List&lt;String&gt; shopTypes = stringRedisTemplate.opsForList().range(key, <span class="number">0</span>, -<span class="number">1</span>);</span><br><span class="line">    <span class="comment">// 2 缓存存在则返回, 存在且不为空</span></span><br><span class="line">    <span class="keyword">if</span> (CollUtil.isNotEmpty(shopTypes)) &#123;</span><br><span class="line">        List&lt;ShopType&gt; temp = shopTypes</span><br><span class="line">                .stream()</span><br><span class="line">                .map(i -&gt; JSONUtil.toBean(i, ShopType.class))</span><br><span class="line">                .toList();</span><br><span class="line">        <span class="keyword">return</span> Result.ok(temp);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 3 缓存不存在从数据库中查询</span></span><br><span class="line">    List&lt;ShopType&gt; temp = query().orderByAsc(<span class="string">&quot;sort&quot;</span>).list();</span><br><span class="line">    <span class="comment">// 4 转为 JSON 字符串</span></span><br><span class="line">    <span class="keyword">if</span> (CollUtil.isNotEmpty(temp)) &#123;</span><br><span class="line">        shopTypes = temp.stream().map(JSONUtil::toJsonStr).toList();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 5 存入缓存</span></span><br><span class="line">    stringRedisTemplate.opsForList().leftPushAll(key, shopTypes);</span><br><span class="line">    <span class="comment">// 6 返回</span></span><br><span class="line">    <span class="keyword">return</span> Result.ok(temp);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div><h3 id="缓存更新策略"><a href="#缓存更新策略" class="headerlink" title="缓存更新策略"></a>缓存更新策略</h3><ul><li><code>内存淘汰</code>：Redis 自动进行，当 Redis 内存使用达到我们设定的 <code>max-memery</code> 时，会自动触发淘汰机制，淘汰掉一些不重要的数据。这个原来是 Redis 来解决数据越来越多内存不足的方式。（有好多种策略），默认是开启的。一致性比较差，不是我们能控制的，淘汰什么数据、什么时候淘汰我们都不知道，但是不用我们管，没有维护成本。</li><li><code>超时剔除</code>：当我们给 Redis 设置了过期时间 TTL 之后，Redis 会将超时的数据进行删除，这样再次查询的时候会把数据库的数据同步过来，根据这个方式来进行维护一致性。所以一致性的情况跟 TTL 设置的时长也有关系（比如 TTL 30 分钟，但是这个期间数据库发生变化了，就出现不一致了）。维护成本也不高，只要加一个过期时间就可以了，一行代码。</li><li><code>主动更新</code>：我们可以手动调用方法把缓存删除掉，通常用于解决缓存和数据库不一致问题。</li></ul><table><thead><tr><th align="center"></th><th align="center">内存淘汰</th><th align="center">超时剔除</th><th align="center">主动更新</th></tr></thead><tbody><tr><td align="center">说明</td><td align="center">不用自己维护，利用 Redis 的内存淘汰机制，当内存不足时自动淘汰部分数据。下次查询时更新缓存。</td><td align="center">给缓存数据添加 TTL 时间，到期后自动删除缓存。下次查询时更新缓存。</td><td align="center">编写业务逻辑，在修改数据库的同时，更新缓存。</td></tr><tr><td align="center">一致性</td><td align="center">差</td><td align="center">一般</td><td align="center">好</td></tr><tr><td align="center">维护成本</td><td align="center">无</td><td align="center">低</td><td align="center">高</td></tr></tbody></table><p>业务场景：</p><ul><li>低一致性需求：使用内存淘汰机制。例如商铺类型的查询缓存（很久都不更新一次）</li><li>高一致性需求：主动更新，并用超时剔除作为兜底方案。例如商铺详情查询的缓存</li></ul><h4 id="不一致性解决方案"><a href="#不一致性解决方案" class="headerlink" title="不一致性解决方案"></a>不一致性解决方案</h4><p>由于我们的缓存数据源来自数据库，而数据库的数据是会发生变化的，因此，如果当数据库中数据发生变化，而缓存却没有同步，此时就会有一致性问题存在，用户就会使用缓存中的过时数据，就会产生类似多线程数据安全问题</p><p>主动更新目前有如下三种主流方式</p><ol><li><p>Cache Aside Pattern 人工编码方式：缓存调用者在更新完数据库之后再去更新缓存，也称之为双写方案</p></li><li><p>Read&#x2F;Write Through Pattern：缓存与数据库整合为一个服务，由服务来维护一致性。调用者调用该服务，无需关心缓存一致性问题，由它来管理缓存和数据库的一致性。但是维护这样一个服务很复杂，市面上也不容易找到这样的一个现成的服务，开发成本高</p></li><li><p>Write Behind Caching Pattern：调用者&#x3D;&#x3D;只操作缓存&#x3D;&#x3D;，其他线程去异步处理数据库，最终实现一致性。但是维护这样的一个异步的任务很复杂，需要实时监控缓存中的数据更新，其他线程去异步更新数据库也可能不太及时，一致性不能完全保证，而且缓存服务器如果宕机，那么缓存的数据也就丢失了</p><p>好处</p><ul><li>比如十次写操作，先放到缓存，然后线程再执行一次批处理，把内容同步到数据库，只操作一次数据库</li><li>比如对某个数据进行了多次修改，只需要同步最后一次的结果就可以，效率高</li></ul></li></ol><h4 id="不一致性解决方法"><a href="#不一致性解决方法" class="headerlink" title="不一致性解决方法"></a>不一致性解决方法</h4><p>在企业的实际应用中，方案一是主流的，而且比较可靠的，那么就需要考虑三个问题</p><ol><li><p>删除缓存还是更新缓存？<br>更新缓存：每次更新数据库都更新缓存，无效写操作较多。比如连续更新了100次数据，但是没有人读，更新缓存其实没什么用。❌<br>删除缓存：更新数据库时让缓存失效，查询时再更新缓存，避免了无效写操作，减少更新次数。✅</p></li><li><p>如何保证缓存与数据库的操作同时成功或失败？<br>需要保证缓存与数据库操作的原子性，比如更新数据库成功了，但是删除缓存失败了，那就没意义了<br>单体系统：将缓存与数据库操作放在一个事务<br>分布式系统：利用 TTC 等分布式事务方案</p></li><li><p>先操作缓存还是先操作数据库？</p><p>在并发情况下，删除缓存和更新数据库操作不同线程之间可能会交叉，不可避免出现问题。</p><ul><li>先删除缓存，再操作数据库</li></ul><div class="tabs"><div class="nav-tabs"><button type="button" class="tab">正常情况</button><button type="button" class="tab active">异常情况</button></div><div class="tab-contents"><div class="tab-item-content"><p>正常情况如下，比如先操作数据库再删除缓存，刚开始缓存和数据库都有数据 <code>10</code>，线程 1 执行的时候先删除缓存，然后更新数据库为 <code>20</code>，然后线程 2 查询的时候缓存未命中，就从数据库查询并且写入缓存，此时缓存和数据库都是数据 <code>20</code>。</p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330133848190.png" alt="image-20260330133848059" style="zoom:40%;" /></div><div class="tab-item-content active"><p>还是刚开始初始值都是 <code>10</code>，线程 1 先删除缓存，然后去更新，业务比较复杂比较慢，这个时候线程 2 也来更新，查询缓存，没查到，就去查询数据库并且写入缓存，因为更新操作还没完成，所以读取的是之前的值 <code>10</code>，然后写入了缓存，之后线程 1 更新数据库完成，最终缓存值为 <code>10</code>，数据库值为 <code>20</code>，出现不一致，出现线程安全性。</p><p>这种情况出现的概率还是很高的！！因为是先删缓存，再操作数据库，删缓存快，写入数据库慢，查缓存、查数据库、写缓存都快。所以这个线程 1 的空隙很大，很容易被线程 2 这种查询的捕捉到。</p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330134707915.png" alt="image-20260330134707779" style="zoom:40%;" /></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div><ul><li>先操作数据库，再删除缓存</li></ul><div class="tabs"><div class="nav-tabs"><button type="button" class="tab">正常情况</button><button type="button" class="tab active">异常情况</button></div><div class="tab-contents"><div class="tab-item-content"><p>这没啥问题，不多说</p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330135957609.png" alt="image-20260330135957483" style="zoom:40%;" /></div><div class="tab-item-content active"><p>这里刚开始缓存和数据库数据都是 <code>10</code>，假如说线程 1 来查询缓存的时候，缓存刚好失效了，没查到（查到不就直接返回了吗），然后去查询了数据库，得到值 <code>10</code>，在它拿值写入缓存的空隙，线程 2 更新了数据库为 <code>20</code>，并且删除了缓存（缓存本来就没数据，删了没影响），然后线程 1 把值 <code>10</code> 写入了缓存，出现了不一致。<br>但是这种情况出现概率很低</p><ul><li><p>出现并发情况</p></li><li><p>需要线程 1 操作的时候缓存失效</p></li><li><p>需要在写入缓存这个极短的间隙中（微秒）完成更新数据库这个比较耗时的操作</p></li></ul><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330140244063.png" alt="image-20260330140243928" style="zoom:40%;" /></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div><p>所以方案 2 胜出，我们先写数据库，再写缓存</p></li></ol><p>最终整体方案就为</p><ol><li>低一致性需求：使用 Redis 自带的内存淘汰机制。</li><li>高一致性需求：主动更新，并以超时剔除作为兜底方案。<ul><li>读操作：<ul><li>缓存命中则直接返回</li><li><mark class="hl-label red">缓存未命中则查询数据库，并写入缓存，设定超时时间</mark></li></ul></li><li>写操作：<ul><li><mark class="hl-label red">先写数据库，然后再删除缓存</mark>（尽可能保证线程安全性）</li><li>要确保数据库与缓存操作的原子性（单体系统用事务，分布式系统用 TTC 等分布式事务）</li></ul></li></ul></li></ol><p>:question: 新增数据、删除数据怎么处理缓存，比如商铺分类列表之类的？</p><h3 id="实现商铺缓存与数据库的双写一致性"><a href="#实现商铺缓存与数据库的双写一致性" class="headerlink" title="实现商铺缓存与数据库的双写一致性"></a>实现商铺缓存与数据库的双写一致性</h3><p>分为查询和更新操作两部分</p><h4 id="查询"><a href="#查询" class="headerlink" title="查询"></a>查询</h4><p>原来缓存未命中去查询数据库并写入缓存的逻辑已经实现了，再加上设置超时时间就可以了。</p><figure class="highlight diff"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">@Override</span><br><span class="line">public Result queryById(Long id) &#123;</span><br><span class="line">    String key = CACHE_SHOP_KEY + id; // cache:shop:id</span><br><span class="line">    // 1 从 Redis 查询商户缓存</span><br><span class="line">    String shopJson = stringRedisTemplate.opsForValue().get(key);</span><br><span class="line">    // 2 判断是否存在</span><br><span class="line">    if (StrUtil.isNotBlank(shopJson)) &#123;</span><br><span class="line">        // 3 存在，直接返回</span><br><span class="line">        Shop shop = JSONUtil.toBean(shopJson, Shop.class);</span><br><span class="line">        return Result.ok(shop);</span><br><span class="line">    &#125;</span><br><span class="line">    // 4 不存在，根据 id 查询数据库</span><br><span class="line">    Shop shop = this.getById(id);</span><br><span class="line">    // 5 不存在，返回错误</span><br><span class="line">    if (shop == null) &#123;</span><br><span class="line">        return Result.fail(&quot;店铺不存在!&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">    // 6 存在，写入 Redis</span><br><span class="line"><span class="deletion">-   stringRedisTemplate.opsForValue().set(key, JSONUtil.toJsonStr(shop));</span></span><br><span class="line"><span class="addition">+   stringRedisTemplate.opsForValue().set(key, JSONUtil.toJsonStr(shop), CACHE_SHOP_TTL, TimeUnit.MINUTES);</span></span><br><span class="line">    // 7 返回</span><br><span class="line">    return Result.ok(shop);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="更新"><a href="#更新" class="headerlink" title="更新"></a>更新</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="meta">@Transactional</span> <span class="comment">// 控制更新数据库、更新缓存两个操作的原子性</span></span><br><span class="line"><span class="keyword">public</span> Result <span class="title function_">update</span><span class="params">(Shop shop)</span> &#123;</span><br><span class="line">    <span class="type">Long</span> <span class="variable">id</span> <span class="operator">=</span> shop.getId();</span><br><span class="line">    <span class="keyword">if</span> (id == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> Result.fail(<span class="string">&quot;店铺id不能为空&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 1. 更新数据库</span></span><br><span class="line">    updateById(shop);</span><br><span class="line">    <span class="comment">// 2. 删除缓存</span></span><br><span class="line">    stringRedisTemplate.delete(CACHE_SHOP_KEY + shop.getId());</span><br><span class="line">    <span class="keyword">return</span> Result.ok();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>结果就不啰嗦了，大致说一下。首先是查询，第一次查询把数据写入缓存，然后第二次再查看这个餐厅的时候就直接从缓存读了。因为这个没有管理端页面，没法发起请求，所以就用 <code>Postman</code> 来发起请求，修改名字，发起请求后，可以看到数据库被修改，缓存里没有这个数据了，直到再次请求这个商铺的信息的时候才会写入缓存。</p><p>注意 JSON 格式的数据最后一行不能有逗号，否则会报错。</p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330153425411.png" alt="image-20260330153425267" style="zoom:50%;" /><div class="tabs"><div class="nav-tabs no-default"><button type="button" class="tab"><i class="fas fa-brain" style="color: #ff69b4;"></i> <font color=#4c7fbf> 注解 Transactional 的作用是什么？怎么保证了操作的原子性？</font></button></div><div class="tab-contents"><div class="tab-item-content"><p>这里 <code>@Transactional</code> 并不是说把操作 MySQL 和操作 Redis 封装成一个事务了，而是假装封装成一个事务。</p><ul><li>假如先删除缓存，再更新数据库<br>缓存删除成功，但是数据库更新失败，这个时候会出发回滚。但是删缓存不会导致有不一致性问题，但是这个时候会导致大量请求从缓存转移到数据库，也就是出现<font color=red>缓存击穿</font></li><li>如果是先更新库，再删缓存<br>那么如果更新成功，但是删缓存报错，函数报错，触发回滚，这个时候就保证了一致性。</li></ul></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div><div class="tabs"><div class="nav-tabs no-default"><button type="button" class="tab"><i class="fas fa-brain" style="color: #ff69b4;"></i> <font color=#4c7fbf> 注解 Transactional 到底管谁呢？底层怎么做呢？</font></button></div><div class="tab-contents"><div class="tab-item-content"><p><code>@Transactional</code> 直接作用于数据库连接，只要支持 ACID 事务且提供了标准 JDBC 驱动的数据库都可以，比如 MySQL（InnoDB引擎，因为这个引擎支持事务）、Oracle、PostgreSQL、SQL Server 都能用。</p><p>当给一个方法加上 <code>@Transactional</code> 时，到底做了什么呢？</p><ol><li><p>创建代理对象<br>Spring 启动的时候，发现你这个方法 <code>update</code> 有这个注解，那就会给这个类 <code>ShopServiceImpl</code> 生成一个代理类</p></li><li><p>环绕增强</p><p>当你调用 <code>service.update(shop)</code> 时，实际上执行的是代理对象的方法：</p><ol><li><strong>开启事务：</strong> 代理对象先通过 <code>DataSource</code> 拿到一个数据库连接，执行 <code>connection.setAutoCommit(false)</code>（关掉自动提交，这是核心！）。</li><li><strong>执行业务：</strong> 调用你真正的 <code>updateById(shop)</code> 和 <code>redis.delete()</code>。</li><li><strong>判断结果：</strong><ul><li><strong>成功：</strong> 如果整个方法没报错，代理对象执行 <code>connection.commit()</code>，数据正式写入磁盘。</li><li><strong>异常：</strong> 如果抛出了 <code>RuntimeException</code>，代理对象立刻执行 <code>connection.rollback()</code>。</li></ul></li><li><strong>释放连接：</strong> 把连接还给连接池。</li></ol></li></ol><p>所以这里真正进行撤回操作的只有数据库。</p></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div><h2 id="缓存穿透"><a href="#缓存穿透" class="headerlink" title="缓存穿透"></a>缓存穿透</h2><p>缓存穿透是指客户端请求的数据在缓存中和数据库中都不存在，这样缓存永远不会生效，这些请求都会打到数据库。（有些人可能恶意请求某些不存在的数据，导致频繁访问数据库）</p><h3 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h3><p>常见的解决方案有两种</p><ol><li><p>缓存空对象：当数据库不存在这个数据的时候，把这个数据也写入缓存，只不过值为空</p><p>当客户端访问不存在的数据时，会先请求 redis，但是此时 redis 中也没有数据，就会直接访问数据库，但是数据库里也没有数据，那么这个数据就穿透了缓存，直击数据库。但是数据库能承载的并发不如 redis 这么高，所以如果大量的请求同时都来访问这个不存在的数据，那么这些请求就会访问到数据库，所以如果这个数据在数据库里不存在，我们也把这个这个数据存在 redis 中去（<code>额外的内存消耗</code>），这样下次请求这个不存在的数据时，redis 缓存中也能找到这个数据，不用去查数据库。</p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330161836173.png" alt="image-20260330161145676" style="zoom:50%;" /><ul><li>优点：实现简单，维护方便</li><li>缺点：额外的内存消耗；可能造成短期的不一致（我们通过设置 TTL 过期时间，不过在这个过期时间内可能会出现不一致，如果一致性要求高，可以在数据写入的时候把缓存更新一下）</li></ul></li><li><p>布隆过滤<br>布隆过滤器是采用哈希思想来解决这个问题，通过一个庞大的二进制数组，根据哈希思想去判断当前这个要查询的数据是否存在，如果布隆过滤器判断存在，则放行，后续操作就和前面一样了。如果布隆过滤器判断这个数据不存在，则直接返回。</p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330161947701.png" alt="image-20260330161947620" style="zoom:50%;" /><ul><li>优点：内存占用较少，没有多余 key</li><li>缺点：实现复杂（不过 Redis 提供了 bitmap 帮我们做了）；存在误判可能（因为底层是用的哈希原理，就可能会出现哈希冲突，所以如果这个里面判断不存在，那就一定不存在，判断存在也可能不存在，也会出现缓存穿透）</li></ul></li></ol><h3 id="解决商铺查询缓存穿透问题"><a href="#解决商铺查询缓存穿透问题" class="headerlink" title="解决商铺查询缓存穿透问题"></a>解决商铺查询缓存穿透问题</h3><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330162918014.png" alt="image-20260330162917946" style="zoom:25%;" /><p>原来的流程是查询的时候根据商铺 id 来从 Redis 查，如果不存在则去数据库查，查到了就写入缓存并且返回商铺信息，查不到就返回 404，说明商铺页面没找到。</p><p>解决缓存穿透问题，需要修改的地方是数据库查不到之后，把空值也写入 redis，并且返回错误信息，不过这样之后呢，取到缓存之后需要多一步操作，先判断一下是不是空值，是空值就 404，不是再返回商铺信息。</p><p>然后，我们去实现一下代码</p><figure class="highlight diff"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line">@Override</span><br><span class="line">public Result queryById(Long id) &#123;</span><br><span class="line">    String key = CACHE_SHOP_KEY + id; // cache:shop:id</span><br><span class="line">    // 1 从 Redis 查询商户缓存</span><br><span class="line">    String shopJson = stringRedisTemplate.opsForValue().get(key);</span><br><span class="line">    // 2 判断是否存在</span><br><span class="line">    if (StrUtil.isNotBlank(shopJson)) &#123;</span><br><span class="line">        // 3 存在，直接返回</span><br><span class="line">        Shop shop = JSONUtil.toBean(shopJson, Shop.class);</span><br><span class="line">        return Result.ok(shop);</span><br><span class="line">    &#125;</span><br><span class="line"><span class="addition">+   if (shopJson != null) &#123;</span></span><br><span class="line"><span class="addition">+       return Result.fail(&quot;店铺不存在&quot;);</span></span><br><span class="line"><span class="addition">+   &#125;</span></span><br><span class="line">    // 4 不存在，根据 id 查询数据库</span><br><span class="line">    Shop shop = this.getById(id);</span><br><span class="line">    // 5 不存在，空值写入缓存，返回错误</span><br><span class="line">    if (shop == null) &#123;</span><br><span class="line"><span class="addition">+       stringRedisTemplate.opsForValue().set(key, &quot;&quot;, CACHE_NULL_TTL, TimeUnit.MINUTES);</span></span><br><span class="line">        return Result.fail(&quot;店铺不存在!&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">    // 6 存在，写入 Redis</span><br><span class="line">    stringRedisTemplate.opsForValue().set(key, JSONUtil.toJsonStr(shop), CACHE_SHOP_TTL, TimeUnit.MINUTES);</span><br><span class="line">    // 7 返回</span><br><span class="line">    return Result.ok(shop);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol><li>查询数据库的时候，如果店铺不存在，也要存一份空值到缓存，注意这里是 <code>&quot;&quot;</code>，不是 <code>null</code>，并且这个 TTL 一般短一点，可以一分钟、两分钟</li><li>在查询用户缓存的时候，第 2 步结束后，如果没有返回，说明商铺信息为 <code>null</code>（说明缓存里面真的没有） 或者 <code>&quot;&quot;</code>（说明是自己设置的空值），所以需要再判断一下。</li></ol><div class="tabs"><div class="nav-tabs no-default"><button type="button" class="tab"><i class="fas fa-brain" style="color: #ff69b4;"></i> <font color=#4c7fbf>写入缓存的时候可以存 null 吗</font></button></div><div class="tab-contents"><div class="tab-item-content"><p>这个代码里面写的是 <code>&quot;&quot;</code>，存 <code>null</code> 是不可以的，会直接报错，因为 Spring Data Redis 底层在执行 <code>set</code> 操作前，会进行严格的非空检查。<br>Redis 设计意图是： <code>SET</code> 命令本身就是在存值，如果你想表达没有值，应该删除它而不是存入 <code>null</code>。</p></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div><div class="note info flat"><p>缓存穿透的解决方案有哪些？</p><ul><li>缓存 null 值</li><li>布隆过滤</li><li>增强 id 的复杂度，避免被猜测 id 规律</li><li>做好数据的基础格式校验（比如规定id长度必须为10，那不是10位的id请求直接pass）</li><li>加强用户权限校验（比如必须登录、访问的频率限制）</li><li>做好热点参数的限流（比如统计 id 被访问的次数，比如正常是每秒访问 50 次，但是某个时刻被访问了每秒 500 次，我们可以设置一个规则进行限流，只能每秒访问100次，超过直接拒绝）这里不介绍，比如阿里巴巴开源的 Sentinel 可以来做这件事。</li></ul></div><h2 id="缓存雪崩"><a href="#缓存雪崩" class="headerlink" title="缓存雪崩"></a>缓存雪崩</h2><p>缓存雪崩是指在同一时段大量的缓存 key 同时失效或者 Redis 服务宕机，导致大量请求到达数据库，带来巨大压力。</p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330171423217.png" alt="image-20260330171423142" style="zoom:30%;" /><ul><li>解决方案<ul><li>解决大量的缓存 key 同时失效问题：给不同的 Key 的 TTL 添加随机值，让其在不同时间段分批失效<br>比如平时做缓存的时候，有些数据是批量导入的，设置的相同的过期时间，比如30分钟，我们可以在这个基础上加一个 1～5 分钟的随机值，避免同时过期</li><li>解决宕机问题：利用Redis集群提高服务的可用性<br>使用一个或者多个哨兵(<code>Sentinel</code>)实例组成的系统，对redis节点进行监控，在主节点出现故障的情况下，能将从节点中的一个升级为主节点，进行故障转移，保证系统的可用性。</li><li>给缓存业务添加降级限流策略<br>发现 Redis 宕机了，对于请求进行限流、拒绝，避免将压力给到数据库</li><li>给业务添加多级缓存<br>浏览器访问静态资源时，优先读取浏览器本地缓存；访问非静态资源（ajax查询数据）时，访问服务端；请求到达Nginx后，优先读取Nginx本地缓存；如果Nginx本地缓存未命中，则去直接查询Redis（不经过Tomcat）；如果Redis查询未命中，则查询Tomcat；请求进入Tomcat后，优先查询JVM进程缓存；如果JVM进程缓存未命中，则查询数据库</li></ul></li></ul><h2 id="缓存击穿"><a href="#缓存击穿" class="headerlink" title="缓存击穿"></a>缓存击穿</h2><p>缓存击穿也叫热点 Key 问题，就是一个被<code>高并发访问</code>并且<code>缓存重建业务较复杂</code>的 key 突然失效了，那么无数请求访问就会在瞬间给数据库带来巨大的冲击</p><p>比如某个 key 失效了，但是重建这个 key 要从数据库查询，然后构建存入 Redis，但是这个构建过程复杂，耗时长，就会出现缓存击穿。</p><p>如图，假设线程1在查询缓存之后，本来应该去查询数据库，然后把这个数据重新加载到缓存的，此时只要线程1走完这个逻辑，其他线程就都能从缓存中加载这些数据了，但是假设在线程1没有走完的时候，后续的线程2，线程3，线程4同时过来访问当前这个方法， 那么这些线程都不能从缓存中查询到数据，那么他们就会同一时刻来访问查询缓存，都没查到，接着同一时间去访问数据库，同时的去执行数据库代码，对数据库访问压力过大</p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330172946756.png" alt="image-20260330172946615" style="zoom:30%;" /><h3 id="解决方案-1"><a href="#解决方案-1" class="headerlink" title="解决方案"></a>解决方案</h3><h4 id="互斥锁"><a href="#互斥锁" class="headerlink" title="互斥锁"></a>互斥锁</h4><p>利用锁的互斥性</p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330173319817.png" alt="image-20260330173319665" style="zoom:30%;" /><p>假设现在线程1过来访问，他查询缓存没有命中，但是此时他获得到了锁的资源，那么线程1就会一个人去执行逻辑，假设现在线程2过来，线程2在执行过程中，并没有获得到锁，那么线程2就可以休眠一会，然后去重试，直到线程1把锁释放后，线程2获得到锁，然后再来执行逻辑，此时就能够从缓存中拿到数据了。</p><p>问题：如果重建缓存数据时间比较久，那在这个期间所有进来的请求线程都在等待，性能比较差。</p><h4 id="逻辑过期"><a href="#逻辑过期" class="headerlink" title="逻辑过期"></a>逻辑过期</h4><p>分析：之所以会出现缓存击穿问题，主要原因是在于我们对 key 设置了 TTL，如果我们不设置 TTL，那么就不会有缓存击穿问题，但是不设置 TTL，数据又会一直占用我们的内存，所以我们可以采用逻辑过期方案</p><img src="https://typora-picture-0.oss-cn-beijing.aliyuncs.com/20260330174029956.png" alt="image-20260330174029814" style="zoom:25%;" /><p>解决方案是将 TTL 设置在 redis 的 value 中，注意：这个过期时间并不会直接作用于 Redis，而是我们后续通过逻辑去处理。假设线程1去查询缓存，然后从 value 中判断当前数据已经过期了，此时线程1去获得互斥锁，获得了锁的进程会开启一个新线程去进行之前的重建缓存数据的逻辑，直到新开的线程完成重建缓存数据逻辑之后，才会释放锁，而线程1不进行等待，直接获取过期数据然后返回，假设现在线程3过来访问，由于线程2拿着锁，所以线程3无法获得锁，线程3也直接返回数据（但只能返回旧数据，牺牲了数据一致性，换取性能上的提高），只有等待线程2重建缓存数据之后，其他线程才能返回新的数据</p><p>这种方案是在异步构建缓存数据，缺点是在重建完缓存数据之前，返回的都是旧数据</p><h4 id="对比"><a href="#对比" class="headerlink" title="对比"></a>对比</h4><table><thead><tr><th align="center">解决方案</th><th align="center">优点</th><th align="center">缺点</th></tr></thead><tbody><tr><td align="center">互斥锁</td><td align="center">没有额外的内存消耗 <br />保证一致性<br /> 实现简单</td><td align="center">线程需要等待，性能受影响 <br />可能有死锁风险</td></tr><tr><td align="center">逻辑过期</td><td align="center">线程无需等待，性能较好</td><td align="center">不保证一致性 <br />有额外内存消耗<br /> 实现复杂</td></tr></tbody></table><ul><li><code>互斥锁方案</code>：由于保证了互斥性，所以数据一致，且实现简单，只是加了一把锁而已，也没有其他的事情需要操心，所以没有额外的内存消耗（不用 expire 字段了），缺点在于有锁的情况，就可能死锁，所以只能串行执行，性能会受到影响</li><li><code>逻辑过期方案</code>：线程读取过程中不需要等待，性能好，有一个额外的线程持有锁去进行重构缓存数据，但是在重构数据完成之前，其他线程只能返回脏数据，出现数据不一致性问题，且实现起来比较麻烦</li></ul>]]>
    </content>
    <id>http://example.com/undefined/36858/</id>
    <link href="http://example.com/undefined/36858/"/>
    <published>2026-03-29T04:46:30.000Z</published>
    <summary>
      <![CDATA[<h1 id="基础篇"><a href="#基础篇" class="headerlink" title="基础篇"></a>基础篇</h1><h2 id="1-初识Redis"><a href="#1-初识Redis" class="headerlink" title="1 初]]>
    </summary>
    <title>redis笔记</title>
    <updated>2026-03-30T09:50:51.874Z</updated>
  </entry>
</feed>
